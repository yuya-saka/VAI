# 作業ログ 2026-06-11

## 1. 全z方向への線伝播を全症例へ適用

少数スライスの4本線アノテーションを、全有効zスライスへ補間・外挿して4領域マスクを生成した。

### 実装

- `Unet/preprocessing/propagate_lines_z.py`
- `Unet/preprocessing/test/test_propagate_lines_z.py`

### 方式

- `annotation_data` の元CT・マスクを使って既存PNGと同じcrop/上下反転で全zを抽出
- 各線を弧長方向へ再サンプリング
- 椎骨マスク重心とbboxサイズで正規化
- アンカー間は線形補間
- 外側は端部アンカーの傾向を使う制約付き線形外挿
- `generate_region_mask()` で4領域マスクを再生成
- 由来、アンカー距離、信頼度、QC警告を `line_provenance.json` に保存

### パイロット

`sample1/C3` で leave-one-out 確認を実施。

| 項目 | 結果 |
|------|-----:|
| 有効z範囲 | 14-86 |
| 全スライス | 73 |
| 手動 | 12 |
| 外挿 | 61 |
| マスク生成失敗 | 0 |
| Angle平均 | 0.51° |
| 領域Dice平均 | 0.9937 |

### 全症例適用

実行:

```bash
uv run python Unet/preprocessing/propagate_lines_z.py --all --skip_existing
```

結果:

| 項目 | 件数 |
|------|----:|
| 対象椎骨 | 284 |
| 成功椎骨 | 284 |
| 全zスライス | 21,507 |
| 手動 | 1,618 |
| 補間 | 362 |
| 外挿 | 19,527 |
| 4領域マスク生成失敗 | 0 |

### 補足

- `annotation_data` NIfTI: 281椎骨
- `annotation_data` NRRD: 2椎骨
- `predata_simple` fallback: 1椎骨
- 出力容量: 約2.8GB
- 出力先: `dataset_zprop/`

---

## 2. 椎体骨折検出ベースライン 設計確定（4領域mask不使用・新方向）

草案 (`memo/草案.md`) の Step1「2.5D axial MIL baseline」を、まず4領域mask抜きで構築する設計を確定。
実装は次セッションで Codex に委譲して着手する。

確定設計: `.claude/docs/fracture-baseline-design.md`
参考手法メモ: `.claude/docs/research/dmil-slice-aggregation.md`
Codex検証: `.claude/docs/codex/20260610-2330-fracture-baseline-design.md`,
            `.claude/docs/codex/20260611-loss-aug-schedule-validation.md`

### 方針

- RSNA2022 の CNN+RNN/LSTM 2-stage は 51症例では過学習リスク大 → **end-to-end 2.5D MIL** を採用。
- slice 損失集約は参考PDF (Hibi et al. 2023 IJCARS WSAD) の **DMIL + center loss** を採用。
- メインタスクは **椎体単位の骨折分類**（slice スコアは局在化の副産物）。

### データ仕様（確定）

- 入力: `dataset_zprop/{sample}/{Cx}/images/slice_XXX.png` (224x224 uint8)、`masks/` 全bag完備。
- 椎体ラベル: `add_fracture_labels.py` の `FRACTURE_VERTEBRAE`。
- slice ラベル(評価のみ): `fracture.nii.gz`、`slice_XXX`番号=z-index で整合。
- 規模: **41症例 / 284 bag / 陽性62・陰性222 (21.8%) / slice 44-138 (中央値71)**、pos_weight≈3.58。
- 10症例 (sample6, sample9 等) は CT 未生成 → 後で追加候補。

### 設計の要点（Codex 2回検証で確定）

- モデル: ResNet34 pretrained, 2.5D 3ch `[z-1,z,z+1]`, mask外抑制, per-slice logit, dropout 0.2。
- top-k: **config化**。default capped `k_i=clip(round(0.10*t_i),3,8)`、ratio `ceil(t_i/alpha)` 切替可
  （α を後から調整可能にする方針）。
- DMIL: top-k の **logit** に BCEWithLogits（sigmoid二重適用厳禁）。
- center loss: 陰性bagのみ、自己 detached mean への分散縮小。**β=5**（最初3ep 0→5 warmup, sweep {2,5,10}）。
- 不均衡: **WeightedRandomSampler(~50:50) + 両クラス同一拡張**。陽性だけ強い拡張は禁止（リーク）。
  balanced sampler 使用時、loss側に追加 class weight は重ねない。
- Augment: Affine のみ p=0.7 (rot±7°/trans±4%/scale0.95-1.05)、**flip無し**、intensity は stack/bag 同一。
- LR: **cosine 不採用**。constant + 2ep warmup、**differential LR (backbone 5e-5 / head 2e-4)**、
  AdamW wd1e-4, grad clip1.0, max35ep, early stop val AUPRC patience8。
- CV: StratifiedGroupKFold(5) by patient（リーク厳禁＝最大リスク）。
- 評価: OOF pooled 椎体 AUROC/AUPRC、患者クラスタ bootstrap CI、レベル別 C1/C2/C3-7 pooled (n_pos併記)。

### 次セッションのタスク（→ 実装済みに変更）

配置を `Unet/fracture_baseline/` から **`learning/`** に変更して実装済み（後述）。

### 未確定・後回し

- top-k default は capped、α は sweep 予定（capped vs ratio 比較）。β sweep {2,5,10}。
- PNG の window が骨折向け（bone window）に最適か未確認 → 必要なら HU 再描画を検討。
- slice-AUC 評価の z-alignment 実装（slice_XXX=z-index 前提、要動作確認）。
- 未生成10症例の dataset_zprop 追加（51症例化）。

---

## 3. 椎体骨折分類ベースライン実装（learning/ 配下）

前セクションの設計を `learning/` に実装した（36テスト全通過）。

### ファイル構成

```
learning/
├── train.py               # エントリポイント（5-fold CV・実効config保存・OOF評価）
├── config/config.yaml     # 全ハイパーパラメータ（augmentation セクション含む）
├── src/
│   ├── model.py           # ResNet34（ImageNet pretrained、dropout+1出力FC）
│   ├── dataset.py         # FractureDataset（2.5Dスタック・mask抑制・RAMキャッシュ）
│   ├── data_utils.py      # load_config / save_effective_config / split_bags_cv
│   └── trainer.py         # 1-fold学習（grad accum 4・AMP・differential LR・early stop）
├── utils/
│   ├── losses.py          # DMIL（top-k BCEWithLogits）+ center loss
│   ├── metrics.py         # AUROC/AUPRC + P/R/F1（threshold=0.5 & F1最適）
│   ├── sampler.py         # WeightedRandomSampler（bag ~50:50）
│   └── augment.py         # Affine + intensity（両クラス共通・config化）
└── test/                  # 36テスト全通過
```

### 追加実装した機能（設計外）

- **実効 config を毎回保存**（`outputs/{run_name}/config.yaml`）
- **P/R/F1 を評価に追加**（threshold=0.5 固定 + F1最適しきい値の両方）
- **epoch ログ**（`fold{N}/training.log`）: train_loss（dmil/center内訳）・val_loss・val_AUPRC・val_AUROC・LR・経過時間・best マーカー
- **RAM プリロード**（`prefetch_to_ram: true`）: NFS IO ボトルネック解消
- **augmentation 全パラメータを config 化**
- `TMPDIR=/tmp` 設定（NFS multiprocessing 一時ディレクトリエラー抑制）

### 学習コマンド

```bash
uv run python -m learning.train 2>&1 | tee learning/outputs/run1/console.log
```

### 初回学習結果（run1/fold0、5epoch時点）

| epoch | loss | val_AUPRC | val_AUROC | sec |
|------:|-----:|----------:|----------:|----:|
| 0 | 0.783 | 0.353 | 0.537 | 23 |
| 1 | 0.724 | 0.367 | 0.477 | 21 |
| 2 | 0.619 | 0.370 | 0.585 | 21 |
| 3 | 0.505 | 0.200 | 0.465 | 22 |
| 4 | 0.358 | 0.234 | 0.566 | 22 |

- train loss は順調に低下、val AUPRC は横ばい（過学習の兆候あり）
- RAMキャッシュ効果で 53秒/epoch → 21秒/epoch に高速化

### 次の候補

- backbone_lr を 5e-5 → 1e-5 に下げて過学習を抑制（小データに対して backbone が強すぎる）
- dropout 0.2 → 0.3 に上げる
- warmup_epochs 2 → 5 に増やす
- fold0 の学習完了後に OOF 集約指標を確認

---

## 4. z方向線伝播: 3D平滑外挿への全面リライト + バグ修正

### 背景と問題

既存の `propagate_lines_z.py` はアンカー間を8点ポリライン線形補間、外側を端部傾向の線形外挿していた。
今セッションで「滑らかな3D境界面」のための外挿方式を確立するため全面リライトした。

### 設計: Hybrid approach

Codex 設計 (`.claude/docs/codex/20260611-1449-smooth-3d-line-extrapolation.md`) に基づく。

| 領域 | 方式 |
|------|------|
| アンカー内部 (interior) | **従来の8点ポリライン線形補間**（精度維持のため戻した） |
| アンカー外部 (exterior) | **新方式: SliceState + tanh cap 外挿** |

### SliceState 表現（外挿用）

各アンカーを 12 スカラーに変換:
- `J_R` (2d): line1 ∩ line2 正規化 junction
- `J_L` (2d): line3 ∩ line4 正規化 junction
- `doubled` (4×2): 各線の doubled-angle `(cos 2α, sin 2α)` — 180° 曖昧性回避

### 主な追加実装

| 関数 | 役割 |
|------|------|
| `SliceState` dataclass | 12スカラー state |
| `extract_slice_state()` | TLS fit → junction + doubled-angle |
| `build_smooth_trajectory()` | CubicSpline + tail slope 推定 |
| `evaluate_trajectory()` | tanh cap 外挿 + mask area taper |
| `reconstruct_lines_from_state()` | state → 4本ポリライン再構成 |
| `propagate_interior_slice()` | 内部は旧方式8点線形補間（変更なし） |

### 重要設計: Junction-as-start-point

線を「junction を始点、逆方向へ延ばす」方式に変更。
→ `generate_region_mask()` の `_nearest_endpoint_junction` が距離=0 で正確に junction を検出できる。
（以前: junction 中心に両方向へ伸ばし → clip 後に junction 位置がずれて緑領域消失バグを引き起こした）

### バグ修正（Codex レビュー後）

Codex バグレビュー (`.claude/docs/codex/20260611-smooth-extrap-bug-review.md`) で HIGH 2件・MEDIUM 3件を検出・修正。

| # | 深刻度 | 問題 | 修正箇所 |
|---|--------|------|---------|
| 1 | HIGH | clip 後に p_far==p_start → 1点線でクラッシュ | `reconstruct_lines_from_state`: 逆方向フォールバック |
| 2 | HIGH | `hard_fail` を無視してマスク保存 | `process_vertebra`: hard_fail チェック追加 |
| 3 | MEDIUM | doubled ノルム≤1e-8 → `alpha=0` に無音で化ける | `reconstruct_lines_from_state`: `[1.0, 0.0]` フォールバック |
| 4 | MEDIUM | `trend_anchor_count=0` 負インデックス | `build_smooth_trajectory`: `k = max(1, min(k, n))` |
| 5 | MEDIUM | `propagate_interior_slice` 0除算 | `lower_idx == upper_idx` 時に early return |

### sample1 検証結果（全修正後）

| 椎骨 | angle (°) | Dice |
|------|----------:|-----:|
| C1 | 1.18 | 0.990 |
| C2 | 0.65 | 0.994 |
| C3 | 0.51 | 0.994 |
| C4 | 0.71 | 0.993 |
| C5 | 0.58 | 0.993 |
| C6 | 0.41 | 0.994 |
| C7 | 0.66 | 0.991 |

mask_fail=0 / 全椎骨正常。

### 次のタスク

- 全284椎骨を再生成: `uv run python -m Unet.preprocessing.propagate_lines_z --all --output_root dataset_zprop`
  （バグ修正前の生成物があるため再実行が必要）

