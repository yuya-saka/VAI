# 2026-06-15 作業ログ

## 作業範囲

- `Unet/line_only` をリファクタリング（学習・評価のセマンティクスは変更なし）

## 変更内容

- `Unet/line_only/src/trainer.py` を784行から351行に削減
- バリデーションメトリクスを `src/evaluation.py` に抽出
- テスト推論・メトリクス集計・JSON/画像出力を `src/inference.py` に抽出
- サンプルヒートマップ書き出しを `src/example_writer.py` に抽出
- 出力パス管理・エポックログ・wandb管理を `src/experiment.py` に抽出
- 互換性のため `evaluate`、`predict_lines_and_eval_test`、`save_examples` は `src/trainer.py` から再エクスポート
- pytestに誤収集されていた診断スクリプト3本をリネーム
- APIバウンダリテストと1エポック学習スモークテストを追加

## 検証

- `uv run pytest -o pythonpath=Unet Unet/line_only/test -q`: 36件パス
- 変更した実装ファイル全てでRuffチェック・フォーマットチェック通過
- Pythonコンパイルおよびパブリックインポートのスモークチェック通過

---

## DICOMデータ転送（夜）

- 外付けSSD（Windows `D:\data`）→ Linux サーバー（`/mnt/nfs1/home/yamamoto-hiroto/research/VAI/data/`）へ `scp -r` で転送
- 全50サンプル中44サンプルは正常転送完了（計7.7GB）
- 以下6サンプルは0バイトファイルが作成されており**再転送が必要**：
  - sample7, sample54, sample55, sample56, sample57, sample60

## 未完了・次回タスク

1. 上記6サンプルをWindowsから再転送
2. Unet/ リファクタリング継続
