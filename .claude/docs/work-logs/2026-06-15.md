# 2026-06-15 Work Log

## Scope

- Refactored `Unet/line_only` without changing training or evaluation semantics.

## Changes

- Reduced `Unet/line_only/src/trainer.py` from 784 to 351 lines.
- Extracted validation metrics into `src/evaluation.py`.
- Extracted test inference, metric aggregation, and JSON/image output into `src/inference.py`.
- Extracted sample heatmap writing into `src/example_writer.py`.
- Extracted output-path, epoch logging, and wandb management into `src/experiment.py`.
- Kept `evaluate`, `predict_lines_and_eval_test`, and `save_examples` re-exported from `src/trainer.py` for compatibility.
- Renamed three diagnostic scripts that were incorrectly collected as pytest tests.
- Added API-boundary and one-epoch training smoke tests.

## Validation

- `uv run pytest -o pythonpath=Unet Unet/line_only/test -q`: 36 passed.
- Ruff check and format check passed for all changed implementation files.
- Python compilation and public import smoke checks passed.
