# 作業ログ 2026-04-02

## 主な作業

region-mask 生成パイプラインの Phase 3〜4 実装・検証、アルゴリズム設計ミス判明。

---

## Phase 3: generate_region_mask.py 実装（→ 要再実装）

- `Unet/preprocessing/generate_region_mask.py` を Codex で実装（約850行）
- テスト6本は全通過
- **しかし後にアルゴリズム設計ミスが判明**（下記参照）

## Phase 4: パイロット100スライス実行

- スクリプト: `Unet/preprocessing/pilot_region_mask.py`
- 結果: 100/100 成功、失敗率 0%
- ただし結果は不正（watershed fallback 42%、junction_clamp 38%）

## Phase 4: 可視化（generate_region_mask の結果確認）

- スクリプト: `Unet/preprocessing/visualize_region_masks.py`
- 出力: `Unet/preprocessing/output/region_mask_viz/combined_grid.png`
- **結果: 全然だめ（領域分割が解剖学的に破綻）**

---

## ⚠️ アルゴリズム設計ミス（根本原因）

現在の実装は「line_1とline_2が交わってT字ジャンクションを作る」前提で設計されているが、
**手動アノテーションなので4本のラインは必ずしも交差しない**。

```python
# 現在のコード（間違い）
j_r = line_intersection(fitted_lines["line_1"], fitted_lines["line_2"])
# → 実在しない仮想交点を計算 → そこからrayを延ばしても意味がない
```

watershed が42%発動 = バリアが正しく4領域を形成できていない証拠。

## 再設計の候補（次セッションで方針決定）

| 案 | 概要 | メリット | デメリット |
|----|------|---------|-----------|
| オプション1: 半平面分割 | TLS直線の左右どちら側かで全ピクセルを分類。交点不要 | シンプル・ギャップなし | 直線近似、曲線アノテーションに弱い |
| オプション2: 折れ線バリア | ポリラインをそのまま壁として描画 + 端点をマスク端まで延長 → ConnectedComponents | アノテーションに忠実 | 端点ギャップ対策が必要 |

→ 次セッションで方針決定 → generate_region_mask.py 再実装
