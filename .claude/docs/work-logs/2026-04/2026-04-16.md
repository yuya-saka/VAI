# 作業ログ 2026-04-16

## 概要

- `multitask/` に aug 変換修正（ポリライン再生成方式）を適用

---

## multitask/ への aug 変換修正適用

### 変更ファイル

`Unet/multitask/src/dataset.py`

line_only で動作確認済みの修正を移植。変更内容は line_only と同一：

1. `get_transforms()` - `hm1/hm2/hm3/hm4: "image"` を廃止、`keypoint_params` 追加、常に `A.ReplayCompose` を返すよう統一
2. `PngLineDataset._swap_polylines_on_flip()` メソッド新規追加
3. `__getitem__()` - 拡張前 heatmap 生成を削除、拡張後ポリライン座標から heatmap・gt_line_params を再生成

### テスト結果

`uv run pytest Unet/multitask/test/ -v` → 19/19 pass

---

## TODO

- [ ] multitask/ で aug 変換修正の実験実施（line_only の改善効果が multitask でも出るか確認）
- [ ] reg + aug変換修正 の組み合わせ実験
- [ ] 評価コードに部位別 angle/rho 記録を追加
