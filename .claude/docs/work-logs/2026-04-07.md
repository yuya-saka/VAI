# 2026-04-07 作業ログ

## 完了した作業

### Phase 7: dataset.py への gt_masks 統合

**実装内容（Codex が実装、Claude が統括）:**

| 変更箇所 | 内容 |
|----------|------|
| `_build_index` | `gt_mask_path` を各 item に追加（存在しない場合は `None`） |
| `_load_gt_mask`（新規） | gt_mask 読み込み・リサイズ（INTER_NEAREST）・クリップ、欠損時ゼロ返却 |
| `_did_apply_horizontal_flip`（新規） | ReplayCompose から flip 判定ロジックを切り出し |
| `_swap_gt_mask_left_right`（新規） | 水平反転時に right(2) ↔ left(3) を交換 |
| `get_transforms` | `additional_targets["gt_mask"] = "mask"` を追加 |
| `__getitem__` | gt_mask ロード・拡張・`gt_region_mask`（LongTensor）・`has_gt_region_mask`（bool）を返却 |
| `_load_bad_slices` | bad_slices_all.json のリスト形式 + `"slice"` キーに対応（フォーマット修正） |

**確認済み:**
- `gt_region_mask`: shape `(224, 224)` dtype `int64`、値 0〜4 が正しく入る
- `has_gt_region_mask`: gt_mask ファイルが存在する場合 `True`
- 32/32 テスト PASS
- bad_slices 3件が正しく除外されることを確認

**Codex 分析ファイル:** `.claude/docs/codex/20260407-gtmask-dataset-integration.md`

## 次セッションでやること

- gt_region_mask を学習にどう活用するか設計
  - 選択肢A: 追加入力チャネル（`in_channels: 2 → 3`）
  - 選択肢B: 補助損失（セグメンテーション損失を追加）
  - 選択肢C: 両方
- モデル・訓練パイプラインへの統合実装
