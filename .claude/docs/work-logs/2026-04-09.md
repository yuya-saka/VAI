# 作業ログ 2026-04-09

## 実施内容

### 1. Dice スコアを評価メトリクスに追加
- `trainer.py` の `evaluate()` に `seg_dice_sum` 集計を追加
- val ループ・test 結果・wandb ログに `seg_dice` を追加
- `train_one_fold` の返却 dict にも `test_seg_dice` を追加

### 2. セグメンテーション可視化の改善
- `visualization.py`: `save_seg_overlay()` を 4 パネル化（CT | Pred | GT | Error Map）
  - Error Map: 一致ピクセル=緑、不一致ピクセル=赤、両方背景=黒
- `trainer.py`: `save_seg_examples()` 関数を新規追加
  - seg ラベルありの全テストサンプルを `vis/fold{N}/test_seg/` に保存
  - `save_examples()` からは seg overlay 保存を削除（専用関数に移管）

### 3. seg-only 実装の設計検討
- Codex に深い設計調査を依頼 → `.claude/docs/codex/20260408-seg-only-design.md`
- 転移学習は seg ラベルが十分あるため不要と判断
- 設計を確定し `unet-work-log.md` に記録

## 確定した設計決定

| 項目 | 決定内容 |
|------|---------|
| アーキテクチャ | `SegOnlyUNet`（Encoder + seg_decoder のみ）を新クラスとして追加 |
| 学習データ | `has_seg_label==True` のみ、スクラッチ学習 |
| Loss | Weighted CE + 0.3〜0.5 × Dice_fg |
| 評価指標 | fg-mDice（bg 除く 4 クラス平均）を primary に |
| 切替方法 | `model.type: SegOnlyUNet` で config 切り替え |

## 次セッション
seg-only 実装（`SegOnlyUNet` 追加 + `compute_seg_only_loss` 追加 + trainer 対応）
