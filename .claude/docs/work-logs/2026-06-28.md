# 2026-06-28 作業ログ

## RSNA SDF 4領域mask復元法の統一

- `Unet/line_only/apply_sdf_segmentation.py` の線分復元を
  `Unet/outputs/rsna_line_vis` と同じ方式へ統一した。
- 各ヒートマップから `(phi, rho)` に加えてcentroidを保持する。
- z方向にはSDFパラメータとcentroidを補間し、centroidを補間後の直線へ射影する。
- `line_1`〜`line_4` を学習データ由来のline別平均長で線分化し、
  同じ線分を `generate_region_mask()` へ渡す。
- 従来の350px線分・画像中心垂足基準の復元は廃止した。
- 5プレーン推論は1プレーンずつではなく `(5, 2, 224, 224)` のbatch推論へ変更した。
- 回帰テスト `Unet/line_only/test/test_sdf_boundary_interpolation.py` を追加。
- 関連テスト11件と対象ruff checkは成功した。

## 複数症例可視化

- seed 42で以下の5 studyを選択し、新方式でC1-C7を再生成した。
  - `1632`
  - `16806`
  - `18010`
  - `27733`
  - `30787`
- 計35枚を `Unet/outputs/sdf_segmentation_vis_multi_new/` に保存した。
- 黄色枠は固定plane 6ではなく、processing metadataの実際の
  `max_area_forced` planeを表示するよう修正した。
- 比較用study `10001` もC1-C7を新方式で再生成した。

## 参照症例ベースQC

- 従来の「全プレーン4領域必須」「固定面積比下限」は、
  正常な端プレーンを過剰検出するため廃止した。
- 上記5 studyを良好参照例として、椎体ごとに以下を比較する
  `Unet/line_only/qc_sdf_reference.py` を追加した。
  - 15プレーンの領域面積比推移
  - 各領域の出現分布
  - vertebra mask bboxで正規化した領域重心
  - region compactness
- 1,895 study / 13,265 vertebra levelを評価した。
- 合格: 12,701 level（95.7%）
- 要確認: 564 level（4.3%）、368 study
- hard failure（mask外label、mask内未割当、shape不正）: 0
- bboxなし要確認: 321 study / 496 level
- bboxあり要確認: 47 study / 68 level
- 出力:
  - `Unet/outputs/sdf_qc_reference_based/qc_report.md`
  - `Unet/outputs/sdf_qc_reference_based/level_scores.csv`
  - `Unet/outputs/sdf_qc_reference_based/failure_samples/`

## 除外方針

- bboxなしQC不合格はstudy全体ではなく、該当vertebra levelのみ除外する方針に修正した。
- `excluded_studies.csv` へ一時追加した321 studyはすべて取り消した。
- `data/rsna_data/excluded_levels.csv` に
  `sdf_region_qc_outlier_no_bbox` として496 levelを追加した。
- 既存level除外134件と合わせ、この時点で630 levelとなった。
- `excluded_studies.csv` は既存のstudy `24673` だけを保持した。
- 更新処理は
  `data_preprocessing/rsna_pipeline/update_exclusions_from_region_qc.py`
  として再実行可能にした。

## 全非除外levelのregion mask再生成

- GPUで全非除外対象を `--overwrite` 再生成した。
- 新方式で正常生成: 13,433 level
- 処理時間: 1,271秒
- 生成不能: 15 level
  - 原因は4本線のうち一部が2 anchor未満となる `insufficient_line_anchors`
  - 該当15 levelを `excluded_levels.csv` へ追加した。
- 最終的な `excluded_levels.csv` は645 level。
- 除外反映後の再走査:
  - existing mask: 13,433
  - failed: 0
  - missing: 0

### anchor不足15 level

| bbox | Study short ID | Level |
|---|---|---|
| あり | 22944 | C7 |
| あり | 28657 | C7 |
| あり | 5482 | C6 |
| なし | 10606 | C7 |
| なし | 1479 | C7 |
| なし | 17809 | C1 |
| なし | 18945 | C4 |
| なし | 19064 | C1 |
| なし | 19182 | C3 |
| なし | 2003 | C7 |
| なし | 30515 | C6, C7 |
| なし | 6200 | C4, C5 |
| なし | 8362 | C3 |

## 注意点・次アクション（region mask生成）

- 参照症例ベースQCと496 levelのQC除外判定は、全件新方式再生成より前の
  `region_4class.npy` を対象に実施した。
- 496 QC除外levelは再生成対象から外したため、旧maskのまま残っている。
- 厳密に新方式の全体分布を確定するには、必要に応じてQC除外を一時無効化して
  496 levelも再生成し、参照症例ベースQCと除外リストを再計算する。

## Unet/line_only/ リファクタリング

`region_4class.npy` 生成完了を機に、`Unet/line_only/` トップレベルのスクリプト9本を
`Unet/line_only/rsna_4region_segmentation/` パッケージへ移動・リファクタリングした。

### 削除したトップレベルスクリプト

- `apply_sdf_segmentation.py` (431行)
- `predict_rsna.py` (327行)
- `verify_seg_4region.py` (638行)
- `visualize_angle_outliers.py` (490行)
- `visualize_rsna.py` (472行)
- `collect_angle_stats.py` (344行)
- `qc_sdf_segmentation.py` (285行)
- `qc_sdf_reference.py` (405行)
- `visualize_sdf_segmentation.py` (168行)

### 新パッケージ構成（`Unet/line_only/rsna_4region_segmentation/`）

**共通モジュール（新規作成）:**

| ファイル | 内容 |
|---------|------|
| `__init__.py` | `sys.path` 管理（Unet/, VAI/ を追加） |
| `constants.py` | パス・定数・色定義を一元化 |
| `model_io.py` | `load_models()`, `compute_avg_line_lengths()` |
| `inference.py` | `PredictedLine` dataclass, `predict_single_slice()`, `predict_5planes()` |
| `exclusions.py` | `load_excluded_studies()`, `load_excluded_levels()` |
| `metadata.py` | `load_metadata()`, `find_max_area_plane_index()`, `load_max_area_indices()`, `load_classifier_plane_z_offsets()` |
| `visualization.py` | `make_region_overlay()`, `draw_lines_on_image()`, `lines_to_polylines()`, `make_heatmap_grid()`, `concat_with_separator()`, `add_title_bar()`, `ct_to_bgr()` |

**移行・リファクタリング済みスクリプト:** 9本 → 新パッケージ内に配置

### 成果

- Before: 9スクリプト計3,778行（重複コード約1,200行）
- After: 16ファイル計2,965行（共通モジュール含む、重複なし）
- `ruff check` 0エラー、`ruff format` 適用済み
- `Unet/line_only/` トップレベルは `train.py` と `__init__.py` のみに整理

### 次のタスク候補

- 骨折分類器の学習（`region_4class.npy` を使った次フェーズ）
- `Unet/line_only/` サブディレクトリ（`src/`, `utils/`, `test/`）のさらなるリファクタリング
