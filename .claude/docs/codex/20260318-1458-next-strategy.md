# Codex Analysis: Next Implementation Strategy
Date: 2026-03-18 14:58

## Question
Review experimental results and determine next implementation strategy.

Experimental Results Comparison:
```
修正前（NaN処理なし、二重平滑化あり、warmup式不完全）:
- test_mse: 0.0110
- peak_dist: 57.59 px
- perpendicular_dist: 25.25 px
- angle_error: 41.66 deg
- rho_error: 18.74 px

修正後（Critical Issues修正: NaN処理、二重平滑化削除、warmup式修正）:
- test_mse: 0.0017 (6.5x improvement)
- peak_dist: 21.29 px (2.7x improvement)
- perpendicular_dist: 5.37 px (4.7x improvement)
- angle_error: 34.30 deg (7.4 deg improvement)
- rho_error: 15.75 px (3.0 px improvement)
```

Current Configuration:
- warmup: 50 epochs, linear
- lambda_theta: 0.1
- lambda_rho: 0.05
- Total epochs: 200
- Loss formula: `(1 - 0.5*w)*mse + w*geometry`

Questions:
1. **Priority analysis**: Which improvement should we tackle first?
   - Adaptive warmup (MSE plateau based)?
   - Increase lambda_rho (0.05 → 0.5)?
   - Disable LR scheduler during warmup?
   - Other hyperparameter tuning?

2. **Adaptive warmup necessity**:
   - Results show fixed 50-epoch warmup worked well
   - Is adaptive warmup still worth the complexity given current performance?
   - Or should we focus on lambda_rho increase first?

3. **Expected improvements**:
   - If we increase lambda_rho 10x, what angle/rho error can we expect?
   - If we implement adaptive warmup, what additional gains are realistic?

4. **Risk assessment**:
   - What are the risks of each next step?
   - Which changes are safe to combine vs should be tested separately?

5. **Recommended implementation order**:
   Provide a prioritized list of next steps with rationale

6. **Success criteria**:
   What target metrics should we aim for in the next iteration?

## Codex Response

最高ROIは**Option A（lambda_rho増加、ただし"0.5へ一気に10x"ではなく段階増加）**です。

### 理由:
- 今回は `test_mse` が **0.011→0.0017 (6.5x改善)** で、熱マップ回帰はかなり改善済み。
- 残課題は `angle/rho` なので、まずは `lambda_rho` を上げて幾何誤差へ直接効かせるのが最短。
- Option B（適応warmup）は実装コストと検証ノイズが増え、改善寄与が不確実。
- Option C（両方同時）は効果の切り分けが難しく、実験ROIが下がる。

### 推奨実行:
`lambda_rho: 0.05→0.1→0.2`（必要なら `0.3`）の2〜3本を先に回し、`angle/rho` が頭打ちならその時点でOption Bを追加。

## Summary for Main Claude Code

**Priority: Increase lambda_rho gradually (Option A)**

### Key Insights:
1. **Heatmap quality is already good** (6.5x MSE improvement)
2. **Remaining issue is geometry** (angle/rho errors still high)
3. **Direct solution**: Increase lambda_rho weight to improve geometry learning
4. **Avoid complexity**: Skip adaptive warmup for now (uncertain ROI)

### Recommended Implementation Order:
1. **Step 1**: Increase `lambda_rho` from 0.05 to 0.1 (2x increase)
   - Expected: 2-5 px rho improvement
   - Risk: Low (gradual change)

2. **Step 2**: If Step 1 successful, increase to 0.2 (4x total)
   - Expected: Further rho improvement, possible angle improvement
   - Risk: Medium (may destabilize if too aggressive)

3. **Step 3**: If plateaus at 0.2, try 0.3 OR implement adaptive warmup
   - Expected: Marginal gains
   - Risk: Medium (diminishing returns)

### Success Criteria (Next Iteration):
- Rho error: <10 px (currently 15.75 px)
- Angle error: <25 deg (currently 34.30 deg)
- MSE: Maintain or improve (currently 0.0017)

### What NOT to do:
- ❌ Jump directly to lambda_rho=0.5 (10x) - too risky
- ❌ Implement adaptive warmup now - add complexity without proven need
- ❌ Combine multiple changes - can't isolate effects
