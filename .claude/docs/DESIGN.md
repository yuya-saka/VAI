# Project Design Document

> This document tracks design decisions made during conversations.
> Updated automatically by the `design-tracker` skill.

## Overview

<!-- Project purpose and goals -->

## Architecture

### Active: New Fully Supervised Cervical Fracture Region Classifier

The PI has discarded the previous four-stage 2.5D/MIL design, including all
mask-average-pooling variants. They are historical artifacts only and must not be
reused as the starting point for the new classifier.

The new task is one 4-label prediction per C1--C7 vertebra (`body`,
`right_transverse_foramen`, `left_transverse_foramen`, `posterior_elements`). Its
**region** supervision comes only from the 268 fully region-annotated
fracture-positive vertebrae. The vertebra-level fracture label is ground truth for
every vertebra and the vertebra-level task is in scope from the start (see the
two-endpoint table below).

**2026-08-07 update:** the plan document `memo/計画書/提案手法.md` now defines the
study. It supersedes the 2026-08-04 restriction that pseudo-labelling / weak
supervision "must not enter the core method": Proposed A (teacher pseudo-labels)
and Proposed B (weak supervision from vertebra labels only) are now first-class
arms alongside the supervised baselines. Supervised region learning on the 268
remains the foundation (it is Baseline 2 and the Proposed-A teacher).

**2026-08-17 update (user decision, supersedes the 2026-08-07 arm structure):**
training on the partial cohort overfits (matched fold-0 diagnostic run: best val
AUROC 0.738, val BCE degrading after epoch 21), so the study switched to
quality-filtered full-data (13,432-bag) training with standard hard-parameter-sharing multi-task
learning and missing-label masking. The rewritten `memo/計画書/提案手法.md` is
the active definition. Arms: **Baseline 0** (CT + whole mask -> CNN+LSTM ->
whole; implemented in `baseline0/` from the former full configuration), **Baseline 1** (10ch early
fusion -> shared CNN+LSTM -> whole head + 4-logit region head), **Proposed**
(shared CNN -> four mask-guided branches, each with its own LSTM). Loss is
`L = L_whole + lambda * m * L_region` with the region loss masked out wherever
the 268-bag region labels are absent (missing labels are never treated as 0),
plus two-stream sampling so annotated bags (~2% of the population) appear in
every batch. The final six configurations use method B for Control, Baseline 1,
and Proposed, plus plane-level max method A for the two Proposed-max arms.
Discarded: Baseline 2 (four independent region models), Proposed A
(pseudo-labels), Proposed B (weak supervision), all matched-cohort training
(frozen cohort artifacts are retained but unused), and the two-stem encoder
(replaced by plain early fusion). The 2026-08-04 ban on mask-average /
per-region hard pooling still stands.

**Same-day second-round user decisions:** entailed-zero region supervision on
vertebra-negative bags is NOT used (region loss touches only the 268 annotated
bags; the existing entailed-zero path in `common/losses.py` must be removed at
implementation time); the method-A aggregation function is plane-level max only;
`pos_weight=2.0` on the whole loss is fixed for
every arm; and the Proposed arm's mask injection follows PMGAN (Zhang et al.,
"Part-Aware Mask-Guided Attention for Thorax Disease Classification", Entropy
2021): per-region Mask-Guided Attention modules after the shared CNN whose
spatial attention maps are regressed to the corresponding region masks by an
RMSE loss `L_att` (training-time only, residual re-weighting `(1+m)*f`), giving
`L = L_whole + lambda*m*L_region + beta*L_att` — consistent with the
hard-pooling ban. **Evaluation-protocol review (2026-08-17, third round; user-approved).** The
frozen patient-level stratified 5-fold `folds.csv` was audited against the
frozen manifest and is sound — no patient leakage, per-fold prevalence
10.08-10.13%, 399-402 bags per level per fold, 53-56 annotated bags per fold
with R1-R4 stratified — and is reused unchanged for every arm. What changes is
the fold's *role*:

- **The outer fold is evaluation-only.** The former contract (early stopping on
  the outer validation fold, then pooling those same predictions into the
  reported OOF) is retired. Selection now uses a **cyclic single-inner-fold**:
  for outer fold `k`, inner fold is `(k+1) mod 5`; the model trains on the
  remaining three folds (8,048-8,074 bags, 159-162 annotated) with early
  stopping on inner whole-vertebra BCE, and the outer fold is then inferred
  once. The checkpoint-selection metric is inner whole-vertebra AUROC for all arms,
  because the inner fold holds only 11-12 R2 positives and region-AP-based
  selection would be unstable.
  **A stage-2 refit was considered and rejected (2026-08-17, user decision on
  compute grounds).** The rejected variant would have retrained on all four
  non-outer folds for the epoch count chosen on the inner fold, costing 10 runs
  per configuration instead of 5. Dropping it keeps the count at 5 runs, puts
  total compute at 0.75x the old protocol (three training folds instead of
  four), and removes two implementation burdens: the fixed RSNA Type1 cosine
  trajectory requires no validation-driven LR record/replay, and the "stage 2
  sees ~1.33x the optimizer steps" caveat disappears.
  Registered limitation: every reported model is trained on three folds (60% of
  the data) rather than the usual four (80%), which cuts region supervision from
  ~215 to ~160 annotated bags per fold on an endpoint whose 268 total is already
  the binding constraint. **No absolute-performance claims are made**; all
  claims are relative comparisons under an identical handicap that applies
  equally to every arm.
  (An earlier note in this document described the nested protocol as roughly
  doubling training cost. That conflated run count with compute: even the 10-run
  variant costs `0.75 + E_best/E_stop` times the old protocol, about 1.07-1.45x
  in practice, not 2x.)
- **No held-out test set**, reconfirmed. A random 20% patient test would cut the
  region endpoint from 268 to 214 (R2 59 -> 47), and the tempting alternative of
  drawing test patients only from the 1,850 non-annotated studies is
  **statistically invalid**: measurement shows the 160 annotated studies are not
  a random sample of positives (31.50% vertebra-level prevalence versus 8.24%;
  2.19 versus 1.34 fractured vertebrae per positive patient; annotated share of
  positive vertebrae ranging from 42.5% at C3 to 9.4% at C7). Annotated patients
  would enter the test set with probability zero, violating positivity, so no
  IPW or standardization can repair it; it is usable only as a domain-shift
  subgroup analysis. External-cohort validation is registered as future work,
  and the study is described as evaluated by patient-grouped nested internal
  cross-validation, not as evidence of external validity.
- **A `Control` arm (no-region-mask MTL) is added.** Its input is the same 6
  channels as Baseline 0 while its heads, region loss, sampler, and budget are
  identical to Baseline 1, so Baseline 0 -> Control isolates multi-task learning
  and Control -> Baseline 1 isolates the region-mask input alone. This is
  required because Baseline 0 emits no region logits and therefore cannot be
  compared on the region endpoint at all, and because Baseline 0 versus
  Baseline 1 confounds input channels, region head, region supervision, sampler,
  and parameter count. Control versus Baseline 1 is also the same-backbone,
  channels-only contrast that the N=268 power work identified as the only
  adequately powered region comparison (rho about 0.9, macro-AP MDE 0.027),
  whereas Baseline 1 versus Proposed differs structurally (rho 0.5-0.7, MDE
  0.047-0.059).

**SideAcc removed (2026-08-17, user decision).** The balanced side-accuracy
endpoint over the 95 R2-xor-R3 vertebrae is dropped. Localization, including
left/right discrimination, is judged by **per-region AP reported separately for
R1, R2, R3, and R4** rather than collapsed into a macro average or a dedicated
laterality metric. `common/metrics.py::side_balanced_accuracy` is no longer an
endpoint; the code cleanup rides along with the planned `common/` modification
pass. Consequence to register: SideAcc was the only endpoint immune to the
level-prior shortcut (level-only reaches macro-AP 0.451, and level-only side
accuracy was 0.511, i.e. chance), so shortcut resistance now rests entirely on
per-region AP against the level-only floors. Recomputing those floors on the
corrected labels is therefore a precondition for pre-registration, not an
optional cleanup. Vertical flip and transpose remain forbidden. Horizontal flip
is allowed only through the laterality-safe transform that flips CT/masks and
simultaneously swaps R2/R3 mask values and targets.

**Analysis plan frozen (2026-08-18, user-approved Codex recommendations).**

*Configurations — six, thirty runs* (down from eleven / fifty-five): Baseline 0;
Control-B; Baseline 1-B; Proposed-B at beta>0; Proposed-max at beta>0;
Proposed-max at beta=0. Method B is the ladder-wide default and method A is
evaluated only on Proposed. **noisy-OR is removed from every arm**: it routes
the whole-loss gradient directly into the region logits, so it alters a weak
region-supervision path rather than being a pure inference-time aggregation.

*Testing — two confirmatory hypotheses in fixed sequence*:
`H1: AUROC(Baseline 1-B) > AUROC(Control-B)` then
`H2: AUROC(Proposed-max, beta>0) > AUROC(Proposed-max, beta=0)`, both on paired
pooled-OOF vertebra AUROC over 13,432 bags, decided by a patient-clustered
bootstrap paired difference whose two-sided 95% CI lower bound exceeds zero. H2
is only confirmatory if H1 succeeds; otherwise it is exploratory. Region AP is
deliberately not primary: dropping macro-averaging turns it into a four-hypothesis
family, and **the existing MDE figures were computed for macro-AP and therefore
do not establish per-region power** — they must be recomputed. Key-secondary
families (outside the fixed sequence): Control-B versus Baseline 1-B on
per-region AP, and the floor gate.

*Two-stream loss composition*: each step draws a natural stream `W_t` (batch
`B_W`) and an annotated stream `A_t` of exactly one bag. `A_t` contributes to
`L_region` only, never to `L_whole` or `L_att`; `L_att` is computed on the
natural stream. **Baseline 0 runs the same natural sampler, the same `W_t`, and
the same optimizer-step count**, simply skipping the annotated forward pass, so
the whole-task empirical risk, bag distribution, and per-step whole coefficient
are identical across every arm and only the added region gradient differs. Epoch
length is one pass of the natural stream, `L_whole` always reduces over `B_W`,
and all arms share the natural-stream seed and order.

*lambda / beta calibration*: no grid search and no inner CV. Per outer fold,
before any optimizer update, 64 deterministic calibration batches drawn from the
three training folds are run in eval mode (no parameter or BatchNorm update) and
per-loss gradient L2 norms are taken at the last shared CNN block, giving
`lambda_k = clip_[1e-2,1e2](0.5 * exp(median_b log((g_whole+eps)/(g_region+eps))))`
with Baseline 1-B as reference, and `beta_k` in the same form with Proposed-B and
`g_att`. **The same `lambda_k` applies to every arm and configuration in that
fold; per-arm tuning is forbidden** because differing lambda between Control and
Baseline 1 would destroy the channels-only contrast. The mixing ratio is not
tuned. Non-finite gradients abort the run as an implementation error, and
clipping is logged but never used to revise the range. Zero additional full runs;
640 calibration batches total.

*Floor gate*: applied only to the pre-specified Proposed-B at beta>0, never to a
winner. The floor is a **cross-fitted OOF comparator** — for each outer fold,
Jeffreys-smoothed level-wise rates `(x+0.5)/(n+1)` estimated from the same three
training folds, assigned to the outer bags by level, pooled across folds.
Building the floor from all 268 labels and testing on those same 268 is label
leakage and is forbidden. The evaluation population is the 268 annotated
fracture-positive bags only; no whole-negative bags are added, since mixing them
in collapses AP mechanically (measured: level-only macro floor falls from 0.5026
at n_neg=0 to 0.0105 with all 12,522 negatives). Four Holm-corrected tests over
R1-R4, 10,000 patient-clustered bootstrap resamples shared between model and
floor, no refitting inside the bootstrap. Two descriptive sensitivity analyses
replace SideAcc without adding an endpoint: a within-level percentile-rank
recomputation, and an R2/R3 swap negative control whose failure would invalidate
any laterality claim.

*Floor values are frozen (2026-08-18).* The implemented cross-fitted comparator
uses only the same three outer-training folds, Jeffreys smoothing, and
`sklearn.metrics.average_precision_score` 1.9.0 threshold grouping for ties.
On the 268 annotated fracture-positive bags the frozen AP values are R1
0.4946387 / R2 0.2863489 / R3 0.4222059 / R4 0.7058684. The old and provisional
values are discarded. Patient-cluster bootstrap standard errors and per-region
normal-approximation MDEs are frozen alongside the predictions; no macro-AP
endpoint is produced.

*Registered limitations*: results hold only under the registered 60%-training
protocol and do not license claims about rank order at 80%/100% training; region
AP and floor results are internally valid conditional on the non-randomly
selected annotated-positive population; one seed per outer fold means the
bootstrap CIs exclude training stochasticity; high R2/R3 AP shows ranking ability
per label, not per-case laterality; and the six configurations, weights, testing
order, and code hash must be frozen before any outer inference, because choosing
a final set after seeing multiple configurations would turn the outer OOF back
into a tuning set.

**Implementation checkpoint (2026-08-18).** The former `baseline1/` package is
now `baseline0/`, supports the quality-filtered full 13,432-bag cohort only, and rejects the
retired matched configurations. Its root contains only `README.md` and the
package marker; implementation is separated into `cli/`, `config/`, `data/`,
`modeling/`, and `training/` by responsibility, while tests remain under
`tests/`. Shared infrastructure now enforces the cyclic
nested split, deterministic natural and annotated-cycle samplers, explicit-only
region validity, one-shot gradient-norm calibration with model/optimizer-state
invariance, and the frozen cross-fitted floor. Baseline 0 pre-specifies
validation-AUROC-best as primary and validation-PR-AUC/average-precision-best as
a secondary sensitivity checkpoint, and stops after 15 consecutive epochs
without validation-BCE improvement. For each checkpoint independently,
protocol v6 maximizes F1 on that checkpoint's validation predictions, resolves
ties toward the highest threshold, freezes the selected threshold, and applies
it to the corresponding outer predictions. Each checkpoint receives exactly
one outer inference. AUROC, PR-AUC, precision, recall, and F1 are reported for
both, but formal arm comparisons remain primary AUROC-checkpoint analyses;
no outer-optimal threshold is calculated or stored, and outer results may not
be used to switch checkpoint roles. Resume rejects old
or configuration-mismatched checkpoints. Protocol v6 uses the RSNA Type1 single
75-epoch cosine cycle (`2.3e-4` to `2.3e-5`) instead of the retired matched-run
Plateau schedule, so every arm follows the same validation-independent LR at a
given epoch. Unit, static, and one-real-bag forward/loss/backward checks pass. No full training or outer inference has
started because all six configurations, calibrated weights, testing order, and
code/config hash must be frozen first.

#### Fixed by the PI (2026-08-04)

- **Input is 2.5D**, using the unified `fracture_dataset` format. On 2026-08-11,
  the 234 real bbox-study directories from `fracture_dataset_blind` were overlaid
  onto the full 2,012-study `fracture_dataset`; the other 1,778 entries in the
  blind view had only been symlinks back to the full dataset. The rebuilt
  bbox-study files take precedence because they remove bbox-informed plane
  assignment. Runtime code no longer reads `fracture_dataset_blind`.
- **Four-region mask generation stays as-is** (line detection cutting the vertebra
  mask). Improvements to the line model proceed in parallel and are not a blocker.
- **Region annotation is capped at 268** and cannot be increased.
- **No mask-average or per-region hard pooling**, in any arm.

#### Proposed method and arms (per `memo/計画書/提案手法.md`, 2026-08-07)

Common architecture: 2.5D CNN + LSTM (RSNA-2022 1st-place type-1 style). The
proposed model takes CT + 5 masks (whole vertebra + 4 regions) through a two-stem
encoder (image stem / mask stem, fused before the shared CNN), with a shared
CNN+LSTM trunk and two heads (whole-vertebra, 4 regions). Note the plan document's
"6ch" is per-plane 5 CT channels + 5 mask channels = 10ch in the existing 2.5D
format; the two-stem split absorbs this.

**Augmentation decision (2026-08-11):** horizontal/vertical reflection and
transpose augmentation are not used in this study. The common dataset preserves
the stored image orientation and intentionally exposes no flip/remapping path.
Consequently, R2/R3 masks, targets, and validity flags are never swapped during
training. Any later augmentation must be non-reflective, must preserve the common
15-plane geometry, and must be applied consistently across arms.

**Stage1 parity update (2026-08-18, user decision):** Baseline 0 and subsequent
comparison arms use the same quality exclusions as `train_models/stage1`, giving
13,432 bags / 2,009 studies / 1,332 positives while retaining all 268 annotated
bags. The frozen fold assignments are filtered, never regenerated. Except for
horizontal flip, vertical flip, and transpose, the Stage1 augmentation recipe is
restored: brightness, affine shift/scale/rotation with `BORDER_REFLECT_101`,
blur/noise, distortion, cutout, and natural-stream batch mixup (`p=0.2`, uniform
lambda). Whole BCE also matches Stage1 exactly: positive elements receive weight
2 and the sum is normalized by the sum of weights, with epoch loss reported as
the mean of batch losses. Protocol `baseline0-nested-v7` additionally matches
Stage1's fast input path: all 15 planes are channel-stacked for one transform
call per bag, samples stay uint8 through nonblocking device transfer, unused
region masks are not loaded by Baseline 0, normalization runs on-device, and
fixed-shape cuDNN autotuning is enabled. Older runs are
not resumable and the new output root is `08_18/v4`.

**Phase-1 common foundation (2026-08-11):** the shared manifest is assembled from
the frozen fold file, complete-bag inventory, vertebra labels, and OR-aggregated
region labels, then pinned by SHA256. The deterministic dataset returns separate
`CT[15,5,224,224]` and `masks[15,5,224,224]` tensors, where mask channels are
whole/R1/R2/R3/R4. Exact-region supervision uses ordinary unweighted BCE over
available labels and entailed all-zero targets from sampled fracture-negative
vertebrae. The evaluation harness reports vertebra AUROC/AP, four separate region
APs, and R2/R3 discordant-case balanced accuracy with study-cluster bootstrap
support. `fracture_detection/common/` contains no model or model-specific loss;
each experiment project owns its architecture and training objective.

1. **Baseline 1**: CT + whole-vertebra mask -> vertebra-level classification.
   It has a required data-mode switch: `matched` uses exactly the same pinned
   2,655-bag / 1,498-patient cohort as Baseline 2 for the direct comparison. It
   keeps all 268 annotated positive bags and deterministically samples 2,387
   negative bags so its 10.094% prevalence and negative fold-by-level
   distribution match the `full` source. The `full` mode uses all 13,928
   complete bags / 2,010 patients as a separately named
   full-data experiment. The implementation sequence, file boundaries, tests,
   and rollback plan are recorded in
   `.claude/docs/work-logs/2026-08/2026-08-11-baseline1-implementation-plan.md`
   and code implementation is complete; training has not begun. Both modes use
   broadcast `BCEWithLogitsLoss` with a fixed `pos_weight=2.0`; other values are
   rejected by config validation. Experiment management follows the
   `Unet/line_only` pattern: required phase/name fields
   derive isolated local output paths, the effective config is saved locally,
   and each fold maps to one optional W&B run with epoch logs and best/final
   summaries. Local checkpoints and prediction files remain authoritative.
   The total split count stays fixed at `data.n_folds=5`, while required
   `data.start_fold` and `data.end_fold` select the inclusive training range;
   CLI fold flags only override those recorded config values temporarily. The
   training entry point supports both module execution and direct repository-root
   execution of `fracture_detection/baseline1/train.py` by adding only the
   resolved project root to the import path. Startup stages are printed with
   immediate flushing, and train/validation/final-checkpoint passes expose
   per-batch progress plus running BCE so DataLoader startup and NFS latency are
   distinguishable from a stalled process.
2. **Baseline 2**: four independent per-region CNN+LSTM models. Training data:
   the same pinned 2,655-bag natural-prevalence cohort as Baseline 1 matched,
   containing 268 annotated positives and 2,387 vertebra-negative bags. Use
   ordinary unweighted `BCEWithLogitsLoss`; annotated other-region
   negatives enter naturally as zero targets. Do not use P/H/N weighting,
   `pos_weight`, or focal loss in the baseline.
3. **Proposed A** (semi-supervised): teacher trained on the 268 region labels
   generates pseudo-labels for unannotated positives; student is the multitask
   model trained on true vertebra labels + true region labels + pseudo-labels.
   Teachers and pseudo-labels must be produced **within each outer fold**.
4. **Proposed B** (weakly supervised): region head trained from vertebra labels
   only via `y_whole = OR(y_regions)`; aggregation candidates max / smooth-max /
   noisy-OR (smooth-max primary per Codex 2026-08-06; noisy-OR has an
   explaining-away gradient failure). The 268 region labels are evaluation-only
   in this arm.

#### Two endpoints, deliberately split by statistical power

| endpoint | population | role |
|---|---|---|
| vertebra-level fracture AUROC | 14,133 vertebrae / 1,444 positive | **confirmatory** arm ranking; paired MDE 0.007-0.012 |
| per-region AP | 268 annotated vertebrae / 160 patients / R1 78, R2 59, R3 72, R4 158 | per-region reporting + shortcut-floor gates |

Region-level **arm ranking is not confirmatory** at n=268 (paired MDE 0.027-0.059,
depending on between-arm correlation) and is reported as exploratory with CIs.
Per-region absolute accuracy *is*
reportable. Each region is tested against its own level-only floor; required AP is
R1 `0.59`, R2 `0.37`, R3 `0.45`, R4 `0.72`. Note R2 has the fewest positives but the
**easiest** bar, because the level prior is nearly useless there, while R4 has the
most positives and the hardest bar.

**Floors pending recomputation (2026-08-07).** The floors above, the paired MDE
range, and the level-only macro-AP 0.451 were all derived on region counts
R1 77 / R3 71 / R4 155, produced by a keep-latest-run deduplication that has
since been found wrong (see the region-label provenance note below). They must
be recomputed on the corrected OR-aggregated labels before any gate is
pre-registered. The shifts are small (three regions gain 1-3 positives) and are
not expected to change conclusions, but the pre-registered numbers must match
the labels actually used.

#### Region-label provenance: runs are fracture sites, not repeat annotations

`fracture_region_labels_dicom.csv` has one row per `run`, and a run is a group
of contiguous fracture-bbox slices within one vertebra, built by
`Unet/dicom_bbox_annotation_tool`. Two runs on one vertebra are therefore two
disjoint fracture sites (observed gaps of 5-50 slices), each rendered and judged
separately by the annotator. 17 of the 268 vertebrae have multiple runs; in 6 of
them the runs carry different region labels, which is two sites reaching
different regions rather than a contradiction. The annotator confirmed
(2026-08-07) that each run's labels are correct as recorded.

**Incomplete-run labels are positive/unknown, not positive/negative
(2026-08-21).** A source-inventory audit found 321 annotatable runs among the
268 vertebrae with at least one recorded annotation, of which 285 are recorded
and 36 remain unreviewed across 33 vertebrae; 235 vertebrae are complete. An
observed regional `1` remains a valid bag-level positive, but a regional `0` in
an incomplete vertebra means only "absent from reviewed runs" and is unknown at
bag level. Region evaluation must therefore use all observed positives plus
zero labels only from fully reviewed vertebrae. The earlier all-268 zero-as-
negative Grad-CAM AUROC/AP summaries are invalid and are superseded by this
per-region validity mask. The same missingness must be resolved or masked in
any region-supervised training arm before confirmatory training.

R2/R3 are the **transverse** foramina (横突孔), the canal the vertebral artery
runs through. Both annotation tools' buttons said 椎間孔 (intervertebral
foramen), a different structure; the annotator confirmed on 2026-08-07 that the
labels themselves are transverse-foramen judgments and only the UI wording was
wrong, so the wording was corrected in place and no label changes were needed.

"Right"/"left" in R2/R3 are **image-side, not patient-side**: class 2 sits on
the image's right (measured mean column 155 vs 66 for class 3), and under the
radiological display convention image-right is the patient's left. Labels,
masks and class indices agree with each other, so training and evaluation are
unaffected, but any clinical statement about vertebral-artery laterality must
flip the name. Both tools now state this convention in their legend.

The per-vertebra region label is therefore the **OR across that vertebra's
runs**. `fracture_detection/folds/load_labels.py` is the single implementation
and asserts the resulting counts. Any earlier document citing R1 77 / R3 71 /
R4 155, 65 multi-region vertebrae, or a 94-vertebra R2-xor-R3 population is
using the superseded rule; the corrected values are 78 / 72 / 158, 70
multi-region vertebrae, and 95.

The primary region result is therefore the **four-component per-region AP vector**
with patient-bootstrap CIs and paired excess over the corresponding level-only
floor (`delta_AP_r = AP_model_r - AP_level-only_r`). Raw macro-AP is secondary and
descriptive only: heterogeneous prevalences and shortcut floors make it possible
for one region's performance to conceal failure in another. If one adjusted
summary is useful, report the mean ceiling-normalized excess
`mean_r(delta_AP_r / (1 - AP_level-only_r))` beside, never instead of, the four
region results.

Fracture-negative vertebrae are used for training and false-positive evaluation.
They are **not** added to the per-region AP population: doing so collapses the AP
scale (level-only floor drops 0.451 -> 0.218 -> 0.086 as negatives go 0 -> 268 ->
1072) and measures fracture detection rather than localization.

Left/right mirror-symmetry weight sharing is admissible as a **training** mechanism
for the foramen head (2.2x labeled positive sides, 59 -> 130); original laterality
must be retained and R2/R3 predictions remapped and evaluated separately. The 130
labels are clustered within vertebrae/patients rather than 130 independent cases.
Use patient-grouped folds and compare strict sharing with a side-specific head or
lightweight side-specific calibration/adapter on identical OOF splits. Diagnose a
failed symmetry assumption through per-side AP/calibration/sensitivity gaps, a
side or side-by-score effect after conditioning on level, and reproducible OOF
improvement from the side-specific alternative. This sharing applies only to bone
fracture detection; it does not assert symmetry of vertebral-artery injury or its
clinical consequences.

#### Confirmatory interpretation contract

The strongest confirmatory regional claim is absolute, not comparative: for every
region whose multiplicity-controlled gate passes, the corresponding model provides
fracture-localization information beyond that region's prespecified level-only
anatomical-prior AP floor. A significant vertebra-level AUROC advantage of the
four-model fusion over the whole-vertebra baseline is a separate confirmatory claim
on the full cohort. Pairwise regional-AP rankings, claims that fusion improves
localization, and claims that better localization mediates any vertebra-level AUROC
gain remain exploratory regardless of their point estimates. If regional arm
comparisons are null but the fusion improves vertebra-level AUROC, the supported
conclusion is that the region-decomposed fusion improves vertebra-level
discrimination; it does not establish superior regional localization. Null regional
comparisons are not evidence of equivalence at the current MDE.

#### Slice count (DECIDED 2026-08-07: fixed 15 planes for all experiments)

User decision on 2026-08-07 (explicit choice among 15-fixed / full-z-first /
variable-length-capable), corrected on 2026-08-11 for dataset provenance:
**all experiments run on the unified `fracture_dataset` 15-plane format**. The
234 bbox-study directories use the rebuilt label-blind plane assignment formerly
stored as real directories under `fracture_dataset_blind`; all remaining studies
retain their existing 15-plane data. This is the frozen preprocessing contract
for every arm and must not change once the arm comparison starts.

Registered limitations accepted with this decision (from measured data): the 15
planes cover only **77.5% (median) of the vertebra SI extent** (p5 62.5%) and
systematically drop the superior/inferior ends, so endplate-involving fractures
can be invisible. Consequences to keep in mind when interpreting results: (a) R1
(body) performance is structurally disadvantaged; (b) in Proposed B, the
"vertebra-positive => some region positive" constraint is label noise for
positives whose evidence lies outside the 15 planes (Codex 2026-08-06 called this
forced hallucination for weak positives); (c) results must not be compared
against any future full-z rerun without rerunning all arms on the new manifest.

#### Still open

- Whether training uses vertebra-level labels for all 14,133 vertebrae jointly with
  the 268 region labels, or the region task is trained on 268 + negatives only. Must
  be identical across arms either way.
- Proposed-B aggregation choice (max / smooth-max / noisy-OR): implement all three,
  smooth-max as the working primary, decide by comparison before pre-registration.
- The vertebra-level aggregation rule, which must be identical across arms or the
  comparison measures the aggregation rule rather than the architecture.
- Pseudo-label details for Proposed A (hard vs soft labels, confidence
  thresholding, loss weight) -- consult the fundus-paper read
  (`.claude/docs/codex/20260806-fundus-semisup-multitask-paper-read.md`) at
  implementation time.

#### Proposed-B design consultation recommendation (2026-08-10; pending PI acceptance)

This recommendation is recorded as pending because it conflicts with the
2026-08-04 blanket prohibition on mask-average pooling. If accepted, that older
prohibition must be revised explicitly for Proposed B before pre-registration.

- Use one full-context CNN pass per plane, followed by anatomically grounded,
  mask-normalized pooling from a stride-16 spatial feature map. Use a fixed soft
  support (region interior plus a small distance-decayed halo constrained to the
  whole-vertebra mask), then one weight-shared BiLSTM and scalar head for all four
  regions. Do not concatenate a shared global feature into each branch. Keep the
  ungrounded four-logit head and four hard-masked weight-shared passes as
  architecture ablations.
- Treat the I3Dr paper as the source of the weight-shared, anatomically separated
  evidence principle, not as an algebra that transfers unchanged. I3Dr's linear
  identity distributes a linear layer and its bias over an additive sum; a
  nonlinear OR / normalized log-mean-exp has no literal train/inference swap.
- Aggregate valid plane logits with normalized log-mean-exp at fixed
  `tau_plane=1`; aggregate valid region logits with the already selected
  normalized log-mean-exp at fixed `tau_region=1` plus a learnable scalar bias.
  These are independent fixed constants even though their primary values match.
- Use asymmetric entailment while retaining aggregate supervision on negative
  bags so that the learnable aggregate bias is identifiable: balanced positive
  and negative class means; positive bags receive aggregate BCE only; the
  negative term is the normalized average of aggregate-negative BCE and direct
  per-region negative BCE. Never repeat a positive vertebra label over planes.
- Do not add a global bypass head to primary Proposed B. A Baseline-1-driven
  common logit shift may be evaluated only as a separately named secondary
  ensemble, never as the Proposed-B primary result.
- A plane whose raw region mask is empty is removed from that region's temporal
  sequence and plane aggregation, rather than represented as a zero-evidence
  observation. An entirely absent region is excluded from whole aggregation and
  loss, receives an explicit validity flag and the lowest score for the
  all-annotated-bag primary region evaluation, and is reported in a valid-only
  sensitivity analysis.
- The 268 region labels remain fully hidden from epoch monitoring, model
  selection, thresholding, and hyperparameter choice. In particular, neither
  region AP nor SideAcc may be logged on train-fold annotated bags. They are
  evaluated only from locked OOF predictions after each fold model is finalized.
- Region outputs are weakly supervised evidence scores, not calibrated
  probabilities: with only one vertebra bit, regional attribution is not
  identifiable, and the 15-plane observation can miss anatomy responsible for a
  positive vertebra label.

### Legacy/Discarded: Stage3 Hierarchical Weak-Label Model

```text
15 planes × (5-channel 2.5D CT + vertebra mask)
    -> shared EfficientNetV2-S encoder
    -> four-scale FPN fusion (256 channels)
    -> anatomical four-region mask pooling
    -> shared 2-layer bidirectional LSTM over 15 planes
    -> contextual evidence logit for every plane and region
    -> evidence-tied attention pooling over planes
    -> normalized smooth-max pooling over regions
    -> one vertebra fracture logit
```

Stage3 has no direct whole-image classification head. Training uses vertebra-level
fracture labels for the final bag BCE and applies an auxiliary instance-level
negative penalty only to fracture-negative vertebrae. Region outputs are contextual
fracture-candidate evidence, not independently supervised or calibrated regional
fracture probabilities.

**Retrospective corrected-label region audit (2026-08-21).** Stage3
`baseline_3/v1` region evidence was evaluated with the same positive-or-complete
target-validity rule used by the Baseline 0 Grad-CAM audit. The 216-bag OOF
subset gave AUROC/AP of R1 `0.758/0.747`, R2 `0.755/0.590`, R3 `0.662/0.506`,
and R4 `0.772/0.833`. The separate 52-bag fixed-test subset, using the mean of
five fold-specific region evidence logits, gave R1 `0.783/0.708`, R2
`0.576/0.629`, R3 `0.583/0.481`, and R4 `0.769/0.867`. Test R1 and R3 contain
only 8 and 9 positives, respectively. These are exploratory ranking metrics for
uncalibrated contextual evidence, not regional probability accuracy; OOF and
fixed-test results remain separate, and neither is directly compared as an arm
effect against the newer Baseline 0 nested-OOF Grad-CAM audit because the score,
cohort, training protocol, and checkpoint-selection protocol differ. Artifacts
are stored under `train_models/stage3/outputs/baseline_3/v1/region_evidence_*`.

**Matched descriptive comparison with Baseline 0 CAM-only (2026-08-21).** On
the same valid examples within each Stage3 partition, OOF macro AUROC was
`0.736` for Stage3 evidence versus `0.769` for CAM density, while macro AP was
`0.669` versus `0.651`. Fixed-test macro AUROC was `0.678` versus `0.816`, and
macro AP was `0.671` versus `0.733`. OOF R3 was the only region whose paired
patient-bootstrap difference excluded zero: Stage3 minus CAM was `-0.125`
AUROC (95% CI `[-0.227, -0.016]`) and `-0.163` AP (`[-0.289, -0.012]`). The
other per-region intervals crossed zero. Thus CAM-only is more consistent
across regions and clearly better for R3 in this retrospective comparison,
whereas Stage3 shows descriptive AP gains in OOF R1/R2 and both AUROC/AP gains
in OOF R4 without conclusive paired intervals. This remains a score comparison,
not a causal arm comparison, because model protocols differ.

## Implementation Plan

### Patterns & Approaches

<!-- Design patterns, architectural approaches -->

| Pattern | Purpose | Notes |
|---------|---------|-------|
| Provider fallback wrapper | Rate-limit resilience across Claude/Gemini | Centralized error parsing, retry, and provider switch |
| Canonical conversation store | Preserve context across provider switches | Single source of truth (history + system prompt + tool logs) |
| Heatmap-moment line geometry loss | Add geometric supervision for line detection | Normal-form (phi, rho) from GT endpoints and heatmap moments; sign alignment; staged warmup |

### Libraries & Roles

<!-- Libraries and their responsibilities -->

| Library | Role | Version | Notes |
|---------|------|---------|-------|
| | | | |

### Key Decisions

<!-- Important decisions and their rationale -->

| Decision | Rationale | Alternatives Considered | Date |
|----------|-----------|------------------------|------|
| Audit Baseline 0's high fracture scores with checkpoint-matched OOF Grad-CAM at `encoder.bn2`, targeting the registered bag readout `mean(sigmoid(plane_logits))`; use both fold-by-level TP/FP sampling and a separate all-268 annotation-bearing-bag audit, report anatomical-region CAM mass plus area-corrected density, and compare observed-positive versus fully reviewed-negative density with per-region validity masks, patient-cluster bootstrap, and within-level rank sensitivity | Matching every bag to its outer-fold checkpoint avoids train-set explanations, stratification prevents saturated C2 predictions from dominating the qualitative audit, the bag-score target includes BiLSTM context across all 15 planes, area correction distinguishes concentration from region size, and the annotation-bearing audit tests whether post-hoc evidence follows known region labels rather than merely the whole-fracture score. Unreviewed-run zeros are unknown and excluded. The 7x7 Grad-CAM remains a coarse exploratory diagnostic rather than a registered region endpoint or pixel-level fracture localization | Reuse one fold model for all bags; inspect only global top scores; treat zeros from incomplete bags as negatives; explain isolated plane logits; report raw region mass alone; interpret Grad-CAM as a causal lesion map | 2026-08-21 |
| If piloting Baseline 0 initialization for regional fine-tuning, make it an exploratory fold-matched 6-channel Control-B ablation: transfer each outer fold's leakage-safe Baseline 0 encoder, BiLSTM, and whole head; train a new four-region head first with the trunk frozen and the positive-or-complete validity mask; only then consider low-LR last-block/BiLSTM unfreezing while retaining the full natural-stream whole loss. Do not optimize Grad-CAM directly and do not replace the frozen six-arm study | CAM-only ranking shows that the Baseline 0 representation already contains regional signal, so a head-first transfer test can ask whether explicit labels add value with minimal variance. Only ~160 annotated bags and roughly 35 R2 positives are available in each three-fold training split, making full-model annotation-only tuning prone to memorization and catastrophic loss of whole-fracture performance. Fold-matched initialization prevents outer leakage; masked targets prevent unreviewed zeros becoming false negatives; keeping this as an independent ablation preserves the registered study | Fine-tune the full model on all 268 annotated positives; use one all-data Baseline 0 checkpoint in every fold; treat all recorded zeros as negative; train from Grad-CAM loss with second-order gradients; silently change the frozen confirmatory arms | 2026-08-21 |
| Do not expand `mtl_type2` v1 beyond its exploratory outer0 run as-is. Any follow-up must first use per-region positive-or-complete validity, measure gradients on actual region-update steps, freeze the executed source/config provenance, and isolate one change at a time. Prefer fold-matched Baseline 0 transfer into the region sequence path or retain the shared BiLSTM rather than randomly initializing a separate 4.86M-parameter region BiLSTM | Outer0 underperformed Baseline 0 and Baseline 1-B on whole AUROC/PRAUC and collapsed region identity: R1-R3 scores correlated 0.973-0.983, R4 won 55/56 annotated bags, and R3 AUROC was 0.317. The separate region path received only 40 updates/epoch from 159 unique bags and no whole-loss supervision; partial-annotation zeros, uncalibrated `lambda=1`, non-overlapping gradient-measurement steps, and whole-best rather than region-best inference further confound the run. Source edits during execution also prevent exact provenance | Run outer1-4 unchanged; attribute the result to branch separation alone despite simultaneous schedule/batch/lambda changes; treat `pretrained: true` as Baseline 0 transfer; accept all partial-label zeros as negatives; regard the current outer0 artifact as confirmatory | 2026-08-21 |
| Add optional fold-process multi-GPU execution for Baseline 1 and later arms: run one fold per independent process and GPU; expose `parallel.mode`, `gpu_ids`, and `max_concurrent_folds`; freeze the fold-to-GPU mapping; and require the same GPU model and compute capability across arms. Do not use within-fold DDP, DataParallel, or FSDP in formal runs | Fold-level parallelism preserves global batch 16, BN behavior, losses, sampler order, optimizer-step count, and per-fold RNG/checkpoint ownership while reducing wall-clock time. Within-fold replication would change per-rank BN or batch semantics and would not reduce each model replica's VRAM requirement | Single-GPU-only execution; within-fold DDP/DataParallel/FSDP; ad hoc or heterogeneous GPU assignment | 2026-08-19 |
| Harden the MTL-arm implementation contract before any full training: use BN-module-only eval on the annotated stream; run natural backward before annotated forward/backward with one optimizer step; isolate augmentation, mixup, and annotated-model RNG streams and checkpoint their states; define every late branch as `blocks[5] -> conv_head -> bn2 -> GAP -> BiLSTM`; finish every arm and resource smoke test before freezing one source/config/dependency manifest; store lambda/beta raw calibration separately and generate immutable `loss_weights.json` only after both exist; and replace the 65-80M estimate with measured lower bounds of 84,290,740 parameters for Proposed-max and 104,041,693 for Proposed-B before MA modules | The joint Claude/Codex review found that `momentum=0` still normalizes from the one-bag annotated batch, shared global RNG silently changes later natural mixup draws, training Baseline 1 before modifying shared core breaks literal same-hash parity, omitting `bn2` leaves the timm branch boundary ambiguous, one combined backward retains both graphs, a lambda-only immutable JSON cannot later accept beta, and the old resource estimate omitted duplicated recurrent/attention work. The corrected contract protects arm comparability, resume reproducibility, peak VRAM, and artifact immutability | Whole-model eval or train-mode `momentum=0`; global RNG; train each arm as soon as implemented; omit/share `bn2`; retain both graphs for one backward; mutate one calibration JSON; keep the 65-80M/174-hour estimate | 2026-08-19 |
| Treat pre-freeze smoke as train/inner-validation only and carry a transitive provenance chain from raw lambda/beta artifacts and resource profiles into the frozen manifest and every formal outer-prediction row | Outer predictions before freezing violate the registered no-peeking order even when stored under a smoke directory. Hashing only the final weights file cannot prove that calibration/profile measurements used the same source, dependencies, input manifest, folds, and effective reference config. Embedding the frozen-manifest hash in formal OOF rows lets analysis reject mixed runs | Let smoke invoke outer inference; trust artifact filenames; record only the final loss-weight hash; combine OOF files without manifest identity | 2026-08-19 |
| Normalize every serialized torch RNG state to a contiguous CPU `uint8` tensor at the RNG restore boundary, even when the containing checkpoint is loaded with `map_location=cuda` | PyTorch applies `map_location` to RNG tensors as well as model tensors, but CPU generators and `torch.set_rng_state` require CPU byte tensors. Boundary normalization preserves the existing device-local model/optimizer load path while making GPU resume deterministic and valid | Load the entire checkpoint on CPU; omit RNG restore; special-case only the global CPU RNG state | 2026-08-19 |
| Freeze the formal MTL experiment to homogeneous A6000 devices `[0, 1, 2]` with at most three concurrent fold processes for every registered configuration, mapping folds `0/1/2/3/4` to GPUs `0/1/2/0/1` | All three devices are idle and homogeneous, fold-level parallelism preserves the registered batch and BN semantics, and one common mapping is required by the frozen-manifest guard across all six configurations | Single-GPU serial execution; use only two available GPUs; configure a different mapping per arm | 2026-08-19 |
| Run gradient calibration with recurrent/dropout modules in training mode, BatchNorm modules only in evaluation mode, BF16 autocast on CUDA, CPU-resident audit snapshots, and exact global-RNG restoration | The first formal calibration exposed two implementation-contract violations: whole-model eval makes cuDNN LSTM backward invalid, while FP32 Proposed-B calibration plus a GPU-resident model snapshot exhausts a 48 GB A6000. BN-only eval prevents buffer mutation, BF16 matches formal training memory semantics, CPU snapshots preserve exact state auditing without consuming VRAM, and RNG restoration keeps dropout from changing subsequent streams | Whole-model eval; full-FP32 calibration; GPU-resident state clones; disable stochastic layers globally | 2026-08-19 |
| Reset the four-region fracture-localization project to a from-scratch fully supervised design and prohibit every mask-average-pooling design; treat the previous Stage1--Stage4 2.5D/MIL systems as discarded historical work, keep the ~1,300 unannotated fracture-positive vertebrae's *inferred region labels* out of the core phase (their ground-truth vertebra-level fracture label is still used), and admit region-label weak/semi-supervision only as a later attachment | The PI explicitly reset the project and mask pooling has been rejected. A clean boundary prevents accidental architectural reuse and makes the 268-label result interpretable | Reuse or fine-tune Stage1--Stage4; build region weak-labels into the initial model; pool features through anatomical masks | 2026-08-04 |
| Evaluate per-region detection with patient-grouped five-fold pooled OOF AP computed separately for each of the 4 regions on only the 268 annotated fracture-positive vertebrae, each tested against its own region-specific level-only floor (R1 0.59, R2 0.37, R3 0.45, R4 0.72); use sampled fracture-negative vertebrae for training and report their false-positive behavior separately, never mixed into the per-region AP population | Adding thousands of all-negative vertebrae to AP changes its no-skill prevalence and allows easy fracture-vs-normal separation to dominate the anatomical-localization endpoint. Regions have very different anatomical-prior floors, so a single macro-AP number can hide a region that fails its own floor | Primary AP over an arbitrary mixture of positives and sampled negatives; a single macro-AP headline number; fold-mean t-tests; comparison only against the pooled no-skill floor | 2026-08-04, revised 2026-08-05 |
| Align `line_2p5d` evaluation and output artifacts with `line_only`: use adaptive thresholding (`mode=adaptive`, `min=0.10`, `peak_ratio=0.4`), evaluate every epoch, report configured angle/rho outlier rates, save VAL/TEST heatmap examples, and save TEST-wide line comparison images plus prediction JSON. Apply `line_extend_ratio` to GT-length prediction segments only for rendering | The new experiment must remain directly inspectable beside the established line detector. The extension ratio changes rendered endpoints but not moment-derived angle/rho metrics, while matching output artifacts makes broken heatmaps and line geometry visually auditable | Metrics-only evaluation; add an unused line extension setting; depend directly on `line_only` implementation files | 2026-08-04 |
| Manage `line_2p5d` experiments with required `experiment.phase` and `experiment.name`, deriving the output root as `Unet/outputs/{phase}/{name}` just like `line_only` | Phase groups comparable experiment variants and the name uniquely identifies each run without repeating or manually coordinating an output directory | Keep `experiment.output_dir + name`; use only one flat experiment name; require a CLI output path | 2026-08-04 |
| Give `line_2p5d` the same config-driven W&B surface as `line_only` (`enabled`, `project`, `run_name`), derive null project/run names from `phase + name` and fold, and log scalar epoch metrics plus best-checkpoint and test summaries | Filesystem experiment naming and online tracking must identify the same run, while scalar-only logging avoids sending nested per-line payloads unintentionally | No W&B integration; hard-code project names; log only training loss; send the full nested metrics object | 2026-08-04 |
| Configure the `line_2p5d` execution range with top-level `folds.start` and `folds.end`; retain `--start_fold` and `--end_fold` only as optional CLI overrides | Fold execution is part of the reproducible experiment definition and should not depend on remembering command-line arguments. Optional overrides remain useful for temporary reruns | Require CLI arguments for every run; infer completed folds from output files; store the range under unrelated model or loss settings | 2026-08-04 |
| Use the same plain elementwise mean MSE as `line_only` for the supervised center-slice heatmap term in `line_2p5d`; do not add positive weighting, Dice, focal, or other heatmap losses. Keep optional delayed local geometry as the only loss difference between the paired `baseline.yaml` and `geometry.yaml` experiments | The first comparison must isolate the effect of local multi-slice geometry after ordinary heatmap learning has converged. Changing the heatmap objective at the same time would confound the result and break parity with the established line detector | Weighted MSE plus soft Dice; focal/BCE heatmap loss; retain multiple configurable heatmap objectives | 2026-08-04 |
| Add an independent `Unet/line_2p5d/` experiment that predicts all five local slices from explicit `[z-2,z-1,z,z+1,z+2]` CT-plus-mask inputs using one slice-shared 2D U-Net, a zero-initialized bottleneck residual temporal convolution, and one shared four-line head. Supervise each manual center image exactly once; use neighboring outputs only for optional local doubled-angle and normal-direction centroid-curvature consistency. Keep all plane, signed-tilt, and extrapolation logic outside this project. Gate the geometric loss with `enabled`, `start_epoch`, and `ramp_epochs`, and compare it against an otherwise identical heatmap-only config | The previous `2N -> 4N` channel-stacked slab model had position-specific input/output channels and no structural reason for one slice's broken heatmap to agree with its neighbors. Slice-shared weights remove slot-specific heads, lightweight z fusion lets adjacent evidence repair an isolated weak slice, and delayed local consistency avoids forcing early noisy predictions to become consistently wrong. A center-once dataset and center-only evaluation also prevent overlapping windows from duplicating supervision or hiding image-level errors | Continue the channel-stacked slab model; add LSTM/RNN/ConvLSTM; use a full 3D U-Net; impose a global plane or tilt target; average overlapping predictions only after inference | 2026-08-04 |
| Explain the poster's anatomical four-region auto-segmentation method with a five-step visual pipeline before the qualitative image: cervical CT input, target-vertebra extraction and orientation normalization, anatomical-boundary inference, cross-slice boundary interpolation/propagation, and final four-region mask generation | The segmentation image alone shows only the output and does not communicate what is automated or how consistency across slices is achieved. A concise process diagram makes the first proposed method independently understandable while keeping sensitive implementation details hidden | Show only the qualitative overlay; add a long prose paragraph; expose the exact line model, SDF coefficients, and propagation parameters | 2026-07-20 |
| Divide the poster's proposed method into two explicit numbered subsections: `1. 頸椎の解剖学的4領域自動分割手法` for the anatomical mask-generation image and `2. 深層学習による解剖学的領域別の骨折検出法` for the hierarchical weak-label architecture | Separates the upstream anatomical partitioning contribution from the downstream fracture-learning contribution and gives the architecture figure an unambiguous methodological role | Keep one undifferentiated proposed-method section; label the second subsection only as generic weak-label learning | 2026-07-20 |
| In the poster's weak-label fracture-learning architecture figure, explicitly show the dual CT/4-region-mask inputs, shared feature extraction, region-mask pooling, four parallel slice-to-region evidence paths, region-to-vertebra aggregation, and the supervision boundary where only the final vertebra label contributes training error | A generic linear processing flow does not communicate why the method is weakly supervised or hierarchical. Showing the missing region labels and the two aggregation levels makes the research contribution understandable without exposing model names, dimensions, or hyperparameters | Keep the generic six-box flow; show detailed implementation classes and tensor sizes; omit the supervision path | 2026-07-20 |
| Structure the company-facing poster draft as a title-and-author header followed by full-width research background and proposed-method sections, then a two-column lower block with experimental results beside discussion/outlook; omit the reference section from the deliverable | Matches the requested visual reading order, keeps the two essential figures together in the method section, and lets the outcome and its limitations/future work be compared without extending the poster vertically | Retain the long internal-authoring document with references and disclosure tables; use three equal columns throughout; stack results and outlook vertically | 2026-07-20 |
| For the company-facing research summary on anatomical-region cervical fracture detection, preserve the four-region anatomy, vertebra-label-only hierarchical weak-supervision path, and confirmed ability to emit region-wise fracture-candidate evidence, while replacing dataset/model names, cohort and input counts, hyperparameters, case IDs, and exact metrics with generic descriptions. Label region outputs as contextual fracture-candidate evidence rather than calibrated regional probabilities, and position vertebral artery injury as the future risk-assessment target rather than a directly validated diagnosis | Keeps the scientific mechanism and current technical achievement understandable without disclosing reproducibility-sensitive details or overstating region-level ground-truth validation and VAI prediction, neither of which the current Stage3 experiment establishes | Publish exact data/model/training/performance details; remove the weak-label and four-region mechanism; describe region evidence as validated fracture probability or direct VAI diagnosis | 2026-07-20 |
| Regenerate bbox-bearing data as a separate bbox-centered view after canonical 3D bbox geometry is built. Treat every contiguous bbox run independently; compute the bbox cross-sectional area curve along the corrected vertebra normal, choose an occupied plane nearest the volume-median position as exact sequence index 7, and place the remaining 14 planes symmetrically with enough physical extent to cover the run plus vertebral context. Split exceptionally long runs into overlapping windows instead of lowering resolution excessively. Resample CT, vertebra mask, line/region geometry, and corrected bbox occupancy from the same new `PhysicalPlane` list, and keep the existing full-vertebra dataset unchanged | The current selector only inserts one representative bbox position among 15 planes spanning the vertebral robust range, so the lesion is not centered: across 440 bbox-bearing study-level pairs, the bbox center is a median two sequence indices from index 7 and at least five indices away in 10% of pairs. Bbox support spans are also variable (median 14.5 mm, 90th percentile 29.9 mm), so one fixed narrow spacing would omit substantial annotations. A separate run-centered view guarantees a real bbox cross-section at the middle plane without creating a positive-label-correlated replacement for the primary full-vertebra sampling protocol | Keep full-range planes and force one bbox anchor; center on the bbox support midpoint even if it falls in a gap; use one fixed spacing for every run; overwrite bbox-positive samples in the primary dataset; reuse old region masks after changing CT planes | 2026-07-19 |
| In bbox-centered QC figures, overlay the generated four-region mask and display the corrected bbox as the exact union of the 3D per-slice envelope intersections. Simplify only the displayed supersampling contour with subpixel Douglas-Peucker tolerance; do not force the occupancy into a rectangle or convex hull | A corrected plane intersects a 3D envelope, not one rigidly transformed 2D rectangle, so valid sections may be triangular, trapezoidal, hexagonal, non-convex, or disconnected when source boxes vary across slices. The stored supersampled contour nevertheless contained a median 182 raster-boundary vertices, which added visual stair-step noise unrelated to geometry. Display-only simplification makes QC readable without altering the soft occupancy used downstream | Force every corrected bbox to an axis-aligned rectangle; convex-hull every section; smooth or simplify the stored occupancy; show bbox without four-region context | 2026-07-19 |
| Separate bbox geometry correction from four-region soft-label construction. First build canonical corrected bbox polygons/occupancy only: convert every native bbox rectangle with that slice's original DICOM header to subpixel LPS corners, split by true contiguous DICOM-index runs, construct a piecewise 3D bbox envelope without bridging gaps, intersect it with every corrected classifier plane, and preserve the resulting polygons/multi-plane occupancy without reducing them to a nearest-plane axis-aligned rectangle. Only after geometry overlays pass QC may the corrected bbox be combined with region masks for soft-label generation. Production generation must default to `native_dicom`; repaired-affine geometry requires an explicit QC-only override because the resampled CT geometry is approximate | The current nearest-plane projection collapses a nonparallel native bbox onto one corrected plane and replaces its projected quadrilateral with an axis-aligned rectangle. Across 7,217 bbox rows, the bbox corners span a median 6.87 mm and up to 48.19 mm along the corrected plane normal; the axis-aligned rectangle inflates projected area by up to 1.53x. Geometry correctness must therefore be independently testable before introducing center weighting, region overlap, or label semantics. Slice-specific original DICOM headers avoid interpreting bbox pixels through repaired-affine approximations, but cannot make an approximate repaired CT geometry exact | Combine geometry correction and soft-label heuristics in one step; hard overlap thresholding; project an axis-aligned bbox to the nearest classifier plane; silently accept repaired-affine CT/bbox overlays as exact; manually redraw every bbox after correction | 2026-07-19 |
| Gate every Stage3 checkpoint with a fixed-holdout data-protocol version and train/validation manifest hash; persist and verify `split_manifest.csv` before training; save OOF metrics/predictions/evidence per fold and rebuild root OOF/test artifacts from every completed compatible fold; emit evidence-collapse diagnostics in `summary.json` | The initial Stage3 run used all studies for CV, and the old checkpoint/config alone could not reveal that contamination. Without per-fold artifacts, later fold sessions also overwrote earlier OOF outputs. Explicit protocol and manifest identities make incompatible checkpoints fail before use, while fold-local artifacts make resumed multi-session aggregation reproducible and diagnostics make the Stage3 go/no-go evidence inspectable | Rely on config equality only; permit old checkpoints after changing split semantics; aggregate only folds trained in the current process; require manual NPZ analysis | 2026-07-16 |
| Preserve the Stage1/Stage2 fixed study-level 20% test holdout in every Stage3 experiment: split it before fold construction, train CV folds only on the remaining 80%, and emit ensemble test predictions, per-fold test predictions, per-fold contextual evidence, and test metrics after training | Stage3 must remain directly comparable with Stage1/2, and the fixed test cohort must never influence fitting or checkpoint selection. Running CV over all studies contaminates that cohort and makes the resulting checkpoint unusable for unbiased test evaluation | Use all studies for CV and report OOF only; add test inference without retraining the contaminated folds; create a new Stage3-specific test split | 2026-07-16 |
| Restrict the fracture-region annotation tool target list to fracture-positive `(study, level)` pairs present in canonical `fracture_bbox_planes.csv`; do not display other positive vertebrae merely because another level in the same study has a bbox | Region annotation requires visible bbox evidence for the selected vertebra. Study-level filtering exposed 25 positive levels with no assigned bbox, leaving no location to annotate and creating avoidable ambiguity | Display every positive level in bbox-bearing studies and mark missing pairs with `has_bbox=false` | 2026-07-15 |
| Assign every RSNA fracture bbox CSV row independently to the nearest fracture-positive vertebra using each candidate vertebra's oriented projection range first and projected centroid distance second; reject bbox-bearing studies with no positive candidate, and remove contiguous-slice representative grouping and Hungarian one-to-one assignment. After level assignment, compress each level's contiguous bbox slices to one actual representative slice only for mandatory classifier-plane anchoring, while persisting every assigned slice separately in `assigned_bbox_slice_numbers`. Keep every non-required fill plane inside the vertebra robust projection range even when a bbox anchor lies outside it; only the exact bbox-forced plane may use the existing empty-mask exception | A contiguous bbox run can cross several vertebrae, so collapsing it before assignment incorrectly sends every row to one level. Conversely, forcing every row to an exact classifier plane can exceed the fixed 15-plane budget, while spreading uniform fill planes across the gap between an outlying bbox and the vertebra creates non-forced empty masks. Row-level assignment preserves ownership, post-assignment anchor compression preserves fixed-count sampling, and robust-range fill isolates unavoidable empty-mask behavior to the annotated plane | One representative point per global contiguous run; force every bbox row as an exact plane; expand the uniform fill range to outlying bbox anchors; unrestricted nearest-vertebra assignment; Hungarian one-to-one assignment | 2026-07-15 |
| Add an explicit study-ID CSV filter to the resumable RSNA dataset runner and use the bbox CSV's `StudyInstanceUID` column for Phase 3 regeneration; require every requested ID to be eligible instead of silently dropping missing inputs | The generic runner's `--limit` selects the first sorted studies and cannot express the exact 235 bbox-bearing subset. Explicit ID filtering makes the production regeneration reproducible and fails early if a requested study lacks DICOM or complete segmentation | Temporary symlink input trees; a one-off custom runner; regenerate all 2,012 studies | 2026-07-15 |
| Make `fracture_bbox_planes.csv` the sole downstream bbox geometry source: assign level from each regenerated metadata file's `assigned_bbox_slice_numbers`, project with the owning level's plane basis and tilted normal, and have the region viewer consume only the resulting CSV. When refreshing a sampled visualization directory, use `--clean-output` to remove previously generated PNGs before writing the new deterministic sample | Recomputing level ownership from overlapping `full_range_mm` or raw scanner-z in a viewer can reintroduce the fixed bugs after preprocessing is corrected. A single projected CSV keeps annotation server, visualizer, and analysis aligned with the exact training-plane geometry. Cleaning the dedicated PNG directory prevents old buggy images that are not selected in the current random sample from being mistaken for current output | Re-run overlap-based level detection in each consumer; use raw DICOM z for plane selection; let each viewer project bbox geometry independently; leave prior sample PNGs mixed with the new sample | 2026-07-15 |
| Route Claude-to-Codex read-only design, debugging, trade-off, and review tasks to `gpt-5.6-sol`; route workspace-writing implementation, fixes, and refactoring to `gpt-terra`; keep project-wide `model_reasoning_effort = "high"` and use `gpt-terra` as the no-flag Codex default | Separating reasoning-oriented consultation from file-writing execution makes the intended model explicit in every active Claude template, while the project default keeps ad hoc implementation calls consistent | Keep `gpt-5.4` for analysis and `gpt-5.3-codex` for implementation; use one model for both modes; retain mixed `high`/`xhigh` effort | 2026-07-10 |
| Make `train_models/stage2/notebook/region_viz.ipynb` a bbox-focused region-head viewer: select only bbox-bearing vertebrae, show only classifier planes intersecting a projected source-DICOM fracture bbox, and render one row per plane with four separate columns for `body/right_foramen/left_foramen/posterior`. Each panel overlays only its own anatomical region, draws the GT bbox in yellow, and reports the per-plane region score plus threshold-based `DETECTED/not detected` status. Keep scores labeled as weakly supervised evidence rather than anatomical fracture probability | Separate panels prevent overlapping region colors from obscuring which branch produced a high score, while bbox-only planes focus inspection on annotated fracture locations. Showing the numeric score and threshold status makes false-positive and false-negative regional evidence directly comparable without implying unavailable region-level ground truth | Combined four-region overlays on all 15 planes; aggregate-only bars; GradCAM without anatomical masks; calibrated anatomical-probability labels | 2026-07-06 |
| For NFS-backed Stage2/RSNA training runs that repeatedly reopen many small `.npy` files, stage the full training dataset onto node-local `/data2` before launch, run the job with outputs/checkpoints/logs on the same local disk, and sync artifacts back to NFS only after clean epoch boundaries or after the run finishes. Add a lightweight watchdog that monitors local heartbeat/progress updates and relaunches the trainer with `--resume` if no progress is observed for a conservative timeout | The current access pattern performs three `np.load()` opens per sample from a hard-mounted shared NFS export. If DataLoader workers block in D-state on NFS, the process cannot be recovered cleanly with signals and can stall the whole machine until reboot. Local staging removes the NFS dependency from the hot path, local outputs avoid checkpoint/log writes hanging at epoch boundaries, and a watchdog plus existing resume support converts future stalls into bounded job restarts instead of manual intervention | Keep dataset on NFS and only reduce worker count; keep checkpoints/logs on NFS; switch to `soft`; rely on manual monitoring and machine reboots | 2026-07-02 |
| For the Stage2 consistency fixes, separate resume state from model-selection state: save a full-state `latest.pt` every completed epoch after `scheduler.step`, keep `best_model.pt` for AUROC-best weights only, validate resume configs with field-level diffs before touching `outputs/.../config.yaml`, prune `training.jsonl` to the resumed `next_epoch`, run validation on rank 0 only, all-reduce train-stat numerators/counts before logging, and structure Stage2 configs as a Stage1-parity base plus explicit ablation overrides via minimal YAML inheritance in `load_config()` | The current Stage2 loop mixes best-checkpoint and resume semantics, logs only rank-local training stats under DDP, duplicates full validation on every rank, and lets the current `test_v2` config differ from Stage1 on many axes at once. A separate latest checkpoint makes resume correct, immutable baseline config makes comparisons attributable, and rank0-only validation plus reduced train stats is the smallest DDP fix for a 2-GPU research setup | Continue resuming from `best_model.pt`; keep append-only logs and dedupe later; shard validation across ranks immediately; adopt a heavier config framework such as Hydra/OmegaConf | 2026-07-02 |
| Keep Stage2 throughput settings unchanged, exclude large generated/data trees from VS Code file watching, search, and Python analysis, and launch long training inside a detached terminal multiplexer | VS Code Server logs showed `SIGPIPE`, missing PTYs, workspace enumeration delays, and an exhausted inotify watch limit without evidence of host OOM. Editor-side exclusion removes unrelated NFS metadata/watch pressure, while terminal detachment prevents editor reconnects from interrupting training and does not reduce GPU or DataLoader throughput | Reduce DataLoader workers/prefetch; disable a GPU; continue launching training directly in the VS Code integrated terminal | 2026-07-01 |
| Redesign Stage2 from first principles as one shared whole-vertebra 2D encoder with two post-encoder paths. The primary path must be exactly Stage1: final `1280`-dimensional per-plane feature -> BiLSTM -> 15 plane logits -> repeated vertebra-label BCE, with mean sigmoid as the primary vertebra score. The auxiliary region path must branch while spatial maps still exist: multi-scale FPN -> mask-normalized pooling for `body/right_foramen/left_foramen/posterior` on every plane -> one shared region temporal module -> per-plane/per-region logits. Because no region labels exist, apply stable Noisy-OR across the four regions **within each plane**, then apply repeated vertebra-label BCE to all valid plane-level `p_any_slice` values. Aggregate planes only after this supervision by mean probability for the auxiliary vertebra score. Train with `L_total = L_stage1 + lambda_any L_region_slice`; never apply the vertebra label independently to each region, never use Noisy-OR across all 15 planes, and never replace the Stage1 loss or score. Treat `p_region` as weakly supervised evidence, not calibrated anatomical probabilities. A conventional vector BiLSTM cannot precede region masked pooling because it has already removed spatial dimensions | Stage2 must remain a controlled extension of Stage1 while adding anatomical multiple-instance evidence. Positive fracture coverage supports dense plane BCE and matches Stage1 supervision. Region aggregation before plane BCE expresses the available label correctly: a positive plane requires at least one positive region, not all four. Applying Noisy-OR over 15 planes would saturate, depend on valid-plane count, and discard the dense-plane assumption; averaging region logits over planes before BCE would again remove direct plane gradients. With only vertebra labels, negatives constrain all four region scores but positives identify only that at least one region is positive, so anatomical attribution is not identifiable or calibratable without external region supervision | Region-level BCE with repeated vertebra labels; one vertebra-level BCE after plane averaging; Noisy-OR across both regions and planes; hard max region aggregation; treating weakly supervised region scores as calibrated probabilities | 2026-07-01 |
| Stabilize Stage2 Region MIL by using BF16 autocast for the CNN/FPN, running both BiLSTM sequence heads and their LayerNorm/linear heads in FP32, fixing the baseline LSE temperature at `1.0`, applying temperature scaling before invalid-region masking, and clipping the global gradient norm at `1.0`. Require BF16-capable CUDA when AMP is enabled; otherwise require explicit FP32 mode. Keep GradScaler recovery only for optional FP16 experiments, use local `/tmp`, and report offending outputs/parameters on persistent failures | The corrected FP16 run progressed for 468 batches and then produced a non-finite forward loss, proving that the remaining failure was FP16 forward-range instability rather than only initial loss-scale overflow. BF16 preserves mixed-precision memory/throughput while using FP32-like exponent range; GradScaler is unnecessary for BF16. Keeping recurrent/head math in FP32 protects the numerically sensitive sequence path, while explicit capability checks avoid silently falling back to the already-failing FP16 path | Disable AMP globally; continue FP16 with a smaller GradScaler value; silently fall back from BF16 to FP16; reduce learning rates without changing numeric format | 2026-06-30 |
| Remove the Stage2 startup-time full-content scan of every `region_4class.npy`: collect samples from exclusions and required-path existence, keep per-sample array validation in `RSNARegionDataset`, and move dataset-wide nonempty validation to preprocessing QC or a reusable manifest | The previous `collect_items()` read about `9.4 GiB` across 13,433 accepted region masks before training. On the project NFS this took `109.3 s`; after switching to required-path checks, the complete collection took `10.9 s`, while the first DataLoader batch took only `4.2 s`. The dataset already passed region QC and `validate_arrays()` rejects empty masks at access time, so repeating the full scan on every run added startup latency without improving steady-state training safety | Keep the full scan on every launch; cache all arrays locally; skip all validation; add more DataLoader workers | 2026-06-30 |
| Implement the anatomy-region MIL classifier as an independent `train_models/stage2/` package, preserve Stage1 split/training/evaluation semantics for controlled comparison, and restrict Stage2-specific changes to region-mask data handling, 56×56 FPN pooling, region/global sequence heads, normalized LSE aggregation, and collapse diagnostics | A separate Stage2 directory prevents baseline regressions and output collisions while keeping the experimental comparison reproducible. Limiting divergence from Stage1 makes performance differences attributable to anatomical region modeling rather than unrelated training-pipeline changes | Add region MIL inside `train_models/stage1/`; use `train_models/region_mil/`; rewrite the full training stack independently | 2026-06-29 |
| For RSNA vertebra-level Region-Masked MIL, do not make four independently zero-masked CNN passes plus probability-space noisy-or the primary design. Encode each full 5-channel plane once, downsample the four anatomical masks to the CNN feature map, perform mask-normalized region pooling with a global/context feature, and use a shared plane-sequence module conditioned by region identity; aggregate region evidence in the logit domain and retain the Stage1/global vertebra head as an auxiliary consistency path. Treat region scores as weakly supervised evidence scores until validated against bbox-derived region labels | Zero masking creates an artificial mask-boundary shortcut, removes clinically useful cross-region context, multiplies compute by four, and makes pretrained BatchNorm statistics especially unreliable. Vertebra-only labels do not identify a unique decomposition into four region probabilities, while noisy-or encourages one-region saturation and increasingly high bag probability as the number of regions grows. Shared full-image features plus explicit region pooling preserve anatomy and make controlled Stage1 ablations possible; auxiliary global supervision and optional bbox-derived labels improve identifiability | Four zero-masked EfficientNet passes with `1-prod(1-p_r)`; separate CNN/BiLSTM per region; Stage1 plus Grad-CAM only | 2026-06-29 |
| During full RSNA SDF region-mask regeneration, exclude only the vertebra level when fewer than two valid anchors are available for any of the four boundary lines; record the level in `excluded_levels.csv` with reason `insufficient_line_anchors` and keep the other levels from the study | Four-line SDF interpolation cannot produce a valid region partition without sufficient anchors for every boundary. Reusing a stale mask or fabricating the missing line would silently mix incompatible outputs, while whole-study exclusion would discard valid levels | Keep the previous mask; synthesize missing lines from another level; exclude the whole study | 2026-06-28 |
| For per-region fracture labels derived from projected bbox annotations and `region_4class` masks, score each slice by **bbox-center-weighted overlap inside the vertebra mask** and normalize by the weighted mass of `bbox ∩ vertebra`, not by the raw bbox area; mark a region slice-positive when its weighted share is at least `0.20` or at least `0.10` with relative support `>= 0.35` of the best-overlap region, then aggregate to the fracture level by requiring persistent support (`>= 2` adjacent positive slices, or one very strong slice with share `>= 0.35`) rather than pure OR or a single representative slice | Absolute pixel counts do not scale across bbox sizes, raw bbox-area fractions are biased when the projected bbox spills outside the vertebra, and pure OR over 30+ slices overlabels from one-slice contacts or mask jitter. Center-weighted overlap preserves boundary-crossing multi-label cases while discounting loose bbox margins, and persistence across z suppresses isolated projection noise without collapsing the lesion to one plane | Absolute pixel threshold only; raw bbox-area fraction only; relative-to-max only; pure OR over all slices; single max-area slice only | 2026-06-29 |
| Add each vertebra level that fails the reference-distribution SDF region QC in a study with no `train_bounding_boxes.csv` annotation to `data/rsna_data/excluded_levels.csv` with reason `sdf_region_qc_outlier_no_bbox`; do not exclude the other vertebra levels from that study, and preserve bbox-positive failures for separate annotation-aware review | Region QC is evaluated per vertebra and a failure does not imply that the remaining C1-C7 levels are unreliable. Level-scoped exclusion removes only the affected region masks while retaining usable anatomy from the same study. Bbox-positive failures may still contain valuable supervised evidence and require separate review | Exclude the entire study after one failed level; retain all QC outliers; exclude bbox-positive and bbox-negative failures equally | 2026-06-28 |
| Evaluate SDF 4-region outputs with a reference-distribution QC built from the accepted multi-case visualizations (`1632`, `16806`, `18010`, `27733`, `30787`): compare each vertebra's 15-plane area-ratio trend, region-presence pattern, mask-normalized centroid positions, and region compactness to its nearest reference case, with vertebra-specific thresholds derived from reference-to-reference distances | Anatomically valid superior/inferior planes can naturally lose one or more regions, especially for C2, so requiring all four labels or fixed minimum area ratios on every plane creates false failures. The accepted examples define the intended cross-z tendency, spatial distribution, and shape while still allowing genuine oblique/orientation and label-layout failures to rank as outliers | Require all four regions per plane; fixed per-region minimum ratios; visual inspection without a reproducible score | 2026-06-28 |
| Standardize line-to-region-mask restoration in the RSNA z-propagation path on the existing `rsna_line_vis` method: retain each heatmap moment centroid alongside `(phi, rho)`, interpolate the centroid trajectory in z, project it onto the interpolated SDF line, reconstruct each `line_1`-`line_4` with its own training-set average length, and pass those segments to `generate_region_mask` | `generate_region_mask` infers junctions from segment endpoints, so reconstructing every SDF line as a 350 px segment centred at the image-centre footpoint changes junction geometry even when the infinite line is correct. Preserving centroid and average line length makes anchor-plane restoration match the accepted visualization method and keeps z-interpolated masks compatible with the same endpoint semantics | Keep 350 px SDF segments; reconstruct around the closest point to image centre; use `(phi, rho)` alone and accept different junctions | 2026-06-28 |
| Add the Stage1 RSNA patient-level auxiliary head as a config-gated multitask option: `model.use_patient_head` controls the extra head, `patient_label` comes from `patient_overall` or max C1-C7 labels, and the training objective uses fixed `slice_loss_weight` / `patient_loss_weight` while preserving vertebra-level metrics for model selection | This matches the RSNA Type2 reference pattern without changing the default baseline. Config gating keeps existing checkpoints and experiments comparable, while fixed weights make the auxiliary supervision explicit and easy to ablate | Always enable the head; replace vertebra-level objective with patient-level training; use adaptive multitask weighting before a fixed-weight baseline | 2026-06-24 |
| Treat RSNA 4-region line QC as a two-stage check: first validate classifier-plane input quality from metadata and CT-mask overlay (`orientation.at_search_boundary`, large corrected tilt, exceptional geometry such as study `24673`, and CT-mask contrast/alignment), then evaluate generated region areas using the same short-line reconstruction as `rsna_line_vis` (`centroid + angle_deg + average training line length`) | Region-size outliers can be caused by upstream plane/orientation/mask issues rather than line geometry alone. The saved `rsna_line_vis` panels are not generated from raw JSON `endpoints`; using endpoints directly creates false QC failures. Input-plane flags explain cases that look non-axial or visually unusual before attributing failure to region splitting | Use JSON `endpoints` directly for QC; judge region masks without checking plane orientation; assume every classifier plane is native axial | 2026-06-26 |
| Flag study `1.2.826.0.1.3680043.24673` for explicit CT/mask orientation review before using its RSNA line-region outputs; at least `C7` max-area plane shows a visually vertical-flipped binary vertebra mask relative to the CT, while other levels are mixed/ambiguous | This study already uses exceptional repaired geometry from a continuously changing DICOM slice orientation series. Region-size failures on this study can be upstream CT/mask sampling inconsistency rather than line prediction failure, so it should be quarantined or corrected before region QC/training use | Treat `24673` as ordinary data; fix region labels downstream; rely on area statistics alone | 2026-06-26 |
| Keep the Stage1 optimization semantics and batch size unchanged while accelerating input delivery: return `(15,6,224,224)` samples as `uint8`, normalize after nonblocking device transfer, use a vectorized no-transform validation path, keep DataLoader workers persistent with configurable prefetch, and limit OpenCV/PyTorch worker-local threads to one | The prior Dataset converted every sample to float32 before multiprocessing transfer, increasing worker IPC and pinned-memory traffic by 4x, while validation still paid per-slice `A.Compose([])` overhead. A local benchmark reduced the validation sample path from about `26.3 ms/item` to `1.7 ms/item`; an eight-worker two-epoch pilot reached `81.8 train items/s` and `372.7 validation items/s` after worker warmup. These changes preserve pixel values, augmentation probabilities, optimizer steps, and the reference batch size | Increase batch size and retune optimization; cache the full 60 GiB dataset in local storage; change augmentations; disable multiprocessing | 2026-06-22 |
| For the Stage1 RSNA baseline, treat the fold2 epoch-11 NaN as a forward-path mixed-precision stability issue first, not a loss-formula bug. Make AMP dtype explicit and prefer BF16 when CUDA supports it; port Stage2's non-finite training-loss / unscaled-gradient diagnostics and bounded AMP-skip accounting; and add validation-side finite guards plus optional parameter/buffer scans so eval crashes report offending batch items and layers before sklearn metrics run | Stage1 currently uses CUDA autocast without an explicit dtype or any train/val finite checks, while the positive-weight-normalized BCE loss is numerically simple once logits are finite. The observed failure surfaced in validation after a clean training epoch, which is more consistent with uncaught FP16 forward non-finites in logits/activations or state/buffer contamination than with GradScaler-missed backward overflow alone. Matching Stage2's BF16 policy reduces forward-range risk, while train+val finite guards make the next failure attributable instead of surfacing later as `roc_auc_score(...): Input contains NaN` | Keep implicit FP16 autocast and add only gradient clipping; blame the positive-weight BCE criterion; add BatchNorm-only checks without generic output/loss guards | 2026-07-04 |
| Force Stage1 Python multiprocessing temporary files into a per-user directory under local `/tmp` before creating DataLoader workers | The shell environment points `TMPDIR` at the NFS-backed project `.tmp`, where DataLoader worker finalizers repeatedly fail to remove `pymp-*` directories with `EBUSY`. Local scratch avoids NFS deletion semantics and removes noisy worker-shutdown tracebacks without changing data loading | Set `num_workers=0`; ignore finalizer tracebacks; manually delete NFS `pymp-*` directories after every epoch | 2026-06-21 |
| Show per-batch `tqdm` progress and running loss for Stage1 training and validation, and print train/validation batch counts before each fold | A full fold has roughly 1,126 training batches and 282 validation batches at batch size 8, but the previous implementation emitted no output until the entire epoch finished, making normal long-running work appear hung. Progress reporting diagnoses data-loading or compute stalls without changing optimization behavior | Log only once per epoch; print every fixed number of batches manually; reduce the dataset merely to obtain faster feedback | 2026-06-21 |
| Use the current `torch.amp` API for Stage1 mixed-precision training and enable CUDA autocast/scaling only when the selected device is CUDA | PyTorch 2.10 deprecates `torch.cuda.amp`; using device-explicit `torch.amp.autocast` and `torch.amp.GradScaler` removes warnings and avoids accidentally enabling CUDA-specific AMP on CPU fallback runs | Keep deprecated `torch.cuda.amp`; suppress warnings; enable CPU autocast implicitly | 2026-06-21 |
| Keep Stage1 augmentation compatible with the locked Albumentations 2.x environment by replacing removed/deprecated APIs with behavior-preserving equivalents: `RandomBrightnessContrast(contrast_limit=0)`, `Affine`, normalized `GaussNoise.std_range`, and range-based `CoarseDropout` arguments | Albumentations 2.0.8 removes `RandomBrightness` and silently ignores several Albumentations 1.x arguments, which either prevents training startup or changes augmentation strength without an error. Updating call sites preserves the intended RSNA-style augmentation while avoiding dependency downgrades | Pin Albumentations 1.x; patch only `RandomBrightness`; tolerate deprecation and invalid-argument warnings | 2026-06-21 |
| For bbox-positive exception studies, preserve the actual DICOM slice-specific orientation when converting bbox pixels to patient coordinates. When the number of contiguous bbox intervals equals the number of positive C1-C7 labels in `train.csv`, assign intervals one-to-one to those positive levels using minimum-cost physical proximity rather than unrestricted independent nearest-level assignment. Permit an empty sampled vertebra mask only on an exact bbox-forced center plane and record that exception in QC. For nonparallel gantry-tilt series rejected by native geometry validation, reconstruct a regularized sampling geometry from the already repaired TotalSegmentator NIfTI affine while retaining original DICOM geometry for bbox pixel conversion | Study `17382` has four bbox intervals corresponding to C4-C7, with one C7 bbox plane lying just outside the segmentation mask. Study `24673` has large slice-orientation variation and two bbox intervals corresponding to C4/C7. Independent nearest-level assignment misplaced intervals, while rejecting empty bbox planes or nonparallel geometry discarded the two annotation-rich studies. The constrained exception path preserves all bbox evidence and makes approximations explicit through `geometry_mode` and empty-mask QC | Drop the studies; assign every interval independently to the nearest vertebra; shift bbox planes into the mask; use repaired affine for bbox coordinates; globally relax DICOM orientation validation | 2026-06-21 |
| Run full RSNA classifier-plane generation with a resumable eight-process CPU pool, two native-library threads per worker, per-study atomic output, current-schema completeness checks, failure isolation, and JSONL progress/ETA logging. Do not generate preview PNGs during the full run | An eight-study pilot completed in `145.5 s` with zero failures, implying about `10.2 h` for 2,016 studies. The host has 32 CPUs and 251 GiB RAM, while each serial study peaks around 1 GiB RSS; eight workers stay comfortably within CPU and memory limits without applying excessive concurrent NFS pressure. Schema and array-shape checks permit safe restart after interruption | Serial processing (~55 h); four workers (~16 h); twelve workers without a longer NFS-load pilot; generate all preview PNGs during preprocessing | 2026-06-20 |
| Store fracture-classifier CT input as true 2.5D stacks with shape `(15, 5, 224, 224)`: for each selected center plane, sample channels at corrected-normal offsets `[-2,-1,0,+1,+2] * median DICOM slice spacing`, while retaining the center-plane vertebra mask as `(15,224,224)`. Also record the physical position of the winning maximum-area plane from orientation search and force that exact position into the fixed 15 center planes alongside any bbox-required positions. Mark both `max_area_forced` and `bbox_forced` provenance in metadata and bump the incompatible output schema to `rsna-preprocessing-v2` | The intended RSNA-style classifier uses five adjacent slices per center rather than one isolated plane. Expressing adjacency in millimeters preserves native inter-slice context while remaining compatible with oblique physical-plane sampling. The orientation objective's winning cross-section is the clearest central anatomical view and must not be lost through uniform rounding; exact insertion guarantees its availability without abandoning full-range coverage | Keep single-channel `(15,224,224)` CT; use fixed `1 mm` channel offsets for every study; assume the nearest uniform center is sufficient; replace a bbox-required plane when the maximum-area plane is added | 2026-06-20 |
| Sample each vertebra's 15 classifier planes directly from the ordered native DICOM HU volume with linear interpolation and from the cleaned NIfTI mask with nearest-neighbor interpolation, using the established self-site bone window (`WL=400`, `WW=2000`, clip `-600..1400 HU`) and atomic study-directory replacement. Treat any sampled mask contact with the `224x224`, `0.4 mm/pixel` FOV boundary as a measured QC signal rather than an immediate hard failure; persist per-plane boundary-pixel counts so pilot data can distinguish minor process-tip contact from material clipping | Direct physical sampling avoids persistent isotropic CT volumes and preserves consistency with the existing line-model intensity preprocessing. A real bbox-positive pilot showed only C7 contact, limited to at most 27 boundary pixels on five planes and visually localized to the spinous-process tip; rejecting any single boundary pixel would discard otherwise valid studies. Raw counts retain enough evidence to set a later material-clipping threshold from the multi-study pilot | Resample and persist a full isotropic CT; use a different RSNA window; fail on any boundary contact; ignore FOV contact entirely | 2026-06-20 |
| Build each vertebra's 15 classifier planes from the cleaned mask projected onto its corrected normal: use the `1%` and `99%` voxel-volume quantiles as robust endpoints, force one physical plane through the center of each assigned contiguous bbox interval, fill remaining slots for range coverage, and serialize the final sequence in patient-superior to patient-inferior order. Resolve RSNA `slice_number` by numeric DICOM filename first and `InstanceNumber` second; assign every bbox interval to exactly one nearest vertebra using projected full-mask range followed by centroid distance | This preserves full-vertebra context while guaranteeing that every annotated fracture interval contributes an actual source-slice location. Exact study-level assignment prevents one bbox interval from being silently dropped or duplicated across adjacent levels. Patient-z ordering makes sequence index semantics stable even when the corrected normal direction differs by acquisition orientation | Purely uniform planes; nearest uniform plane instead of exact bbox position; assign bbox to every overlapping vertebra; order planes by raw DICOM index or corrected-normal sign | 2026-06-20 |
| Implement physical orientation correction by resampling only the cleaned vertebra-mask ROI to a transient `0.4 mm` isotropic grid aligned with the DICOM row/column/normal basis, then reuse the established maximum-axial-area coarse-to-fine rotation search (`±30°/5°`, local `1°`). Never resample or persist the full CT for angle search; convert the winning local rotation back to corrected LPS row/column/normal basis vectors and record boundary QC | Directly applying the old voxel-index rotation to anisotropic TotalSegmentator masks is geometrically invalid and extremely slow on full `512x512xZ` volumes. Point-projection approximations introduced axis and small-angle biases. A small in-memory isotropic mask ROI preserves the tested objective and angle convention, removes spacing bias, and produced plausible non-boundary C1-C7 corrections on a real study | Rotate the full anisotropic mask; estimate orientation from PCA or centroid trajectories; use projected voxel-center histograms; resample and persist the full CT or mask volume | 2026-06-20 |
| Use `process_study.py` as the first study-level RSNA orchestration boundary: require an existing complete C1-C7 segmentation set, load DICOM geometry once, process all seven masks in memory, and atomically replace one study metadata JSON only after every geometry and alignment check succeeds. Persist DICOM source/SOP mapping, physical geometry, per-vertebra ROI/component metrics, and QC, while returning cleaned masks in memory for the next orientation stage | This creates a resumable checkpoint without prematurely persisting cropped or rotated volumes. Requiring all seven masks prevents partial studies from appearing complete, loading geometry once avoids repeated header I/O, and same-directory temporary-file replacement preserves the previous valid metadata if any vertebra fails. Returning cleaned masks avoids a redundant read/clean cycle when orientation search is integrated | Write metadata incrementally per vertebra; overwrite the final JSON before validation; persist cleaned 3D masks as another intermediate; reload DICOM geometry independently for each vertebra | 2026-06-20 |
| Process each TotalSegmentator vertebra mask by binarizing it, retaining the largest 26-connected 3D component, and computing voxel count, physical volume, centroid, axis-aligned physical bbox, and superior-inferior extent in DICOM LPS millimeters. Treat the NIfTI affine as RAS and convert explicitly at the module boundary; validate alignment against the DICOM geometry using deterministic samples plus occupied-axis extrema, while cropping to the nonzero voxel bbox before connected-component labeling | The downstream orientation and plane-selection stages require reproducible physical ROI measurements rather than NIfTI voxel indices. Explicit RAS-to-LPS conversion prevents silent sign errors, 26-connectivity preserves diagonally connected vertebral structures, and nonzero-bbox labeling reduces a real C1-C7 pilot from about 2.5 seconds to 0.6 seconds per mask. Sampled alignment QC bounds runtime while still detecting gross affine or patient-coordinate mismatches | Keep every component; use six-connectivity; compute ROI in NIfTI RAS; transform every mask voxel through DICOM geometry for QC; label the full 512x512xZ volume directly | 2026-06-20 |
| For bbox-annotated RSNA training studies, force every contiguous bbox slice interval to contribute at least one actual annotated slice to the fixed 15 classifier planes. Convert the representative bbox slice center to patient coordinates, project it onto the corrected superior-inferior axis, preserve that physical position exactly, and fill the remaining slots by maximizing spatial coverage over the vertebra range; expand the robust `1%-99%` range when a required bbox position lies outside it | A fixed 15-plane sequence cannot contain every bbox row because the dataset has up to 167 bbox slices per study, but contiguous bbox rows describe lesion intervals and there are at most seven intervals per study. Requiring one real annotated slice per interval guarantees that each annotated fracture focus is represented while retaining enough slots for full-vertebra context. The forced positions must be recorded in metadata because this is annotation-informed sampling and differs from inference-time sampling without bbox labels | Keep purely uniform 15-plane sampling; include every bbox row and make sequence length variable; select the nearest uniform plane without preserving an actual bbox slice | 2026-06-20 |
| Decouple fracture-classifier planes from line-detection anchor planes. Keep the 15 classifier planes fixed over the full vertebra, but transiently sample a denser physical grid along the corrected superior-inferior axis, run the line model only on anatomically clear candidates, select spatially distributed high-quality anchors, and propagate the four-line geometry to the 15 classifier planes with the existing junction/orientation z-trajectory method | The uniformly distributed 15 classifier planes must include superior/inferior anatomy where the four internal boundaries can be weak, while the line model was trained on clearer central slices and is known to fail locally on ambiguous edge slices. Separate anchor selection preserves full fracture coverage without forcing unreliable direct line predictions on every classifier plane. Distributed anchors prevent all selected slices from collapsing around the single maximum-area plane | Run the line model directly on all 15 planes; replace classifier planes with only central clear slices; select global top-k line predictions without positional coverage; save a fully resampled intermediate volume | 2026-06-18 |
| Score line-anchor candidates without GT using mask cross-sectional area/solidity, per-line heatmap validity (peak, connected-component support, moment anisotropy), generated-region topology, and cross-slice smoothness; initially form five physical-position bins over the usable mask range and select the best valid candidate per bin, requiring at least three anchors and recording propagated/extrapolated confidence for each final plane | Evaluation-only angle error cannot be used on unseen RSNA studies. Combining image-independent mask quality, model confidence, anatomical topology, and neighboring-slice consistency targets the observed failure modes. Position bins preserve z coverage, while a minimum-anchor rule makes failure explicit rather than silently generating unstable region masks | Use heatmap peak alone; use mask area alone; use evaluation angle error at inference; always accept the highest-scoring slice even when topology is invalid | 2026-06-18 |
| Select the 15 classifier slices by uniform physical-position sampling over each orientation-corrected vertebra mask: project the cleaned mask onto the corrected superior-inferior axis, define robust endpoints from the `1%` and `99%` cumulative mask-volume positions, and sample 15 planes with `linspace(t_low, t_high, 15)` in millimeters; store every plane position and transform in metadata | This follows the RSNA first-place design of 15 uniformly distributed slices while remaining independent of native DICOM slice spacing. Robust cumulative-volume endpoints suppress unstable one-voxel mask tips without restricting sampling to the central peak, so the sequence still covers the full vertebra and does not systematically discard superior/inferior fractures. Physical plane positions also make bbox projection and preprocessing reproducible | Select 15 native DICOM indices; use the old peak `±5` slice interval; sample raw mask `min/max` including isolated edge voxels; select the 15 largest-area slices | 2026-06-18 |
| Reuse the existing vertebra orientation-correction algorithm for the RSNA pipeline: estimate `(rx, ry)` by maximizing the largest axial vertebra-mask cross-sectional area with the established coarse-to-fine search (`±30°/5°`, then local `1°` refinement), and generate slices by direct oblique-plane sampling instead of saving a rotated 3D volume; preserve the same objective and search behavior while evaluating transforms in physical patient coordinates for anisotropic DICOM spacing | This algorithm has already been used for the existing annotation dataset and produces inputs aligned with the line model's training convention. Direct plane sampling performs only one interpolation and avoids the additional blur and storage cost of rotating and persisting a full volume. Physical-coordinate evaluation is required because the original implementation searched voxel coordinates after a `0.4 mm` isotropic crop, whereas the redesigned pipeline intentionally preserves native anisotropic spacing | Introduce PCA-based vertebra-axis alignment; rotate and save each cropped 3D volume; apply the old voxel-index transform unchanged to anisotropic DICOM data | 2026-06-18 |
| For cervical-vertebra 4-line CT detection on 42 patients / 1,610 slices, do **not** escalate first to larger 2D backbones (HRNet, Attention U-Net, Transformer, wider/deeper UNet, direct coordinate regression, or DSNT variants); treat the current plateau as primarily a **robustness / context-selection** problem and prioritize post-processing hardening, slice-level confidence selection/aggregation, and only then a small-footprint inter-slice context upgrade (2.5D) if edge-slice collapse remains | The strongest current evidence argues against a capacity bottleneck: heatmaps already converge in the correct direction, the largest historical errors were created by moment extraction rather than prediction quality, outliers are local to ambiguous edge slices while neighboring slices in the same patient/vertebra remain correct, and slice selection alone nearly removes the tail (`outlier 10.9% -> 1.1%`). In this regime, larger 2D architectures are more likely to add variance than solve the actual failure mode. The only architecture change with strong problem-mechanism alignment is limited inter-slice context, because the residual collapse is slice-local and concentrated at anatomical edges | Immediate switch to HRNet / Attention U-Net / Transformer; wider/deeper 2D UNet first; direct `(phi, rho)` regression; DSNT-first replacement; 3D CNN as the next default step | 2026-06-18 |
| Redesign RSNA cervical-fracture preprocessing as a streaming per-study pipeline: do not persist full CT NIfTI volumes; run TotalSegmentator from DICOM directly when supported, otherwise create a per-study temporary NIfTI under local scratch and delete it after C1-C7 masks/metadata are produced; manage ROI extents in physical patient coordinates without whole-volume isotropic resampling, then generate final fixed `224x224` per-slice images from the physical crop transform | Persisting all CT NIfTI volumes creates a large avoidable intermediate and duplicates the original DICOM source. The downstream classifier and line model only need per-vertebra fixed-size 2D slices plus reproducible transform metadata. Physical-coordinate crop definitions preserve the original anisotropic acquisition geometry and avoid z-interpolation blur, while still allowing deterministic `224x224` rasterization for model input | Persist all CT NIfTI volumes before segmentation; resample each CT to isotropic 3D first; save per-vertebra cropped NIfTI volumes as the main intermediate | 2026-06-18 |
| Build RSNA DICOM geometry from header-only reads in one process, preserve irregular slice positions through piecewise-linear physical-position/index conversion, and sample output planes in vectorized chunks (default 8 planes) rather than adding nested study-level or per-slice parallelism | Header reads avoid loading pixel data during QC, while explicit interpolation over actual `ImagePositionPatient` values prevents regular-spacing assumptions from corrupting coordinates. Chunked `map_coordinates` limits temporary coordinate arrays to roughly 10 MB at `8 x 224 x 224` while retaining vectorized throughput. A real 220-slice study loaded in about 5.2 seconds from the current NFS, which is small relative to segmentation runtime and does not justify extra I/O pressure yet | Read full pixel data during geometry validation; assume median z spacing for every slice; sample one pixel or one plane per Python call; parallelize every DICOM header read and every plane across all CPU cores | 2026-06-18 |
| Run RSNA TotalSegmentator with one worker per GPU by default, a global eight-thread CPU budget, at most two mask-saving threads per worker, and local scratch under `/tmp/rsna_totalsegmentator`; require all seven non-empty C1-C7 masks before atomically replacing the study output, and retry once through a temporary dcm2niix NIfTI only when direct DICOM input fails | This avoids GPU-memory competition and CPU/OpenMP oversubscription while still allowing explicit higher concurrency after profiling. Local scratch prevents temporary CT and multiprocessing result traffic from loading NFS. Atomic validation prevents interrupted or partial outputs from being mistaken for completed studies. The fallback preserves progress on converter-specific failures without storing CT NIfTI permanently | Default to three workers per GPU and all available CPU threads; treat one mask as completion; write directly into the final study directory; preconvert every study to persistent NIfTI | 2026-06-18 |
| For the current RSNA dataset, default TotalSegmentator input to a per-study temporary dcm2niix NIfTI instead of attempting the library's DICOM path first; retain `--input-mode auto` and `--input-mode dicom` only for diagnostics or future datasets | Two real RSNA studies failed in TotalSegmentator's internal DICOM conversion because the source files do not provide the `Modality` attribute expected by its converter, while controlled dcm2niix conversion succeeded in about 1.5 seconds and both GPU segmentations completed. Skipping the known-failing direct path reduces latency and log noise without changing storage policy because the temporary NIfTI is deleted immediately | Keep DICOM-first retry for every study; permanently store all converted NIfTI; patch original DICOM files with synthetic tags | 2026-06-18 |
| When dcm2niix splits one RSNA series into multiple NIfTI candidates because slice orientation changes within the series, retry once with forced slice merging (`-m y`); if the merged NIfTI has a zero slice-axis affine, estimate an average RAS slice vector from the two physical-position endpoints before TotalSegmentator inference, and retain the case as exceptional geometry for downstream QC | Study `1.2.826.0.1.3680043.24673` contains 218 slices with continuously changing orientation, causing 150 candidate files and a zero third affine column after forced merge. Endpoint-based affine repair produced a valid `512x512x218` volume and all seven non-empty C1-C7 masks. The approximation is sufficient for segmentation but must not replace per-slice DICOM geometry for later bbox mapping | Drop the bbox-positive study; choose one split candidate; assume a regular axial series; rewrite source DICOM geometry | 2026-06-19 |
| Replace single-pass raw second moments for `Unet/line_only` with a two-pass robust estimator: first suppress low-confidence tails via intensity tempering (`h^γ`, default `γ≈2`), then refine moments with a **soft perpendicular-distance band** around a detached coarse line estimate; do **not** use centroid-only radial clipping as the primary fix | The observed failure mode is bounded-influence mismatch: second moments amplify faint off-axis tails by `distance^2`, so a slightly wider lateral smear can flip the dominant eigen-axis while keeping anisotropy falsely high. A centroid-radius clip is geometrically mismatched to finite lines: preserving 30-50 px lines with `σ≈4` requires a large radius (`k≈5-7`), which removes little of the harmful off-axis mass, while smaller radii shorten valid lines and bias confidence/ρ. A soft line-distance band directly targets lateral contamination, preserves along-line extent, remains differentiable for the training loss path, and is safer when the coarse geometry used to build the window is detached before the refinement pass | Hard centroid-radius clipping only; hard thresholding only; connected-component filtering only; full IRLS/M-estimator fitting from the start | 2026-06-16 |
| Use adaptive peak-relative thresholding for `Unet/line_only` evaluation-time moment extraction (`max(min, peak_ratio * peak)`, default `min=0.15`, `peak_ratio=0.4`) and pass the same threshold spec to visualization line extraction | Fixed absolute thresholds are calibration-sensitive: high cutoffs erase low-peak but valid lines, while low cutoffs admit scattered noise. Adaptive thresholding is a minimal first step that preserves weak line predictions without immediately adding heavier connected-component or RANSAC fitting, and keeping metrics/visualization on the same threshold avoids diagnostic drift | Keep fixed `0.5`; lower fixed threshold globally; jump directly to connected-component filtering plus robust line fitting | 2026-06-16 |
| Refactor `Unet/line_only` by keeping `src/trainer.py` as the stable public facade and fold-training orchestrator while extracting validation (`evaluation.py`), test inference/report generation (`inference.py`), example image writing (`example_writer.py`), and experiment/wandb concerns (`experiment.py`) | The former 784-line trainer mixed five independent responsibilities and made scientific metric changes difficult to review. Re-exporting the existing public functions from `trainer.py` preserves ad-hoc script compatibility while reducing the orchestrator to 351 lines and allowing each evaluation path to be tested independently | Rename or remove the public trainer functions; introduce a generic class-based training framework; keep the monolithic trainer | 2026-06-15 |
| Refactor the repository incrementally by subsystem, starting with shared training infrastructure in `learning/`, while preserving experiment-specific loss and evaluation behavior | A repository-wide rewrite would mix structural changes with scientific behavior changes and make regressions difficult to attribute. The MIL and bag-only trainers already duplicated device selection, model construction, data-loader setup, optimizer setup, warmup, and ranking metrics, so extracting only those stable mechanisms provides immediate simplification without changing experimental semantics | Rewrite all trainer variants at once; create a highly generic trainer framework; defer refactoring until experiments finish | 2026-06-15 |
| Use patient-grouped 5-fold CV over all 284 bags and report both per-fold validation metrics and pooled OOF metrics, without reserving a fixed test cohort | With only 41 patients, a fixed holdout materially reduces already-limited training data. OOF predictions still evaluate every bag exactly once with a model that did not train on that patient, while per-fold files preserve fold-level variability | Fixed patient-level test holdout; nested CV | 2026-06-11 |
| When fracture masks cover most slices within positive vertebrae (mean 73.9%, median 82.8%), stop treating slice labels as evaluation-only and prefer a slice-supervised 2.5D classifier with vertebra score from mean pooling plus optional bag-consistency loss over hard top-k DMIL | Dense positive-slice coverage breaks the core sparse-instance assumption behind hard MIL. Top-k DMIL now discards most true-positive evidence and makes optimization noisier, while slice-supervised mean pooling uses nearly all available labels, matches the observed prevalence pattern, and keeps capacity lower than sequence/3D alternatives for 41 patients | Keep hard top-k DMIL; vertebra-level mean pooling without slice supervision; attention pooling without slice supervision; 3D CNN or RNN sequence model | 2026-06-11 |
| For dense positive-slice vertebrae, rank non-MIL follow-ups as: (1) slice-supervised per-slice classifier with vertebra score from mean pooling, (2) attention pooling as a secondary ablation, (3) plain vertebra-level mean pooling without slice supervision; use max pooling only as an analysis-time aggregator, not the default training target | The mask coverage statistic implies that most slices in positive bags carry usable signal, so direct slice supervision should be the primary source of gradients. Attention pooling can still adapt to slice importance but adds parameters and variance on 41 patients. Plain mean pooling is simpler but throws away reliable slice labels. Max pooling is too sensitive to single-slice noise when positives are broad rather than focal | Stay with hard top-k DMIL; switch directly to a sequence model; use max pooling as the main vertebra score | 2026-06-11 |
| Explicitly condition fracture classification on vertebra level by concatenating a fixed C1-C7 one-hot vector with the ResNet18 pooled image feature before the final classifier | Fracture prevalence and morphology differ by level, while the previous pipeline carried the level string only for reporting and discarded it during training. One-hot conditioning is low-capacity, transparent, and safer than a learned high-dimensional embedding for 41 patients | Image-only model; separate classifier per vertebra; learned vertebra embedding; spatial FiLM conditioning | 2026-06-11 |
| Report vertebra-level classification metrics only for individual C1-C7 levels and remove the redundant `C3-7` pooled row | The pooled row duplicates C3-C7 observations, obscures direct per-level interpretation, and is not needed for the current analysis | Keep both individual and pooled rows; report only broad C1/C2/C3-7 groups | 2026-06-11 |
| Switch the 2.5D MIL backbone from ImageNet-pretrained ResNet34 to ResNet18 while keeping frozen BatchNorm, the same DMIL head, and identical fold definitions | On fold 0, the regularized ResNet34 run still underperformed the original run (`best AUPRC 0.4599` vs `0.5452`) and showed renewed validation-loss divergence. With only 41 patients, reducing backbone capacity is the next controlled ablation and preserves direct attribution | Continue tuning ResNet34; freeze the full ResNet34 backbone; move immediately to attention MIL or a sequence model | 2026-06-11 |
| For bag-wise 2.5D MIL fine-tuning, freeze pretrained BatchNorm statistics/affine parameters, remask normalized backgrounds to zero, and use stronger regularization defaults (`dropout=0.5`, lower backbone LR, higher weight decay, effective noise/blur augmentation) | A forward batch contains slices from only one patient/vertebra, so train-mode BatchNorm estimates patient-specific statistics while validation uses global running statistics. The 41-patient dataset also showed rapid memorization (`train loss 0.79 -> 0.04` while validation loss rose), and normalization converted masked background zeros into large constant negative values | Keep BatchNorm trainable; replace BatchNorm with GroupNorm; freeze the full backbone; change to ResNet18 immediately | 2026-06-11 |
| For the first per-vertebra cervical fracture baseline on the tiny 51-patient dataset, prefer a simple end-to-end 2.5D slice-MIL classifier (ImageNet-pretrained CNN + slice logits + fixed top-k mean pooling) over a RSNA-style two-stage frozen-feature CNN+RNN stack | The dataset is too small for a high-capacity sequence model to be a reliable first baseline. Fixed top-k pooling imposes the right inductive bias for focal fractures, keeps the parameter count low, works with imperfect slice labels as light auxiliary supervision, and yields directly inspectable slice scores | Two-stage CNN+LSTM/GRU; attention-MIL pooling as the initial baseline; Transformer encoder over slice features | 2026-06-10 |
| For the weakly supervised per-vertebra fracture baseline, implement DMIL on selected slice logits (`top-k` chosen by logit rank, optimized with `BCEWithLogits`), keep the center term as a **negative-bag variance penalty on sigmoid scores** (`mean((s - stopgrad(mean(s)))^2)`), replace uncapped `alpha=2` with a **capped small-k rule** (`k_i = clip(round(0.10 * t_i), 3, 8)`), use identical stack-wise spatial/intensity augmentation for the 3-slice input plus mask, and prefer **constant LR + short warmup + differential LR** over cosine/one-cycle/plateau-by-AUPRC as the default scheduler | This setup preserves MIL ranking correctness and numerical stability, avoids the strongest pseudo-label dilution failure mode of `top-50%` positives, regularizes repeated positive sampling through online augmentation without inducing class-conditional artifact shift, and is more robust than reactive or full-budget schedulers under tiny-data, short-run, early-stopped training | Raw BCE on probabilities; global running center as the main center loss; uncapped `k_i = ceil(t_i / 2)`; positive-only stronger augmentation; cosine decay; one-cycle; ReduceLROnPlateau driven by noisy fold-level AUPRC | 2026-06-11 |
| Adopt flat package layout with explicit package list in hatchling | Aligns with requirement to remove src/ while keeping deterministic builds | Dynamic discovery; keep src layout | 2026-01-31 |
| Keep distribution name configurable via `[project].name` while package import name remains stable | Enables identity switch without code moves | Rename package directory per release | 2026-01-31 |
| Implement rate-limit fallback at a single provider wrapper layer | Reduces duplication and keeps switching logic consistent | Hook-based or per-agent fallback | 2026-01-31 |
| Prefer wrapper-based detection over Claude hook reliance for rate-limit fallback | Hooks may not fire on API errors and only run inside Claude lifecycle | Hook-based detection; log scraping | 2026-01-31 |
| Use semi-automatic fallback (prompt user to switch) by default | Prevents unexpected provider switches and preserves user intent | Fully automatic switching | 2026-01-31 |
| Persist canonical conversation state for cross-provider reuse | Enables Gemini to continue with full context | Ad-hoc copy/paste | 2026-01-31 |
| Align angle evaluation by using the same estimator on GT and predictions; default to heatmap-moment angle for both and keep polyline PCA as an oracle metric | Avoids systematic bias from representation mismatch and separates estimator error from model error | Compare raw polyline PCA vs predicted heatmap moments | 2026-02-18 |
| Fix GT angle extraction: replace endpoint-based method with PCA over all polyline points | V-shaped polylines (57.3% of data) caused endpoint method to give random angles (46.4% had >10° error); PCA uses all points and gives geometrically correct principal axis | Endpoint-based (first/last point only) | 2026-03-25 |
| Fix `detect_line_moments`: apply threshold=0.2 before computing moments | Sigmoid background noise dominates raw moments (mu20/mu02 ~1000-2000 without threshold vs ~10-200 with threshold), causing completely wrong angle extraction; thresholding reduces mean error from 28.26° to 4.64° | No threshold; higher/lower threshold | 2026-03-25 |
| Refactor training/evaluation into smaller, single-purpose functions (data split/load, model/opt setup, train loop, eval metrics, visualization) | Improve readability, testability, and reduce regression risk during future changes | Keep monolithic functions; split only by file | 2026-03-04 |
| Minimal line-eval reporting: angle_error_deg + rho_error_px as core, keep perpendicular_dist_px for interpretability, and one heatmap peak metric (e.g., peak_dist) for debugging | Keeps task metrics primary while preserving a light diagnostic signal for heatmap quality | Drop all heatmap metrics; report only angle/rho | 2026-03-16 |
| Set Gaussian heatmap sigma based on output resolution and annotation uncertainty; start with sigma ≈ 2–3 px on the output map (scale by stride), and ensure L^2/12 ≫ sigma^2 for stable moment-based angle; consider anisotropic sigma (smaller perpendicular) if angle is unstable | Balances training stability vs geometric precision for moment-based line estimation | Fixed sigma without scaling; isotropic-only kernels | 2026-03-16 |
| Treat anisotropic line-GT kernels as an optimization-shaping option, not a new-information source; prioritize prediction-quality and estimator-robustness fixes before replacing distance-transform-based line heatmaps | Distance-to-line Gaussian targets already encode orientation through elongated support and second moments. Remaining angle error is more likely driven by prediction blur/noise, threshold/gating sensitivity, boundary truncation, and mismatch between pixel MSE and geometric evaluation metrics | Immediate anisotropic-GT replacement as primary intervention | 2026-03-31 |
| Default to no hard thresholding for moment-based line estimation; if background noise dominates, prefer soft weighting or percentile-based gating to suppress low-confidence regions while preserving Gaussian tails | Hard thresholding can bias centroid/covariance and increase angle jitter when signal is blurred or low-SNR | Always-threshold; binary mask moments | 2026-03-16 |
| For sigmoid heatmaps trained on Gaussian GT, avoid fixed absolute cutoffs (e.g., `>=0.2`) as the default for moment extraction; use soft confidence weighting first, and if gating is required use adaptive peak/quantile thresholds with mass/anisotropy safeguards | Absolute thresholds are calibration-sensitive and can truncate line endpoints/tails, shrinking second moments and destabilizing theta on short or low-SNR lines; adaptive gating better preserves geometry under scale/contrast drift | Global fixed cutoff (`0.15`, `0.2`, `0.5*max`) as the primary method | 2026-03-23 |
| For U-Net sigmoid heatmaps (`0-1`) with Gaussian GT (`sigma≈2.5`), do not use all pixels for moments; default to adaptive soft gating and only use fixed cutoff as fallback (`~0.1` preferred over `0.2`) | Full-map accumulation is highly sensitive to low-level background mass (large-area bias), while `0.2` can over-truncate tails/endpoints on weak lines; `~0.1` keeps more geometric mass with sufficient noise suppression in typical calibration ranges | Keep all pixels; fixed `>=0.2` as default | 2026-03-23 |
| Standardize moment-angle extraction with closed-form `theta=0.5*atan2(2*mu11, mu20-mu02)` plus anisotropy gating (`lambda1-lambda2` / confidence threshold), and treat weak off-center crosstalk as a second-moment amplification risk (`d^2` leverage) rather than an eigensolver-formula bug | The eigensolver formula `(mu11, lambda-mu20)` is mathematically valid, but branch-based eigenvector selection and near-isotropic covariance make angles unstable; closed-form theta and anisotropy guards improve continuity/debuggability and prevent low-SNR channels from poisoning metrics/loss | Rewrite only eigenvector components; rely on `min_mass` guard without anisotropy/confidence checks | 2026-03-23 |
| For post-eval visualization, aggregate distributions at the line level but rank worst cases at the slice level with a default `max(line error)` score, while keeping `mean` as an option | Distribution plots need all valid lines, but failure triage in medical CT is better driven by the single worst line on a slice so that clinically bad outliers are not hidden by averaging | Rank directly at line level; use slice mean only | 2026-04-01 |
| Use staged training for heatmap+geometry: short heatmap-only pretrain, then enable angle loss, then add rho loss | Early geometry signals are noisy when heatmaps are weak; angle is more stable than rho and can shape representation before full constraints | Linear warmup from epoch 1; two-stage heatmap-only then all geometry | 2026-03-16 |
| For multitask vertebra boundary supervision paired with 5-class region segmentation, prefer a dense signed distance auxiliary (`per-boundary truncated SDF`) over Gaussian line heatmaps, with a shared decoder trunk and late task-specific heads | Dense distance supervision provides full-image gradients, aligns better with boundary-aware region partitioning than sparse heatmaps, and remains easier to optimize than pure global line-parameter regression; sign distinguishes opposite sides of each boundary and is therefore more useful to segmentation than unsigned distance alone | Keep Gaussian heatmaps as sole auxiliary; unsigned distance only; direct `(phi, rho)` regression only | 2026-04-14 |
| Detach prediction-derived confidence in line losses and avoid confidence-gradient gating during training to prevent trivial zero-heatmap minima | Self-weighting by confidence lets the model reduce loss by shrinking/flattening heatmaps; detaching preserves the weighting signal without incentivizing collapse | Remove confidence weighting; use GT-only masks; add explicit mass regularizer | 2026-03-17 |
| Never propagate NaN sentinel values through loss arithmetic; keep a separate validity mask, index only valid entries for angle/rho losses, and add finite-guard checks before backward | `NaN * 0` is still `NaN`; mixed valid/invalid channels can poison batch loss and collapse training when geometry warmup becomes dominant | Keep NaN sentinels and rely on weighting/masking after elementwise ops | 2026-03-17 |
| Treat epoch-48 NaN collapse as a NaN-masking arithmetic failure amplified by late warmup; prioritize NaN-safe valid-only reduction and finite guards before hyperparameter retuning | Current code computes trigonometric losses on NaN-marked entries and then masks (`NaN * 0`), which can immediately poison loss/gradients once invalid channels appear | Tune LR/clip/warmup first; rely on confidence thresholding alone | 2026-03-17 |
| Implement loss-time valid-index slicing (`idx = valid_mask & isfinite`) before trig/exp operations in `angle_loss` and `rho_loss` | Prevents NaN propagation from invalid channels and avoids empty reductions (`mean of empty slice`) | Multiply-by-mask after full-tensor arithmetic; post-hoc `nan_to_num` | 2026-03-17 |
| Sequence loss-design fixes as: (1) NaN handling in angle/rho loss, then (2) warmup formula, then (3) rho double-smoothing removal; rollout as two commits (`#3` first, then `#1+#2`) | NaN-safe arithmetic is a hard prerequisite for stable training; warmup and smoothing tune gradient shape after numerical safety is guaranteed | Apply warmup first; apply all three in one opaque change | 2026-03-18 |
| Prefer phased rollout order `#3 -> #2 -> #1` with validation gates after each commit; if speed is required, still isolate `#3` then batch `#2+#1` | `#3` is a hard stability prerequisite, while `#2` changes rho-loss curvature/scale and should be established before global reweighting in `#1` to keep effects attributable and tuning interpretable | `#3 -> #1 -> #2`; one-shot three-fix commit | 2026-03-18 |
| For 200-epoch heatmap+geometry runs, prefer adaptive geometry warmup triggered by robust `val_mse` plateau detection (EMA + relative-improvement patience + hysteresis) over a fixed 50-epoch ramp | Typical MSE plateau appears around epochs 30-50; adaptive trigger reduces wasted MSE-only epochs while avoiding premature geometry activation under noisy validation curves | Fixed 50-epoch warmup; pure gradient-threshold trigger; immediate full geometry weight at plateau | 2026-03-18 |
| Standardize plateau trigger as dual-gate detector: EMA-smoothed `val_mse`, relative-improvement threshold, patience, min-epoch guard, and one-way latch (no deactivation) | Reduces false triggers from noisy validation while still reacting near true MSE saturation; one-way latch avoids oscillation once geometry warmup starts | Raw metric slope only; absolute-delta only; trigger/revert oscillating gate | 2026-03-18 |
| After plateau trigger, ramp geometry weight with bounded cosine over ~20 epochs and keep fixed full geometry weight thereafter; enforce fallback latest-start epoch (e.g., 60) | Cosine ramp avoids gradient shock at activation; fallback start prevents never-trigger cases and guarantees geometry training budget in 200-epoch runs | Immediate step to full weight; linear ramp without fallback; fixed 50-epoch start | 2026-03-18 |
| Fix adaptive warmup defaults for current 200-epoch setup: EMA(0.9) `val_mse`, trigger when relative improvement stays below 0.2% for 5 epochs after epoch >= 20, force start at epoch 60 if not triggered; use `w(e)=0.5*(1-cos(pi*clip((e-e_start)/20,0,1)))` and total loss `L=(1-0.5w)L_mse+wL_geo` | Aligns trigger with observed MSE plateau window (30-50), limits false positives, and preserves at least ~140 epochs of geometry training even in worst case | Keep fixed 50-epoch linear warmup; use absolute-delta trigger only; immediate full geometry after trigger | 2026-03-18 |
| Given latest stable 200-epoch run (large MSE gain, but angle/rho still high), prioritize geometry-loss reweighting experiments before implementing adaptive warmup; test `lambda_rho` increase in small staged steps and tune `lambda_theta` next | Fixed 50-epoch warmup is no longer the main bottleneck after critical fixes; direct geometry signal strength is the most likely limiter for `rho_error_px`/`angle_error_deg`, while adaptive warmup adds complexity with uncertain incremental gain | Implement adaptive warmup immediately; broaden scheduler changes first | 2026-03-18 |
| From post-fix metrics (`test_mse=0.0017`, `angle=34.3°`, `rho=15.8px`), choose staged geometry reweighting as highest-ROI next step: increase `lambda_rho` gradually (e.g., 0.05->0.1->0.2), then decide on adaptive warmup only if MSE plateaus while angle/rho stall | Current gap is geometry accuracy, not heatmap fit; direct rho gradient strengthening is lower-cost and more attributable than introducing a new warmup controller, while avoiding instability risk from a 10x single jump | Immediate 10x jump to 0.5; implement warmup adaptation first; do both at once | 2026-03-18 |
| Do not disable `ReduceLROnPlateau` during warmup in current code path because scheduler is stepped with `val_loss_mse` only | Warmup changes train-time total loss mix, but scheduler monitor remains `val_loss_mse`, so disabling risks missing useful LR reductions without fixing an observed issue | Disable scheduler until warmup ends based on older assumption of total-loss monitoring | 2026-03-18 |
| Mandate explicit CLI invocation in subagent prompts via a `CRITICAL: You MUST call [Tool] CLI` prefix | Ensures subagents leverage specialized Codex/Gemini CLI tools instead of returning self-analysis | Implicit delegation instructions; allow subagents to decide whether to call CLI | 2026-03-17 |
| For moment-based line extraction, treat eigenvector-formula mistakes as low-likelihood; prioritize coordinate-convention checks (`x=col`, `y=row`), anisotropy/confidence gating, and background-mass suppression before changing core eigensolver math | For symmetric covariance `[[mu20, mu11], [mu11, mu02]]`, `(mu11, lambda-mu20)` and `(lambda-mu02, mu11)` are equivalent eigenvectors; observed 40-50° errors are more consistent with axis mixups or low-SNR/near-isotropic moments than with a pure eigensystem bug (which often yields 90°/180° patterns) | Rewrite eigenvector formula only; force minor-axis/major-axis swap without diagnostics | 2026-03-18 |
| For `2x2` symmetric eigensolver checks, accept only eigenvector forms derived from `(a-λ)x + by = 0` or `bx + (d-λ)y = 0`: `(b, λ-a)` and `(λ-d, b)`; reject component-swapped variants | Confirms `line_losses.py` current form `(mu11, lambda1-mu20)` is mathematically valid and equivalent to `(lambda1-mu02, mu11)` up to scale/sign, so large angle errors should be debugged in convention/anisotropy/noise handling instead | Replace with `(lambda-a, b)` or `(b, lambda-d)` due to apparent mismatch | 2026-03-18 |
| Standardize robust phi extraction as `theta = 0.5*atan2(2*mu11, mu20-mu02)`, `phi = wrap_pi(theta + pi/2)` with explicit degeneracy guard on anisotropy (`lambda1-lambda2`) | Closed-form `atan2` avoids component-order ambiguity in manual eigenvector construction and improves debuggability; anisotropy guard prevents unstable angles when covariance is near-circular | Keep manual eigenvector path as sole estimator without degeneracy flag; hard-threshold only | 2026-03-18 |
| **Y-axis coordinate system in moment-based line extraction must use standard math convention (Y increases upward)** - Fix: negate y_grid in line_losses.py:96 | Image convention (Y down) causes 40-50° angle errors in moment calculations; flipping Y-axis to math standard eliminates systematic angle bias and achieves <1° precision | Accept image convention and compensate in normal-vector computation; apply sign correction post-extraction | 2026-03-18 |
| For vertebra region-mask generation from 4 polylines, enforce topology first: fit each polyline with TLS/PCA, snap `(line_1,line_2)` and `(line_3,line_4)` to shared junctions, extend them to vertebra/image boundaries as separator barriers, then assign the 4 regions with marker-based filling (connected components when barriers are closed; watershed/geodesic fallback when gaps remain) | Raw annotated polylines do not always meet cleanly, so direct half-plane thresholding leaves ambiguous holes or merged regions. Topology-enforced barriers plus seed-based filling preserves the intended anatomy (body / right foramen / left foramen / posterior) and degrades gracefully on imperfect annotations | Pure connected components on unsnapped lines; direct per-pixel half-plane classification only; nearest-line Voronoi without topology repair | 2026-04-01 |
| In `Unet/preprocessing/generate_region_mask.py`, standardize fallback execution order as `junction_snap -> local_tangent_refit -> junction_clamp -> barrier_reinforcement -> seed_relabel_merge -> watershed`, keep debug payload keys fixed (`J_R`, `J_L`, `component_count`, `region_areas`, `fallback_type`), and emit `swap_detected` when left/right x-order is inconsistent | Deterministic fallback sequencing and stable debug schema make preprocessing failures reproducible and easier to triage; explicit swap signaling reduces silent left/right label drift | Ad-hoc fallback branching; variable debug payload; implicit swap handling only at training time | 2026-04-02 |
| Replace `Unet/preprocessing/generate_region_mask.py` with a compact half-plane partition pipeline (TLS line fit + nearest-endpoint junctions + direct region half-plane tests) while preserving exported API and debug schema (`J_R`, `J_L`, `component_count`, `region_areas`, `fallback_type`, `swap_detected`) | Reduces maintenance cost and branch complexity by removing multi-stage topology fallbacks, while keeping one-hot mask guarantees and existing validation/debug interfaces stable for downstream code | Keep the 800+ line fallback-heavy implementation; partially refactor existing fallback chain | 2026-04-06 |
| For z-axis propagation of sparse 4-line annotations, generate lines across the full contiguous z range where the vertebra mask exists: canonicalize each polyline orientation, resample by normalized arc length, express points relative to each slice's vertebra-mask centroid and bbox scale, interpolate between anchors, and use constrained linear-tail extrapolation outside the outer anchors before mapping through each target mask and running the existing region-mask generator | Full-volume supervision and later 3D reconstruction require endpoint slices even when anatomy is weak. Mask-space normalization follows slice translation/scale changes, while linear tails preserve the nearest observed 3D trend without the instability of unconstrained spline extrapolation | Stop at the outer anchors; interpolate 4-class masks directly; copy the nearest anchor unchanged; unconstrained high-order spline extrapolation | 2026-06-10 |
| Preserve every generated exterior slice but attach provenance and confidence metadata (`manual`, `interpolated`, `extrapolated`, anchor distance, QC flags); decay training weight with physical distance from the nearest anchor and cap extrapolated control-point displacement/rotation per slice rather than deleting low-confidence labels | Structurally ambiguous endpoint slices may still contribute to 3D continuity, but they should not influence 2D supervised training as strongly as manual or interior-interpolated labels. Explicit metadata keeps future reconstruction and training policies separable | Treat extrapolated labels as equal-confidence GT; discard all ambiguous endpoint slices; overwrite provenance in `lines.json` | 2026-06-10 |
| Gate z-interpolation rollout with leave-one-annotated-slice-out validation: reconstruct hidden anchor slices from their nearest bracketing anchors and measure per-line angle/rho/perpendicular error plus per-region Dice against masks generated from the true annotations; additionally reject outputs with left/right junction inversion, empty regions, abrupt area-ratio jumps, or failed `validate_region_mask` checks | This tests the exact synthetic-label error introduced by interpolation before expanding the training set and catches geometrically plausible but anatomically mislabeled slices | Visual inspection only; generate every slice first and assess only downstream training accuracy | 2026-06-10 |
| For sparse straight-line boundary propagation, prefer a joint slice-wise representation of two shared junction trajectories (`J_R(z)`, `J_L(z)`) plus four line orientations in doubled-angle form, fit in vertebra-mask-normalized coordinates with light smoothing inside the anchor hull and explicit capped linear tails outside it | This keeps each slice exactly straight, enforces pairwise junction consistency by construction, avoids `phi` unwrap/sign ambiguity, and is more stable than fitting raw `(phi, rho)` or bbox-edge intersections independently when annotations are sparse and endpoint extrapolation is required | Independent `(phi, rho)` splines per line; bbox-edge intersection endpoints as the primary state; generic tensor-product surface fitting | 2026-06-11 |
| For line-trajectory smoothing/extrapolation, use physical-z regression with small-noise smoothing on interior anchors, but never trust spline extrapolation directly: estimate edge slopes from the last few anchors, continue them with displacement/rotation caps, and damp tail motion as vertebra-mask area shrinks toward the ends | Anchor noise should be smoothed, but endpoint behavior must remain conservative. Explicit tail control prevents natural-cubic divergence and reduces implausible line motion on shrinking end slices while preserving 3D continuity | Exact interpolating cubic splines with natural extrapolation; unconstrained global polynomials; nearest-anchor copying at the ends | 2026-06-11 |
| For full-dataset z propagation, support source volumes in `annotation_data` NIfTI and gzip-NRRD formats, with `predata_simple` NIfTI as an explicit last-resort fallback; keep `source_mode` in each generation report | Three annotated vertebrae lacked loadable NIfTI sources, but two retained original NRRD volumes and one only had the normalized preprocessing volume. Recording the source path prevents silent mixing of coordinate provenance | Drop the three vertebrae; add an external NRRD dependency; silently use fallback volumes | 2026-06-10 |
| In `_classify_by_half_planes`, orient `line_2/line_4` body-side normals from `centroid + [0,-1]`, keep `line_3` body reference on the left side (`j_l`), and apply side-gated body constraints using `x_mid` (`right side -> s2`, `left side -> s4`) | A tilted `line_2` creates a diagonal half-plane that can incorrectly reject far-left body pixels when `s2` is enforced globally; side-gating prevents cross-side leakage, and left-reference `line_3` keeps left-body support aligned with expected anatomy in the tilted test geometry | Junction-referenced normal orientation for `line_2/line_4`; right-reference `line_3`; unconditional global conjunction of `s2 >= 0` and `s4 >= 0` | 2026-04-07 |
| Replace the `x_mid` split for the body/posterior boundary with a junction-bounded piecewise separator: use the outward `line_4` ray for `x < J_L.x`, the explicit `J_L-J_R` bridge in the central interval, and the outward `line_2` ray for `x > J_R.x`; never extend `line_2/line_4` inward past their junctions | In `sample1/C2/slice_037`, the inward infinite-line extensions intersect at `(113.5, 93.2)`, about 16 px anterior to the junctions, creating a false V-shaped boundary that assigns 129 body pixels to posterior. The piecewise bridge restores all 129 pixels with no body-to-posterior regressions in that slice and matches the intended T-junction topology | Keep the `x_mid` half-plane split; globally intersect both posterior half-planes; post-process posterior with morphology | 2026-06-11 |
| Apply the piecewise `line_4 -> J_L-J_R bridge -> line_2` separator to z-extrapolated slices only when using the propagated junction state directly; do not reconstruct junctions afterward with nearest clipped endpoints, and mark slices invalid/downweighted when junction order, bridge length, or junction-height consistency fails | The approach is valid for `sample1/C2` (all 111 extrapolated slices preserve positive `J_R.x-J_L.x`), but a full-dataset scan found 1,913/19,527 extrapolated slices with reversed junction order when junctions were re-inferred from clipped endpoints. These failures already correlate with empty-region and swap QC warnings, so unconditional application would encode invalid topology rather than fix it | Apply the bridge unconditionally to all generated `lines.json`; continue nearest-endpoint junction inference; exclude all extrapolated slices | 2026-06-11 |
| Replace global half-plane region classification with a finite planar separator graph and marker-based filling: preserve `J_R/J_L` as explicit nodes, clip each annotated line to its outward ray from the assigned junction, add only the anatomically required `J_L-J_R` bridge, rasterize these finite edges as barriers inside the vertebra mask, and label the resulting components from fixed body/right/left/posterior seeds; reject the slice when graph validation fails instead of allowing separators to cross unrelated regions | Infinite lines can influence every pixel and therefore permit `line_1/line_3` or `line_2/line_4` to create remote wedges and false intersections. Finite graph edges make separator influence local by construction, while component/seed labeling prevents one boundary from changing a non-adjacent region. Hard topology checks provide an explicit failure mode for malformed annotations rather than silently producing anatomically invalid masks | Continue patching half-plane predicates; polygon clipping without graph validation; morphology-only correction after classification | 2026-06-11 |
| For finite separator-graph construction on manual annotations, derive `J_R/J_L` from pairwise TLS-line intersections with nearest-endpoint fallback only for near-parallel pairs, and choose each outward ray sign from the largest absolute projection over all annotated points rather than from array endpoints | Annotation point order is not reliably junction-to-outward. Endpoint-only direction selection caused four valid manual slices to collapse to three components, including `sample12/C3/48` and `sample15.2/C1/46`. TLS intersections plus all-point projection restored 4-region generation for all 1,618 valid annotated slices | Infer junctions and directions from first/last points; require annotation point reordering; exclude the four affected slices | 2026-06-11 |
| Use the anterior arc of the enclosed spinal-canal hole in the vertebra mask as the central body/posterior separator, connecting its nearest contour points to `J_L/J_R`; use a straight bridge only when no valid enclosed canal contour exists | A direct `J_L-J_R` chord still cut through the posterior vertebral body and left visible posterior wedges in multiple manual slices (`sample56/C4`, `sample15.2/C7`, `sample32/C2`). Following the actual canal boundary keeps the entire vertebral body green while retaining finite, local separator topology. The canal arc is available in 1,557/1,618 manual slices, with 61 explicit straight-bridge fallbacks | Keep the straight chord; globally extend `line_2/line_4`; post-process yellow wedges by area threshold | 2026-06-11 |
| Supersede full finite-graph regeneration with a local posterior-to-body correction on top of the established half-plane mask: only relabel pixels that are currently posterior, lie between `J_L/J_R`, remain inside both body-side constraints from `line_1/line_3`, and sit anterior to the spinal-canal anterior boundary; cap the correction at 7% of vertebra area and leave right/left classes untouched | Full separator-graph and canal-arc regeneration altered otherwise-correct boundaries and visibly worsened some slices. The local correction changes only the known failure mode. Across all 1,618 valid manual slices it changed 15,513 pixels, exclusively `posterior -> body`, with zero red/blue changes and zero generation failures | Replace all four regions with finite graph output; apply canal arc globally; hand-edit individual slices | 2026-06-11 |
| When TotalSegmentator vertebra masks are sometimes distorted, treat the vertebra mask as a noisy outer support rather than trusted anatomy: prioritize automatic QC/triage and region-generation logic that is insensitive to outer-boundary defects (line/junction-driven partitioning, conservative erosion/interior weighting), and only then add training-time compensation; escalate to more direct line-supervised formulations only if this still leaves an error floor | Gross outer-mask errors are structured upstream noise. Fast wins come from blocking bad masks and reducing how strongly the algorithm depends on the distorted boundary. Training tricks rarely repair severe support errors, while post-processing is mostly useful for small leaks/holes rather than topological mistakes | Rely on augmentation/loss tuning first; keep raw vertebra boundary as a hard constraint everywhere; jump immediately to a fully boundary-free weak-supervision rewrite | 2026-04-06 |
| For `Unet/preprocessing/visualize_region_masks.py`, use deterministic random sampling (`seed=42`) that first picks one random slice per `(sample, C3-C7 vertebra)` pair and then truncates/fills to ~20 total slices, saving both per-slice PNGs and a 4-column combined grid | Keeps sample/vertebra coverage while making visualization results reproducible for debugging fallback behavior (`fallback_type`, `J_R`, `J_L`) and region-label quality checks | Fixed first/last slice selection per pair; random draw from full global slice pool without pair-aware first pass | 2026-04-02 |
| Root cause of 40-50° angle errors was Y-axis inversion at coordinate grid creation (line_losses.py:96), NOT eigenvector formula | Y-grid defined as `range(H)-H/2` maps row 0→-H/2 (top, Y increases downward); math requires Y increase upward; fix: `y_grid = -(range(H)-H/2)` eliminates non-linear angle distortion in moments | Modify eigenvector calculation; add post-hoc angle correction; redesign normal-vector logic | 2026-03-18 |
| When resolving GT-vs-pred convention mismatch in `line_losses.py`, keep prediction extraction in math coordinates (Y-up) and apply Y-flip to GT center coordinates (`p*_c[1] = -(p*[1]-center)`) | Preserves validated moment-extraction fix on prediction side and restores `(phi, rho)` consistency between GT and prediction without reverting known angle-accuracy improvements | Remove Y negation in prediction grid (revert to image coordinates) | 2026-03-18 |
| Validate moment-based fixes with synthetic line tests (0°, 45°, 90°, 135°) achieving <0.5° error before full re-training | Unit tests with known-angle synthetic heatmaps confirm coordinate-system fix eliminates systematic errors and validates eigenvector math correctness | Skip unit tests and validate only on real data after full training | 2026-03-18 |
| Keep `(phi, rho)` in math coordinates (`y` upward) end-to-end and convert GT polylines from image coordinates at extraction time (flip centered `y` in `extract_gt_line_params`), rather than removing prediction-side `y_grid` negation | Predicted moment extraction, synthetic-angle tests, and phi/rho visualization already assume Cartesian convention (`row = -y + H/2` for rendering). Converting GT once preserves annotation format while removing pred/GT frame mismatch that yields ~90° diagonal errors | Remove `y_grid` negation and keep all phi/rho math in image coordinates | 2026-03-18 |
| Adopt a boundary-adapter coordinate pattern: keep raster operations (annotation pixels, heatmaps, overlays) in image coordinates (Y-down), while defining geometric line parameters `(phi, rho)` in math coordinates (Y-up) with explicit one-way converters at GT extraction and visualization boundaries | Aligns with CV data conventions for pixel-domain tasks and with Hesse-normal stability for geometry; minimizes refactors by localizing conversions to interfaces and preventing hidden frame drift across train/eval/vis | Force pure math coordinates everywhere; force pure image coordinates everywhere; ad-hoc mixed conversions without explicit adapters | 2026-03-18 |
| Make `Unet/line_only/line_detection.py` the single source of truth for `detect_line_moments` and evaluation logic; keep `Unet/line_detection_moments.py` only as a short-lived compatibility shim before removal | The legacy file still uses image-space moments and older centroid/angle metrics, which can reintroduce coordinate drift and duplicate maintenance. A shim-first migration preserves external callers while preventing future divergence | Directly copy patches between two files indefinitely; immediate hard delete without compatibility window | 2026-03-19 |
| Do not "copy only `detect_line_moments`" into legacy `Unet/line_detection_moments.py`; if migration is needed, route callers to `Unet/line_only/line_detection.py` via a thin shim and deprecate | Legacy evaluation compares predicted angle against polyline PCA in image coordinates (`centroid_dist/angle_diff`), while canonical implementation evaluates `(phi, rho)` in math coordinates (`angle/rho/perpendicular`). Partial copy creates mixed-frame metrics and silent regressions | Keep legacy evaluator and patch only moment grid; maintain two divergent evaluators long-term | 2026-03-19 |
| In `line_only` metrics, convert GT points to math coordinates before perpendicular-distance evaluation (`x'=x-center`, `y'=-(y-center)`), not image-centered coordinates (`y'=y-center`) | Predicted `(phi, rho)` already lives in math coordinates; using image-space `y` in distance checks inflates errors (especially diagonals) and masks true geometry progress | Keep current mixed-frame distance calculation; drop perpendicular metric entirely | 2026-03-19 |
| Prefer a two-phase unification rollout: compatibility shim now (`line_detection_moments.py` re-export/deprecation warning), hard removal only after zero-usage scan and parity tests | Reduces immediate break risk for ad-hoc scripts while still enforcing a single canonical implementation (`line_only`) and preventing further coordinate drift | Immediate delete with potential hidden import breaks; continued dual-maintenance | 2026-03-19 |
| For 4-channel line heatmap crosstalk (5-10%), prioritize a GT-aware anti-crosstalk suppression term (one-vs-rest negative masks) plus optional inference-time competitive gating; treat global channel-correlation minimization as auxiliary only, and use spatial softmax only with an explicit background channel when per-pixel exclusivity is valid | The observed failure mode is localized leakage near other GT lines that strongly perturbs moment covariance/angle despite small global energy. Local negative supervision directly targets this mechanism, while pure correlation loss is weakly aligned and 4-channel softmax without background causes forced false assignments in background pixels | Keep MSE-only; add correlation-only independence loss; 4-way softmax without background | 2026-03-20 |
| Standardize crosstalk training as a staged composite objective: keep base heatmap regression, ramp in anti-crosstalk suppression after initial convergence, optionally add **masked** 5-class local competition (`4 lines + background`) only inside GT-union neighborhoods, and cap any decorrelation regularizer to a small auxiliary weight | Prevents early optimization collapse and avoids global softmax side effects in background regions; localized competition aligns with the real failure mode (leakage near other GT lines) while preserving valid overlaps/intersections and keeping gradients attributable in ablations | Apply all penalties at epoch 1; full-image softmax competition; heavy decorrelation regularization | 2026-03-20 |
| Treat full multi-head UNet (`shared encoder + 4 full decoders`) as a secondary option for crosstalk, not the default: first ship loss-level anti-crosstalk + localized competition, and if architectural separation is needed, prefer lightweight head-specific adapters/late-branch decoders before full decoder duplication | Shared skips still leak class-ambiguous spatial cues; full decoder split can reduce gradient interference but costs ~2-3x params/VRAM and can overfit line identity priors. Loss-level supervision is more compute-efficient and directly targets localized leakage that drives moment-angle errors | Immediate full multi-head migration; keep single-decoder MSE-only baseline | 2026-03-20 |
| Use an explicit crosstalk escalation gate: (1) loss-level anti-crosstalk first, (2) late-branch/head-specific adapters next, (3) full shared-encoder + 4-decoder only as last resort; for current TinyUNet baseline (`483,348` params), full 4-decoder variant is `1,050,180` params (~`2.17x`) | Keeps compute growth proportional to measured failure severity and preserves attribution in ablations. This avoids paying full decoder-duplication cost before lower-cost supervision/branching options are exhausted, while still defining a clear path if residual leakage keeps moment-angle errors high | Immediate full multi-head jump; indefinite loss-only tuning without architecture fallback criteria | 2026-03-20 |
| If architecture escalation is required, prefer late-branch multi-head with head-specific skip adapters (per-head `1x1` projections/gates on skip tensors) before full decoder duplication; if full multi-decoder is used, avoid raw shared skips | Crosstalk often propagates through class-ambiguous skip features. Head-specific skip adapters suppress leakage while retaining most shared compute; this captures much of the separation benefit at lower parameter/VRAM cost than duplicating all decoder blocks | Full 4-decoder with unfiltered shared skips; immediate encoder+decoder duplication | 2026-03-20 |
| For observed 5-10% inter-channel crosstalk, do not assume full multi-head UNet alone will deliver equivalent 5-10% absolute reduction; treat expected gain as moderate/uncertain and justify the ~2.17x parameter cost only if loss-level suppression + late-branch adapters fail to recover angle/rho targets | Decoder separation can reduce gradient interference, but shared encoder/skips and localized overlap failure modes often leave residual leakage; lower-cost objectives/adapters usually offer better ROI first | Immediate full 4-decoder adoption as first intervention | 2026-03-20 |
| For multitask cervical-spine MRI line detection + segmentation, prioritize quick-win improvements in this order: robust line loss / moment extraction, targeted hard-case handling for C1/C7, then architecture branching only if shared-decoder interference remains measurable | Current multitask baseline already converges quickly and segmentation is strong (`fg_mIoU≈0.888`, `fg_mDice≈0.939`), so the main ROI is reducing line-estimation outliers from diffuse or ambiguous heatmaps rather than increasing model capacity first. This preserves iteration speed and keeps ablations attributable | Immediate backbone replacement; full decoder split as first step; segmentation-weight increase as primary lever | 2026-04-09 |
| Define the "uncertain effectiveness" claim for full multi-head UNet as a three-factor statement: (1) **theoretical ceiling** from shared encoder/skip information sufficiency, (2) **empirical variance** in how much decoder-only decoupling reduces leakage, and (3) **cost gate** from ~2.17x model size | Multi-head decoders remove parameter sharing in decoder blocks but do not remove gradient coupling or representational entanglement in shared encoder/skips; therefore gains are plausible but not guaranteed, and must be validated against cheaper objective-level interventions first | Treat uncertainty as intuition-only; assume either guaranteed failure or guaranteed success without ablation | 2026-03-20 |
| **[REVISED] Apply fixed threshold (>= 0.2) to sigmoid heatmaps before moment extraction as post-processing; threshold is NOT applied during training loss computation** | Experimental validation on 411 test samples shows threshold=0.2 achieves 27.4% mean error reduction (32.70°→23.73°), 64.1% median error reduction (28.10°→10.09°), and 340% confidence boost (0.214→0.941) with only 1.2% sample degradation. Root cause: removes low-intensity "tails" that cause isotropic variance (mu20≈mu02) and unstable atan2 denominator. Previous "avoid fixed cutoffs" guidance was based on theoretical bias concerns, but empirical results show dramatic practical benefit. Training remains threshold-free to preserve gradient signal. | Soft weighting only; adaptive per-sample threshold; reject all hard thresholds based on theoretical bias risk | 2026-03-23 |
| Threshold processing should be implemented as inference-time post-processing in `extract_pred_line_params_batch()`, NOT in the training loss computation path | Applying threshold during training would remove gradient signal from low-confidence regions and potentially cause training collapse. Threshold at inference preserves full training dynamics while gaining stability benefit when actual line parameters are extracted for evaluation/deployment. | Apply threshold in both training and inference; make threshold trainable; use different thresholds for train vs test | 2026-03-23 |
| Hard threshold (`0.2`) inside training-time geometry loss is not the default; allow only as a late-phase ablation with strict safeguards (MSE-dominant weighting, capped geometry-gradient ratio, and dual-path monitoring), while preferring soft gating for production training | A fixed cutoff in the loss path can starve gradients below threshold and incentivize peaky heatmaps. Late activation lowers collapse risk but does not remove optimization bias, so guardrails are required. Soft gating preserves most stability benefit while keeping gradient continuity. | Enable `0.2` from epoch 1 in angle/rho loss; disable threshold at inference despite measured gains | 2026-03-23 |
| If hard threshold is tested in training-time geometry loss, enable only after `val_mse` plateau and ramp both threshold (`0.0 -> 0.1 -> 0.2`) and geometry weight; auto-disable on spike/collapse indicators | Preserves early dense gradients for heatmap formation, then introduces noise filtering gradually to stabilize moment geometry while reducing abrupt optimization bias from binary gating. Automatic rollback limits irreversible collapse when geometry loss starts dominating. | Fixed `0.2` from warmup start; one-shot switch to thresholded geometry branch | 2026-03-23 |
| Expected post-threshold performance after training code integration: angle_error <15° (ideally <10°), maintaining 98.8% improvement rate with 1.2% acceptable degradation on weak-response samples | Current threshold=0.2 on existing model achieves 23.73° mean error. With training optimizations (proper gradient flow, geometry loss tuning), threshold processing should push final error below 15° while confidence remains >0.9. Degraded 1.2% samples are training issues (weak heatmap response), not threshold failures. | Expect <5° immediately; accept >20% degradation rate; require 100% improvement with no trade-offs | 2026-03-23 |
| Keep `extract_pred_line_params_batch()` backward compatible by adding optional `threshold` with default no-filter (`None`); enforce explicit call-site values `threshold=0.0` for training loss path and `threshold=0.2` for evaluation metrics path | Preserves existing callers/tests while making behavior explicit at critical paths and preventing accidental training-time filtering. Default no-filter avoids silent behavior changes in scripts that rely on current outputs. | Separate threshold-specific extraction function; changing default extractor behavior to thresholded by default | 2026-03-23 |
| Implement thresholding as a shared pre-moment transform (`hm_proc = torch.where(hm >= threshold, hm, 0.0)`) inside `extract_pred_line_params_batch`; keep `threshold=None` default, hard-code training/loss caller to `0.0`, and set metrics caller to `0.2` (optionally config-driven with default `0.2`) | Centralizing threshold logic avoids duplicated extractor variants and keeps confidence/moment math consistent between filtered/unfiltered paths. Explicit call-site constants remove accidental training-time filtering while preserving backward compatibility. | Separate threshold-specific function; apply mask only in metrics post-step; change extractor default to `0.2` | 2026-03-23 |
| **[CRITICAL BUG] Fix meshgrid coordinate assignment in both `line_detection.py:87` and `line_losses.py:109` to correct mu20/mu02 swap causing vertical heatmap → horizontal line mismatch** | `X, Y = np.meshgrid(x_grid, y_grid)` in line_detection.py and `Y, X = torch.meshgrid(y_grid, x_grid, indexing="ij")` in line_losses.py both swap X/Y variable assignment, causing mu20 (X-variance) to measure Y-variance and vice versa. Result: vertical blobs incorrectly show mu20 > mu02 and are interpreted as horizontal lines. Fix: `Y, X = np.meshgrid(y_grid, x_grid)` and `X, Y = torch.meshgrid(x_grid, y_grid, indexing="xy")` | Accept current wrong coordinate system; add post-hoc angle correction; redesign entire moment extraction | 2026-03-23 |
| For vertebra-aware conditioning (C1-C7) on TinyUNet with small patient count, start with bottleneck-only conditioning and identity-preserving initialization: one-hot label map concat at bottleneck, `1x1` projection initialized as pass-through for original channels and zero for label channels, then run staged ablation (bottleneck-only -> FiLM if needed -> multi-level only if necessary) | Keeps implementation delta small and attribution clear, limits overfitting risk under low-N regime, and avoids disrupting pretrained behavior at step 0 while still enabling label-aware adaptation during fine-tuning | Immediate multi-level conditioning; high-dimensional learned embeddings first; random-init fusion that changes backbone output at load time | 2026-03-30 |
| Load optional offline `gt_masks` in `PngLineDataset` without dropping slices; store missing files as `gt_mask_path=None`, return a zero `gt_region_mask` plus explicit `has_gt_region_mask`, treat `gt_mask` as an Albumentations `mask`, and swap labels `2<->3` only when horizontal flip is actually applied | Keeps dataset cardinality stable, preserves default DataLoader collation, allows mixed supervision without special-case batching, and keeps anatomical semantics consistent after flips | Skip slices without `gt_masks`; return `None` and require custom collate; augment as image-like target without label-aware interpolation | 2026-04-07 |
| Add dataset-wide offline GT mask generation script that traverses every `dataset/sample*/C1-C7`, validates all four polylines plus image/mask presence, skips `hard_fail` slices, and saves successful outputs as single-channel label PNGs (`0-4`) under per-vertebra `gt_masks/*.png` | Creates a reproducible supervision-preparation step for UNet training while preserving explicit failure reporting for bad annotations or degenerate geometry and keeping labels storage-compatible with image pipelines | Ad-hoc per-slice generation; save outputs even when validation hard-fails | 2026-04-06 |
| Expose region-mask readiness in the annotation tool as a per-slice list with `ready/outdated/missing` states, using both file existence and modification time relative to `lines.json`, and allow direct overlay inspection on the annotation canvas | Lets annotators verify that every completed line annotation has a corresponding current `gt_masks` and `gt_overlays` output without leaving the annotation workflow; timestamp comparison flags annotations saved after the last preprocessing run | Existence-only badge; separate command-line inspection; aggregate counts without slice navigation | 2026-06-10 |
| Retrain the line-only TinyUNet on the expanded 1,610-slice C1-C7 annotation set using the established sigma=3.5 augmentation configuration, writing to a new dated experiment directory instead of overwriting the April checkpoints | Preserves the prior baseline for comparison while incorporating all newly completed line annotations into a reproducible full-vertebra training run | Overwrite `outputs/aug変換修正/sig3.5`; train only C3-C7; switch architecture during the data refresh | 2026-06-10 |
| Persist one shared effective line-only training configuration as `config.yaml` in the experiment output directory after applying common CLI overrides | Fold runs share the same hyperparameters, so one resolved config is sufficient and avoids redundant fold-numbered files while still capturing overrides such as `--gpu_id` and `--all_vertebrae` | Per-fold config files; copy only the source config; rely on checkpoint-embedded config only | 2026-06-10 |
| For the first multitask cervical-spine CT segmentation + line experiment, use a GN-based basic ResUNet with learnable `1x1` projection shortcuts, shared encoder + fully independent seg/line decoders, and default width `[24,48,96,192]`; defer seg-to-line feature feeding until after the baseline is measured | Dual decoders already add substantial capacity under partial labels; `[24,48,96,192]` stays within the practical ~1-2M budget, avoids extra cross-task coupling in the first run, and is safer than a full `[32,64,128,256]` dual-decoder model (~2.8M params) for ~500 training slices | Full `[32,64,128,256]` from the start; zero-padded shortcuts; early cross-task attention/concatenation between decoders | 2026-04-08 |
| For the initial partial-label multitask loss, keep `L_total = L_line + alpha * L_seg` with line treated as the primary task and segmentation as auxiliary regularization; compute `L_seg` only over labeled samples, start with fixed `alpha` in the `0.02-0.05` range (default `0.03`), avoid conditional per-batch alpha scaling, and escalate to uncertainty weighting or GradNorm only if monitoring shows seg stagnation or clear negative transfer | This matches the data regime (100% line GT, ~50% seg GT), keeps optimization behavior interpretable in the baseline, avoids hidden changes in effective seg weight across batches, and delays dynamic weighting complexity until there is evidence that fixed weighting cannot maintain both line quality and seg learning | Make segmentation the primary/base term in reporting; conditional alpha boosts on seg-labeled batches; immediate dynamic weighting from epoch 1 | 2026-04-08 |
| When multitask training degrades line-heatmap quality relative to line-only, first protect the line task with staged segmentation activation, line-metric-based checkpoint selection, and lightweight task-specific line adapters before escalating to heavier architecture splits; keep early fixed `alpha_seg` at `0.01-0.02`, ramp to `0.03` only after heatmaps stabilize, and use dynamic weighting only after the warmup phase if negative transfer persists | In this failure mode, the dominant risk is encoder drift toward dense segmentation features while the sparse heatmap task loses peak sharpness and angular fidelity. Curriculum and selection changes are cheaper, easier to attribute, and usually recover more line quality per unit complexity than immediate backbone/decoder duplication | Raise `alpha_seg` to recover segmentation; select checkpoints by combined val loss; immediately fork the encoder or duplicate full decoders | 2026-04-09 |
| If replacing the dual multitask decoders with a shared decoder trunk, prefer sharing `up4/dec4 + up3/dec3` and branching into task-specific `up2/dec2/head`; route `x3/x2` skips through the trunk, keep `x1` branch-local, and ramp segmentation influence into the shared trunk before considering an earlier split or one-way boundary fusion | In the current `[24,48,96,192]` ResUNet this preserves task-specific full-resolution decoding while removing the heaviest duplicated decoder blocks (`dec4/dec3`), reducing total params by about 27% (`1.58M -> 1.15M`) and keeping the first conflict-mitigation step small and attributable. A gradual seg ramp is safer than a hard delayed-on switch, and detached boundary fusion should remain a phase-2 ablation behind the base shared-trunk test | Keep fully independent decoders; split earlier at `dec3`; add detached line-map concat from the start | 2026-04-12 |
| For 5-class vertebra region segmentation, prefer a loss-level boundary emphasis over new heads: compute **internal foreground-foreground boundaries** directly from the label map (`class change` where both sides are `>0`), dilate to a narrow band (`~3 px` default), replace plain CE with a normalized boundary-weighted CE map, and add a small class-wise Dice term restricted to that boundary band; keep distance/surface losses as secondary ablations if this plateau remains | The clinically important targets are separator lines between `body/right/left/posterior`, not the outer vertebra silhouette. A GT-derived internal boundary band is exact, cheap, and architecture-free; boundary-weighted CE stabilizes class assignment at separators while band-restricted Dice offsets thin-region imbalance. This is simpler and less brittle than full contour/topology losses, and fits the current strong baseline (`fg_mdice≈0.936`) where gains are expected to be local rather than global | New decoder/head for boundaries; topological loss as first intervention; outer-boundary-dominant weighting; pure surface loss as the only boundary term | 2026-04-13 |
| For the first `Unet/seg_sdf/` baseline, use fixed multitask loss weights instead of EMA normalization, learned uncertainty weighting, or GradNorm | Fixed weights are the most reproducible and easiest to debug across 5-fold CV; the expected raw loss scales (CE `~0.3-0.8`, truncated-SDF SmoothL1 `~0.05-0.2`) are different but not catastrophically mismatched, so a simple tuned constant is safer than adaptive schemes that can drift across folds or training phases | EMA-normalized loss weights; Kendall uncertainty weighting; GradNorm | 2026-04-14 |
| For 4-boundary truncated-SDF supervision, define channels by anatomical boundary identity (`upper/lower/left/right`) and generate the signed distance target after spatial augmentation from the transformed boundary geometry or masks | This keeps channel semantics stable under flips/rotations and avoids fragile sign/channel bookkeeping. Recomputing SDF post-augmentation is the safest way to preserve a consistent `inside-positive, outside-negative` convention for each boundary channel | Image-frame sign conventions tied to raw coordinates; precomputed SDF targets with augmentation-time sign flips/swaps only | 2026-04-14 |
| For the initial seg+SDF shared-decoder architecture, keep sharing through `dec3` and branch at `dec2` so each task gets its own final full-resolution block plus head | This preserves most of the mutual benefit from boundary-aware shared features while giving segmentation and continuous SDF regression separate high-resolution adaptation capacity where task interference is most likely. It is a low-risk baseline and matches the current shared-trunk implementation pattern | Share all decoder blocks and split only at the final `1x1` head; split earlier and give SDF more private decoder depth | 2026-04-14 |
| For line-heatmap supervision under online augmentation, keep Albumentations for `image`/`mask` transforms but pass each boundary polyline as `keypoints`, then regenerate heatmaps from the transformed polylines instead of warping pre-rendered heatmaps or reconstructing affine matrices from replay logs | This keeps geometric transforms consistent with the actual augmentation implementation, cleanly separates geometric vs intensity-only transforms, preserves correct interpolation policies (`image`: bilinear, `mask`: nearest), and removes brittle replay-to-matrix reconstruction logic. Using `remove_invisible=False` keeps polyline topology intact so out-of-frame points can be clipped only at rasterization time | ReplayCompose + manual affine reconstruction; fully custom augmentation stack; direct interpolation of pre-rendered heatmaps | 2026-04-15 |
| Use the raw-DICOM tool as the sole future manual fracture-region annotation path: draw each `train_bounding_boxes.csv` rectangle directly in the native DICOM pixel frame without resize or reprojection, attach `C1-C7` only from persisted `assigned_bbox_slice_numbers`, split runs by true DICOM series adjacency, and save judgments to `fracture_region_labels_dicom.csv`; retain the bbox-centered corrected 15-plane tool only for visualization/QC and never use it to create or update annotation labels | Keeps the manual label source of truth on the original acquisition frame with the exact source bbox and assigned vertebra mask, prevents derived/reprojected views from becoming annotation inputs, and avoids maintaining two competing label-generation paths | Continue both tools as annotation paths; create labels with the bbox-centered tool; modify the existing tool in place; infer levels from slice ranges; share the previous label CSV | 2026-07-20 |
| In the raw-DICOM bbox annotation tool, overlay only the canonically assigned vertebra's cleaned 3D mask in cyan by nearest-neighbor sampling at every native DICOM pixel center, then draw the unmodified CSV bbox in red above it; keep both rendered PNGs and bounded mask volumes memory-only | Directly exposes whether the persisted vertebra assignment agrees with the source bbox while preserving categorical mask labels and the native DICOM frame. Drawing bbox last prevents the mask tint from hiding the source annotation, and memory-only generation avoids a redundant image dataset | Show bbox without mask context; overlay all C1-C7 masks simultaneously; persist rendered PNGs; interpolate categorical masks linearly | 2026-07-20 |

| Implement Stage4 as a separate `train_models/stage4/` package and run confirmatory Weak-only/Mixed training for exactly 75 epochs without outer-fold early stopping | Keeps Stage4 mixed-supervision data, loss, sampler, and evaluation behavior isolated from the established Stage3 pipeline while fixing identical update budgets across confirmatory arms | Extend Stage3 behind config flags; retain validation-selected stopping; choose epochs after opening confirmatory region OOF results | 2026-07-29 |
| Invalidate the initial Stage4 Weak-only run and do not resume confirmatory training until stratum-level bag-risk correction and effective 1:1 strong/negative region-loss weighting are implemented and verified over a complete epoch | The fixed `2 strong / 2 weak / 4 negative` sampler overrepresented strong positives in `L_vertebra`, while repeated strong bags versus once-presented sampled negatives produced approximately `12.5:1` rather than `1:1` region-supervision exposure. Fold 0 consequently showed train-loss collapse and severe probability saturation unlike Stage3 | Accept the fold 0 result because AUROC/AP remained nontrivial; continue all 50 folds with the same biased sampler; correct only positive-vs-negative class prior | 2026-07-30 |
| Recover Stage4 with four-stratum population-risk aggregation (`strong`, `weak`, `sampled negative`, `other negative`), exact per-batch strong/sampled-negative region supervision of `1:1`, exposure manifests, and data-protocol version `2`; monitor validation every epoch but keep the epoch-75 final checkpoint fixed | Fixed-ratio sampling remains useful for batch stability, but its raw mean is not the Stage3 population objective and repeated matched negatives otherwise dominate both bag and instance losses. Separating sampling from objective weights restores the unique-fold population risk, while exact per-batch region balance makes the configured region `pos_weight` match actual optimization exposure. Protocol and output versioning prevents accidental resume from invalid checkpoints | Natural-frequency batches with unstable strong-label coverage; positive-vs-negative-only correction; epoch-only balance that can drift by DDP rank; resume the invalid version-1 checkpoints | 2026-07-30 |
| Implement 3D line-surface learning as an independent `Unet/line_surface_3d/` project, use only `Unet/line_only/` as the existing-code reference, and base the first model on a standalone TinyUNet adaptation with derived `2N` input and `4N` output channels | Keeps the new slab/ribbon experiment isolated from established projects, honors the line-only baseline as the sole reference, and avoids coupling the implementation to multitask ResUNet behavior while preserving the proven heatmap-moment path | Extend `Unet/line_only/` in place; reuse `Unet/multitask/` ResUNet; import either project as a runtime dependency | 2026-07-30 |
| Make `line_surface_3d` emit the full `line_only` validation schema using the same manual-polyline `(phi,rho)` targets and adaptive-threshold connected-component prediction extraction; select checkpoints by the common `angle_error_deg`, drive the scheduler and early stopping by common `val_loss_mse`, and expose ribbon-only diagnostics separately under `surface_*` keys | The first implementation monitored unthresholded surface moments and reused fitted angle for patience, stopping fold 0 at epoch 17 while MSE improved continuously. A common primary schema makes 2D/3D results directly comparable and preserves the proven `line_only` control behavior, while namespaced raw/fitted angle and centroid diagnostics retain the additional 3D observability without changing metric meaning | Keep only surface-moment metrics; alias different algorithms under the same names; stop and select by fitted surface angle; stop and select only by MSE | 2026-07-30 |
| Keep `line_surface_3d` at eight DataLoader workers but cap prefetch to one batch per worker and persist workers across epochs | Batch size 16 with 15 CT and 15 mask channels plus 60 target heatmaps makes each prepared batch large. Eight workers reduced epoch time from 281.7 to about 37 seconds, but default two-batch prefetch and per-epoch worker recreation add avoidable host-memory, file-descriptor, and pin-memory pressure | Return to zero workers; use default prefetch two; recreate workers every epoch; reduce the user-confirmed batch size | 2026-07-30 |
| Treat each anatomical boundary in the future full-3D `line_surface_3d` formulation as one plane, not a curved or twisted surface, and preserve the signed direction of that plane's tilt along the z axis as essential target information | The cross-slice geometry must identify not only the line seen on each image but also which way the plane moves as z increases; curved-surface capacity would change the intended problem definition | Curved surfaces; twisted ribbons; representations that discard the signed z-direction tilt | 2026-08-03 |
| Concentrate manual plane supervision on the central z band where anatomical boundaries around the transverse foramina are visually identifiable; make avoiding transverse-foramen-crossing partitions in this band the primary correctness requirement, and use the resulting single plane for geometric extension outside the visible band rather than requiring manual outer-slice boundaries | The boundary cannot be identified reliably by eye on superior/inferior slices, while an anatomically correct central plane prevents the clinically important failure of cutting through a transverse foramen. Requiring broad-z manual labels would add uncertain targets rather than better supervision | Require labels distributed across the full vertebral height; optimize outer-slice line agreement ahead of central transverse-foramen topology | 2026-08-03 |
| Defer a new 3D architecture and use the completed `line_surface_3d/baseline-v1` 2.5D pipeline as the immediate validation vehicle: add a strict differentiable plane projection to its per-slice heatmaps, define reliable-tilt versus zero-slope GT, and test tilt-aware losses and evaluation before deciding whether a 3D model is justified | The current independent dataset is too small to attribute a failed first 3D run cleanly to geometry, supervision, or model capacity. The trained 2.5D baseline already provides stable central line evidence and a five-fold comparator, so it can isolate whether signed plane tilt is learnable and measurable with the existing annotations. A 3D model remains a later option only after this validation establishes useful targets, losses, and metrics | Begin with a new 3D encoder-decoder; direct plane regression; inference-only plane fitting without tilt-aware fine-tuning; abandon plane-tilt validation because the first 3D model is difficult | 2026-08-03 |
| When generating a plane GT from the central manual band, keep the signed z slope only when its direction is supported reliably by the annotations; otherwise set the slope to zero and create the GT by vertically extruding the fitted central line unchanged along z. Apply this decision per boundary plane and persist the original slope, fallback flag, and confidence/QC evidence | Near-zero or unstable slopes have no reproducible signed direction and would inject arbitrary tilt labels. A zero-slope plane preserves the visible central anatomy, remains a valid plane, and gives a conservative full-z target without inventing uncertain superior/inferior geometry | Force every fitted nonzero slope into GT; discard ambiguous cases; replace the plane with a curved interpolation | 2026-08-03 |
| Treat the current data audit as a reason to validate with the shared 2.5D baseline first: use the 175 vertebrae with at least five valid central slices for plane-projected supervision, optionally use the remaining annotated vertebrae for local sparse line loss only, share one model across C1-C7, and preserve sample-level splits | After QC there are only 40 independent samples; a five-fold split leaves 24 training samples and roughly 102-108 plane-supervised vertebrae per fold. Correlated slices and multiple vertebrae from one sample do not replace independent subjects. Reusing the existing compact 2.5D model minimizes capacity confounding while the plane GT, fallback, loss, and evaluation contracts are validated | Start a 3D feasibility model before validating the supervision contract; count every annotated slice as independent; train separate C1-C7 networks | 2026-08-03 |
| Replace the ribbon fit with a strict differentiable plane projection: obtain a shared in-slice normal from a confidence-weighted doubled-angle mean, canonicalize it once per slab with `normal_y > 0` (and `normal_x >= 0` at the tie), compute every signed offset by projecting the heatmap centroid onto that shared normal, and fit only a regularized linear offset in centered physical z. Represent signed motion by the orientation-invariant vector `v = k * n`; augment the heatmap fit with a small zero-initialized bottleneck tilt-vector head rather than relying on frozen-prediction post-processing alone | Per-slice normal canonicalization can flip rho and corrupt slope, while the current 3.116 px central rho error is larger than the 1.296 px median annotated-band movement. Doubled angles remove the line's 180-degree ambiguity, one slab-level sign choice preserves signed offsets, ridge shrinkage handles concentrated evidence, and a global head gives the encoder a direct path to learn weak cross-slice tilt while retaining the heatmap geometry path | Keep the first-order ribbon; canonicalize every slice independently; fit planes only after inference; use either pure implicit heatmap fitting or unconstrained direct plane regression alone | 2026-08-03 |
| Train the strict-plane experiment by fine-tuning each fold's own baseline-v1 checkpoint with masked central heatmap loss retained throughout, delayed plane-projected loss, annotation-uncertainty-weighted tilt-vector loss, and low-weight virtual extrapolation at `+/-4 mm`. Treat unreliable surfaces as operational vertical fallbacks but down-weight their tilt-vector term and use a near-zero dead zone. Define reliability independently per surface from annotation count/span, movement, slope significance or strong movement, leave-one-out/odd-even sign agreement, and plane residual QC | Fine-tuning gives the small-data experiment a controlled central-accuracy starting point. Sparse labels must never become negatives; annotation-derived uncertainty should weight rather than be learned away; and full-strength zero labels on roughly 40% near-flat surfaces would encourage trivial tilt collapse. Per-surface decisions retain trustworthy boundaries when another boundary on the same vertebra is ambiguous | Retrain from scratch first; supervise unlabeled slices as background; give every fitted nonzero slope equal weight; apply reliability at whole-vertebra level; use long-range synthetic extrapolation as if it were observed anatomy | 2026-08-03 |
| Select strict-plane checkpoints by reliable-surface symmetric virtual line-position error at `+/-4 mm`, subject to central-band non-inferiority gates, and judge image-dependent signed tilt against training-fold-only zero/global/per-line/per-level/per-level-plus-line priors using pooled sample-clustered OOF inference. Require paired cluster-bootstrap superiority, not fold-mean significance, before considering any 3D escalation | Central rho noise can hide or mimic the small tilt signal, and five folds are not independent replicates. The virtual metric magnifies signed slope error, central gates protect the clinically visible anatomy, and per-level-plus-line priors test whether the network uses image evidence rather than memorizing anatomy. If the 2.5D model cannot beat that prior, greater 3D capacity on the same subjects is not evidence-based | Select by raw heatmap MSE or unguarded tilt loss; compare only with `k=0`; treat surfaces/windows as independent; proceed to 3D after a null 2.5D result | 2026-08-03 |

| Redesign Baseline 1 `matched` optimization around the current 2,122--2,125 training bags/fold: no backbone freeze, two-epoch all-layer warmup from 0.1x, differential initial LR `backbone=1e-4` / `head=3e-4`, then `ReduceLROnPlateau` on `val_bce` (factor 0.5, patience 4, relative threshold 0.1%, cooldown 1, minima `1e-6` / `3e-6`), with a 100-epoch safety cap, checkpoint eligibility from epoch 1, AUROC patience 15, and global-norm clip 5.0 | The superseded 10-epoch freeze generated about 1,330 head-only updates, then reset head LR by 33x while unfreezing. Fold 0 learned (`train_bce 0.619 -> 0.064`) but overfit after its epoch-21 AUROC peak (`0.737`; final val BCE `1.781`). The eventual useful epoch count is unknown, so validation-driven LR reduction is safer than fixing a cosine horizon; `val_bce` is smoother for scheduling while `val_auroc` remains the model-selection endpoint. The revised pilot stopped at epoch 22 with best epoch 7 (`AUROC 0.738`, `AP 0.310`) | Resume `matched_b0_test/08_12`; retain 200 epochs / 10-epoch freeze / clip 1.0; fix a 40-epoch cosine horizon before the pilot; drive LR reductions from noisy AUROC; change data, loss, or architecture simultaneously | 2026-08-14 |
| Force Baseline 1 multiprocessing temporary files to `/tmp/vai-baseline1-{uid}` by setting `TMPDIR`, `TEMP`, `TMP`, and `tempfile.tempdir` before DataLoader construction | The project environment points `TMPDIR` at NFS-backed `.tmp`; worker finalizers then emit repeated `_remove_temp_dir` tracebacks when `.nfs*` files remain open. Local `/tmp` avoids NFS unlink semantics without changing training or saved artifacts | Ignore the shutdown traceback; set only `tempfile.tempdir`; use zero DataLoader workers | 2026-08-14 |

## TODO

<!-- Features to implement -->

- [ ] Run paired five-fold `line_2p5d` experiments with `config/baseline.yaml` and `config/geometry.yaml`, then compare per-image angle/rho/combined error and heatmap-collapse rate with sample-clustered uncertainty.
- [ ] Inspect the heatmap-only convergence epoch before fixing the final `geometry.start_epoch`; re-audit geometry gradient balance if loss definitions or context size change.

### Stage4 Recovery

- [x] Replace positive-vs-negative-only bag correction with four-stratum population-risk aggregation
- [x] Make per-batch and epoch-level strong/sampled-negative region-loss weight exactly 1:1
- [x] Add epoch-exposure manifests and DDP-global-weight invariant tests
- [ ] Re-run only corrected Weak-only fold 0 and compare its loss trajectory with Stage3
- [ ] Resume full Stage4 experiments only after explicit user review

### Stage2 Consistency Audit

- [ ] Version `train_models/stage2/config/config.yaml` and `train_models/stage2/tests/`
- [ ] Persist one common-population study split manifest for Stage1/Stage2
- [ ] Log validation `stage1_loss`, `region_loss`, and total loss separately
- [ ] Fix invalid-region masking to avoid `0 * inf -> NaN`
- [ ] Make resume config-safe and save a latest-epoch checkpoint
- [ ] Guard experiment output-directory reuse
- [ ] Remove duplicated DDP validation and all-reduce training statistics
- [ ] Run the operational `region_loss_weight=0` Stage1-parity baseline
- [ ] Run `region_loss_weight=0.5` with all other controls unchanged
- [ ] Add auxiliary region metrics and availability-aware diagnostics
- [ ] Resolve stale differential-LR and augmentation-replay assumptions
- [ ] Make Stage2 output root configurable so long-running jobs can write checkpoints/logs to `/data2` without symlink workarounds
- [ ] Add a local heartbeat file plus watchdog wrapper that auto-restarts stalled training with `--resume`

- [ ] Refactor Stage2 into the documented shared-encoder Stage1-primary plus four-region auxiliary architecture
- [ ] Execute `.claude/docs/stage2-redesign-implementation-plan.md` phase by phase
- [ ] Add a parity test proving `lambda_any=0` reproduces Stage1 plane logits, loss, and mean-sigmoid score
- [ ] Add per-plane stable four-region Noisy-OR BCE, mean-probability vertebra output, and region-collapse diagnostics without direct region BCE
- [ ] Correct Stage2 region semantics to `1=body, 2=right_foramen, 3=left_foramen, 4=posterior` and change horizontal-flip remapping to `[0,1,3,2,4]`
- [x] Replace Stage2 `collect_items()` full region-mask scan with path/exclusion filtering and add startup-phase timing logs
- [x] Add all-valid-z axial extraction from the original `annotation_data` volume using the same crop/flip/resampling path as the existing PNG dataset
- [x] Implement mask-normalized polyline interpolation/extrapolation with explicit `manual` / `interpolated` / `extrapolated` provenance
- [x] Add leave-one-anchor-out interpolation metrics and per-slice QC reports
- [x] Generate full-z `gt_masks` while preserving warnings and confidence metadata instead of deleting ambiguous endpoint slices
### High Priority: Threshold Processing Integration (2026-03-23)

- [ ] **Implement threshold processing in inference path**
  - [ ] Modify `extract_pred_line_params_batch()` in `line_only/line_losses.py`
  - [ ] Add `threshold` parameter (default: no threshold, configurable)
  - [ ] Apply `torch.where(heatmaps >= threshold, heatmaps, 0.0)` before moment calculation
  - [ ] Ensure training loss computation uses threshold=0.0 (no filtering)
  - [ ] Add unit tests for threshold vs no-threshold behavior

- [ ] **Re-train model with threshold-aware evaluation**
  - [ ] Run full training: `cd Unet && uv run python train.py`
  - [ ] Verify angle_error drops to <15° (ideally <10°)
  - [ ] Confirm confidence >0.9 on test set
  - [ ] Document training curves and final metrics

- [ ] **Alternative approaches (if needed)**
  - [ ] Implement adaptive per-channel threshold
  - [ ] Test soft weighting alternatives
  - [ ] Investigate heatmap quality improvements

### Lower Priority

- [ ] Flatten package layout (move package to repo root, update hatchling/pytest/mypy paths)
- [ ] Add provider wrapper that detects rate-limit errors and switches to Gemini CLI
 - [ ] Define wrapper CLI contract (args, env, exit codes) for Claude/Gemini switching
 - [ ] Define canonical conversation state persistence format and storage location
 - [ ] Implement context export command for Gemini (system + history + last tool results)
 - [ ] Decide on user confirmation flow for fallback
 - [ ] Unify coordinate conventions (x=col, y=row) across GT heatmap generation, angle extraction, and evaluation
 - [ ] Deprecate and remove legacy `Unet/line_detection_moments.py` after migrating any external callers to `Unet.line_only.line_detection`
 - [ ] Fix `line_only/line_metrics.py::compute_perpendicular_distance` to flip centered GT `y` into math coordinates before distance computation
 - [ ] Ensure major-axis selection in moment-based angle extraction (eigenvalues or variance check)
 - [ ] Add analytic-covariance unit tests for moment extraction (known angles, axis-swap sentinel, near-isotropic degeneracy handling)
 - [ ] Add evaluation metrics beyond angle error (e.g., perpendicular distance, Hausdorff, heatmap overlap)
- [ ] Implement minimal eval reporting set (angle_error_deg, rho_error_px, perpendicular_dist_px + one peak metric)
- [ ] Refactor train/eval pipeline: split train_one_fold/evaluate, extract line-eval/visualization utilities, and standardize metric aggregation/logging
- [ ] Refactor train_heat2.py: separate dataset validation/split, dataloader factory, model+optim+scheduler setup, epoch train step, epoch eval step, checkpoint/early-stop, and reporting
- [ ] Extract evaluation utilities: metric aggregation (overall + per-vertebra), heatmap visualization helpers, and line-detection evaluation wrapper
- [ ] Normalize non-English code comments to English; move Japanese explanations to docs
- [ ] Add crosstalk ablation suite: baseline MSE vs +anti-crosstalk loss vs +softmax-with-background formulation, and report crosstalk ratio + angle/rho changes
- [ ] Add staged crosstalk ablation: anti-crosstalk ramp timing, masked-local softmax competition window size, and decorrelation-weight sweep (`0`, `1e-4`, `5e-4`) with stability/metric tracking

## Open Questions

<!-- Unresolved issues, things to investigate -->

 - [ ] Exact Claude rate-limit error signatures to detect (exit codes/stderr text/HTTP status)
 - [ ] Gemini CLI invocation contract (flags, input format, output parsing)
 - [ ] Best source for Claude Code conversation logs (hooks vs local logs directory)
 - [ ] How much bias is introduced by GT polyline point spacing vs heatmap-based moment estimates?
- [ ] Do image-space augmentations (rot/flip/resize) apply consistently to GT angle evaluation?
- [ ] What bias does UNet smoothing/blur introduce in moment-based angle estimates, especially for short or curved lines?
- [ ] Are channel overlaps/intersections frequent enough to invalidate strict per-pixel exclusivity (softmax + background) for the 4 line classes?
- [ ] Can a held-out learned model predict each plane's continuous signed z tilt, especially for surfaces whose manual-band movement is only 1-2 px?

## Changelog

| Date | Changes |
|------|---------|
| 2026-08-23 | Fixed partial region-label validity before pseudo-label MTL. Annotation-run coverage is now a shared manifest input; 235 fully reviewed bags expose all four targets, while 33 partially reviewed bags expose recorded positives only and keep unreviewed zeros unknown. Valid cells are R1/R2/R3/R4 = 245/243/244/251, removing 89 false-zero cells from supervision. Both canonical datasets consume the per-region mask, horizontal flip swaps R2/R3 validity with targets and masks, and prediction artifacts persist per-region validity. The regenerated input-manifest SHA256 is `b5d46161c374b38456393c0dfd65893d535f12eb17595c42f0c78c2b4a36b955`; old frozen runs must not resume against it, and new MTL runs require a newly frozen manifest. All 210 tests, Ruff, a real partial-zero bag smoke, and mypy with only unavailable-stub diagnostics suppressed pass. |
| 2026-08-23 | Implemented the user-approved pseudo-ranking pair construction: form pairs only among fracture-positive bags from one fold-matched teacher and one region; exclude non-finite/non-positive CAM scores and, for combined supervision, cells with valid human targets; retain exact and near ties as soft targets near 0.5; use a fixed-seed random cyclic derangement so each eligible bag appears once on each side without self-pairs; and average pairwise loss within region before averaging active regions. Pseudo-only may include annotated cells because it does not consume their human labels. Whole-negative bags never enter CAM pairs and remain a separate exact-negative source. A real outer-0 smoke produced 3,207 valid pairs from 802 positive bags, and the full suite passes 207 tests plus Ruff and targeted mypy. |
| 2026-08-23 | Completed fold-matched pseudo-score generation: 40,296 rows cover all 13,432 bags exactly once for each of their three eligible teachers, with 20 positive-population temperatures and two undefined positive region scores to exclude during pair construction. Row-level fold/provenance invariants and score/temperature SHA256 hashes were verified; the full fracture-detection suite passes 198 tests. |
| 2026-08-23 | Fixed pseudo-ranking temperatures to fold-matched fracture-positive training bags only. CAM pairwise ranking remains conditional localization and never forms negative-negative or positive-negative CAM pairs; the all-bag comparison arm differs by adding separately source-balanced exact-negative supervision from whole-negative bags. Pseudo-score artifacts exclude human region labels and record teacher ID, teacher training folds, student outer fold, and checkpoint SHA256 explicitly. |
| 2026-08-23 | Approved the CAM-audit gate deviation and continuation to fold-matched pseudo-label generation. The audit is not described as a gate pass: the prespecified `erode_4` cross-region argmax condition failed, but cross-region argmax is outside the independent multi-label regional design, while the relevant within-region rank stability and memorization checks passed. Remaining limitations are internal validation on 268 annotated bags and unverified generalization to the unlabeled target population. |
| 2026-08-21 | Added a checkpoint-matched OOF Grad-CAM audit for Baseline 0 with fold-by-level TP/FP sampling, an all-268 annotated-bag mode, 15-plane case figures, anatomical CAM mass and density summaries, target-positive/negative comparisons, patient-cluster bootstrap intervals, and within-level rank sensitivity metrics. |
| 2026-08-21 | Audited fracture-region run coverage and corrected label validity: 33/268 partially annotated vertebrae contain 36 unreviewed runs, so recorded positives remain usable but zeros in those vertebrae are unknown; region evaluation now uses per-region positive-or-complete masks, and confirmatory region-supervised training is blocked until the same missingness is masked or completed. |
| 2026-08-21 | Retrospectively evaluated legacy Stage3 `baseline_3/v1` contextual region evidence with the corrected positive-or-complete label mask, keeping its 216-bag OOF and 52-bag fixed-test ensemble results separate and recording that these uncalibrated ranking metrics are not directly comparable as an arm effect with the newer Baseline 0 Grad-CAM audit. |
| 2026-08-21 | Added a same-case descriptive comparison of legacy Stage3 evidence against Baseline 0 CAM density with study-paired bootstrap intervals. CAM had higher macro AUROC in OOF and fixed test and higher fixed-test macro AP; OOF R3 was the only conclusive per-region difference, favoring CAM for both AUROC and AP. |
| 2026-08-21 | Recorded a pending exploratory Baseline 0 transfer-learning pilot: initialize a fold-matched 6-channel Control-B trunk and whole head from the leakage-safe Baseline 0 checkpoint, train the new region head first with corrected per-region validity, and preserve whole-task training if later unfreezing; direct Grad-CAM-loss optimization and annotation-only full-model fine-tuning are rejected for the pilot. |
| 2026-08-21 | Diagnosed the exploratory `mtl_type2` outer0 failure: a randomly initialized 4.86M-parameter region BiLSTM received sparse annotation-only updates after separation from whole-task transfer, produced near-identical R1-R3 scores and an overwhelming R4 bias, and was additionally confounded by invalid partial-label negatives, unaudited `lambda=1`, checkpoint mismatch, and source drift; blocked unchanged fold expansion. |
| 2026-08-19 | Implemented the six-arm shared core, canonical data/RNG path, Control/Baseline 1 and Proposed models, calibration/profile/freeze CLIs, and pooled-OOF analysis. Pre-freeze smoke now stops before outer inference; calibration/resource provenance is transitively hashed into the frozen manifest; formal outer rows carry that manifest hash for pooled verification. Real-data one-step GPU smoke passed for Control-B, Baseline 1-B, Proposed-B, and Proposed-max with both beta paths. Formal calibration, 20-step profiles, manifest freezing, and full training remain intentionally unstarted. |
| 2026-08-19 | Completed real-data one-step GPU smoke for all six paths including Baseline 0, then validated Control-B with two independent folds on two A6000 GPUs and a checkpoint resume. The resume smoke exposed that `torch.load(map_location=cuda)` relocates saved CPU RNG tensors; restore now normalizes every torch RNG state to contiguous CPU `uint8`. Final validation passed 111 tests, mypy on 31 source files, and Ruff format/check. |
| 2026-08-19 | Selected the three idle homogeneous RTX A6000 devices for formal fold-process execution and aligned all six frozen configs to `gpu_ids=[0,1,2]`, `max_concurrent_folds=3`, yielding the fixed fold mapping `0->0, 1->1, 2->2, 3->0, 4->1`. |
| 2026-08-19 | Started the formal calibration-to-training pipeline. The first real 64-batch calibration attempt found cuDNN LSTM backward failure under whole-model eval and Proposed-B FP32 OOM; calibration now uses train-mode recurrent/dropout modules, BN-only eval, BF16 autocast, CPU audit snapshots, and restored RNG. The restarted lambda/beta jobs are stable at approximately 21 GB and 38 GB on GPUs 0 and 1. |
| 2026-08-19 | Added optional fold-process multi-GPU execution for Baseline 1 and later arms by user instruction. Formal parallel runs use one independent fold process per GPU with explicit concurrency and a frozen fold-to-GPU mapping; within-fold DDP/DataParallel/FSDP, heterogeneous GPU assignment, output/RNG sharing, and GPU oversubscription are prohibited. This option reduces wall-clock time only and does not replace the per-run 49 GB resource gate. |
| 2026-08-19 | Approved and incorporated the joint Claude/Codex MTL implementation-plan review by user instruction. The plan now requires BN-only eval for annotated batches, isolated/checkpointed RNG streams, sequential natural/annotated backward with one optimizer step, explicit branch-local `bn2`, completion of all implementations and resource smoke tests before one final frozen manifest and any full training, separate immutable lambda/beta calibration artifacts, and corrected Proposed parameter lower bounds. The prior 174-hour schedule is withdrawn pending profiling. |
| 2026-08-18 | Bumped Baseline 0 to `baseline0-nested-v7` for Stage1 input-path parity and non-GPU-parallel speedup. Fifteen planes × five CT channels and fifteen masks are channel-stacked into one Albumentations call per bag instead of 75 replay calls. Baseline 0 skips unused region-mask loading, returns uint8 samples, normalizes after nonblocking device transfer while preserving the whole-mask 0/1 model input, and enables fixed-shape cuDNN autotuning. Local medians improved from 0.440 to 0.145 s for augmentation and 0.434 to 0.109 s for full item creation; per-item transfer payload fell from 17.23 to 4.31 MiB. The run root remains isolated at `08_18/v4`. |
| 2026-08-18 | Bumped Baseline 0 to `baseline0-nested-v6` to match Stage1 data, augmentation, mixup, and loss semantics. The shared manifest now applies the same study/level quality exclusions and is pinned at 13,432 bags / 2,009 studies / 1,332 positives (SHA256 `9bc0b8b91a5ff719519a63a3b2a7aa7f14476b45fade5582efb58a258ef21ac3`); all 268 region-annotated bags remain. Horizontal/vertical flip and transpose stay forbidden, while brightness, affine, blur/noise, distortion, cutout, and batch mixup `p=0.2` match Stage1. Whole BCE now normalizes the 2x positive-weighted loss by total weight, and epoch BCE is a mean over batch losses. The isolated run root is `08_18/v4`. |
| 2026-08-18 | Bumped Baseline 0 to `baseline0-nested-v5` for threshold-based formal evaluation. Epoch console/history/W&B logs now include precision, recall, and F1 at 0.5 plus the validation F1-optimal threshold and its metrics. Validation-AUROC-best remains primary and validation-PR-AUC-best is pre-specified as a secondary sensitivity checkpoint; each independently chooses a maximum-validation-F1 threshold with a highest-threshold tie break, then applies that frozen threshold to one corresponding outer inference. Fold and pooled artifacts report AUROC, PR-AUC, precision, recall, and F1 for both checkpoint variants. The new run root is `08_18/v3`, leaving existing v4 `08_18/v2` artifacts untouched. |
| 2026-08-18 | Added an independent Baseline 0 validation PR-AUC checkpoint by user decision and bumped the contract to `baseline0-nested-v4`. `best_model.pt` remains validation-AUROC-best and is the only checkpoint used for final validation consistency, outer inference, pooled OOF, and formal comparisons. `best_val_prauc_model.pt` stores the maximum validation average precision as a diagnostic artifact only. `last_checkpoint.pt` persists both best epochs/metric dictionaries for exact resume; history and W&B expose separate `is_best_val_auroc` / `is_best_val_prauc` states and `val_prauc`. A regression test forces AUROC and PR-AUC maxima onto different epochs and verifies the PR-AUC checkpoint contents. The user-set execution identifiers `08_18/v2` and GPU 1 are preserved, and no v4 output exists yet. |
| 2026-08-18 | Standardized Baseline 0 user-facing validation terminology on `val`: console/progress output, `history.csv`, W&B keys and summaries, `fold_metrics.json`, and `val_predictions.csv` no longer expose `inner_*`. The nested-CV structural key `runtime.inner_fold` remains internal because it validates the cyclic split assignment. Regression tests assert both console and W&B naming. |
| 2026-08-18 | Separated Baseline 0 checkpoint selection from stopping by user decision. Protocol `baseline0-nested-v3` keeps inner vertebra AUROC as the common best-checkpoint metric but drives patience 15 from inner vertebra BCE; resume checkpoints persist both AUROC-best metrics and the independent best-BCE/bad-epoch state. The already-started `08_18/v1` v2 process remains unchanged and is retained only as a superseded operational run; subsequent outputs use a distinct experiment root. W&B profiling of v1 showed train, not validation, dominates each epoch (~347 s versus ~25 s) while GPU utilization averaged only 56-60%, identifying input starvation as the primary throughput bottleneck. DataLoader workers increase from 4 to 8, per-epoch data-wait/compute timing is recorded, and every worker now calls `cv2.setNumThreads(1)` because the host OpenCV default was 32 threads/process. This prevents an 8-worker pool from expanding to roughly 256 OpenCV threads. With batch 16 and prefetch factor 2, input-tensor prefetch is approximately 4.3 GiB per active loader, safely below the measured host RAM and tmpfs margins; an 8-worker startup/batch smoke test passed outside the restricted sandbox, followed by 49 unit tests, Ruff, and mypy over 27 source files. |
| 2026-08-18 | Optimized the one-time Baseline 0 NFS-to-tmpfs stage after observed serial throughput of roughly 45-60 MiB/s for 68.3 GiB / 41,784 files. Content copies now use a configurable `ThreadPoolExecutor` (`data.stage_copy_workers=8`, validated range 1-32) and `shutil.copyfile` rather than serial `copy2`, avoiding unnecessary timestamp/metadata replication. Clearing all of `/dev/shm` on every launch was rejected because it forces the full transfer every time and removes the fastest path, READY-cache reuse. Instead, while holding the manifest-specific lock, staging deletes only abandoned `.{manifest}.tmp-{pid}` directories; completed hash-addressed caches remain reusable and atomic marker/rename integrity is unchanged. |
| 2026-08-18 | Made Baseline 0 shared-memory staging fully observable. The staging path now reports the manifest/cache destination, inter-process lock wait/acquire/release, complete source inventory scan with current path and total bytes, existing-cache marker/file-count verification, data/reserve/required/available capacity, copy byte/file/current-path progress, elapsed time and throughput, READY marker creation, atomic finalization, and cache reuse. Progress uses stdout so terminal and captured job logs show the same state. The existing integrity contract remains unchanged: copy into a PID-specific temporary directory, write the marker only after every file succeeds, then atomically rename. |
| 2026-08-18 | Reorganized `fracture_detection/baseline0/` by responsibility after the root accumulated ten implementation modules. The package root now contains only `README.md` and `__init__.py`; `cli/` owns train/evaluate entry points, `config/` owns the frozen YAML and schema validation, `data/` owns dataset/staging, `modeling/` owns network/loss, and `training/` owns optimizer/trainer/experiment management. Imports, direct CLI tests, README commands, and ignore exceptions were updated together. No compatibility wrappers remain at the root, and experiment outputs still resolve to `baseline0/outputs/`. The concurrently configured `08_18/v1` experiment identifiers were preserved: phase/name are operational output identifiers rather than scientific hyperparameters, so validation requires safe non-empty path components instead of freezing the phase to `baseline0`. |
| 2026-08-18 | Corrected Baseline 0 scheduling before any full training or outer inference. `ReduceLROnPlateau` had been inherited from the retired matched-cohort diagnostic, where the useful horizon was unknown after observed overfitting; it was not part of the RSNA Type1 full-data recipe and had no evidence base for the new 13,928-bag protocol. Protocol `baseline0-nested-v2` now uses the reference initial LR `2.3e-4`, minimum LR `2.3e-5`, and one 75-epoch cosine cycle. The notebook spells this as `CosineAnnealingWarmRestarts(T_0=75)`, but no restart occurs within its 75-epoch budget, so the implementation uses the equivalent `CosineAnnealingLR(T_max=75)`. This also removes arm/fold-specific validation feedback from the optimizer trajectory: inner AUROC is used only for checkpoint selection and early stopping. |
| 2026-08-18 | Implemented and verified the shared statistical/training contract and Baseline 0. Renamed the active package from `baseline1/` to `baseline0/`, removed matched-cohort support, froze the full-only RSNA-derived V2-S + BiLSTM configuration, and changed training to three-fold fit / cyclic inner selection / one-time outer inference. Added deterministic natural and annotated-cycle samplers, explicit-validity-only region BCE, nested split checks, state-invariant lambda/beta gradient calibration, cross-fitted Jeffreys level floors, and patient-cluster MDE artifacts. Frozen floor AP is R1 0.4946387 / R2 0.2863489 / R3 0.4222059 / R4 0.7058684 under scikit-learn 1.9.0 tie grouping. Forty-five unit tests, Ruff, mypy, and a real-bag V2-S forward/loss/backward smoke test pass. Full training and outer inference remain intentionally unstarted until the six configurations, weights, test order, and code/config hash are frozen. |
| 2026-08-18 | Froze the analysis plan on user-approved Codex recommendations, closing the last four open items. Six configurations / thirty runs replace eleven / fifty-five, with noisy-OR removed from every arm because it routes whole-loss gradient into the region logits rather than acting as pure inference-time aggregation. Confirmatory testing is a fixed sequence of `H1: Baseline 1-B > Control-B` then `H2: Proposed-max beta>0 > beta=0`, both on paired pooled-OOF vertebra AUROC judged by a patient-clustered bootstrap CI lower bound above zero; region AP is key-secondary because dropping macro-averaging makes it a four-hypothesis family whose power the existing macro-AP MDEs do not establish. The annotated stream contributes only `L_region`, and Baseline 0 runs the identical natural sampler and optimizer-step count so the whole task is distributionally identical across arms. lambda and beta come from a one-shot gradient-norm calibration on outer-training data (64 batches, eval mode, last shared CNN block), with one shared lambda per fold across all arms and zero additional full runs. The floor gate applies only to the pre-specified Proposed-B beta>0, uses a cross-fitted Jeffreys-smoothed level prior built from the three training folds, evaluates on the 268 annotated positives alone, and applies Holm over R1-R4; two descriptive sensitivity analyses (within-level percentile rank, R2/R3 swap negative control) replace SideAcc. Recomputed floors do not reproduce the recorded values and remain unfrozen pending the cross-fitted implementation and a pinned AP tie convention; per-region MDEs must also be recomputed. Six presentation limits are registered, including that the six configurations, weights, testing order, and code hash must be frozen before any outer inference. |
| 2026-08-17 | Settled the nested-selection protocol at 5 runs per configuration by dropping the stage-2 refit (user decision, compute trade-off). Each reported model trains on three folds with early stopping on the inner fold and infers the outer fold once; total compute is 0.75x the old protocol. This removes the LR-trajectory record-and-replay requirement (`ReduceLROnPlateau` now just monitors the inner fold) and the "stage 2 sees ~1.33x optimizer steps" caveat. Registered limitation: all reported models are trained on 60% of the data with region supervision cut from ~215 to ~160 annotated bags per fold, so no absolute-performance claims are made and all claims are relative comparisons under a handicap applied equally to every arm. Also corrected an earlier cost statement: the nested protocol was described as roughly doubling training cost, but that conflated run count with compute — even the rejected 10-run variant costs `0.75 + E_best/E_stop`, about 1.07-1.45x, not 2x. |
| 2026-08-17 | Dropped the SideAcc (balanced side-accuracy) endpoint by user decision; localization including laterality is now judged by per-region AP reported separately for R1-R4, never collapsed into a macro average. `side_balanced_accuracy` leaves the endpoint set and its code cleanup joins the planned `common/` pass. The pending "SideAcc aggregation and 0.65 gate" item is replaced by "per-region floor gates with four Holm-corrected tests on one preselected model". Registered consequence: SideAcc was the only shortcut-immune endpoint (level-only side accuracy was 0.511, chance, while level-only region macro-AP reaches 0.451), so resistance to the level-prior shortcut now rests entirely on per-region AP versus the level-only floors, making the corrected-label floor recomputation a precondition for pre-registration. The no-flip/no-transpose augmentation rule is unchanged because per-region evaluation still requires R2/R3 label-mask correspondence. |
| 2026-08-17 | *(The nested-protocol details in this row — stage-2 refit, 10 runs, LR replay, "doubles training cost" — were superseded later the same day by the 5-run entry above. The fold audit, no-test-set rationale, and Control arm stand.)* Evaluation-protocol review and four user-approved decisions. The frozen 5-fold was audited against the manifest and kept unchanged (no patient leakage, per-fold prevalence 10.08-10.13%, level counts 399-402, 53-56 annotated bags per fold), but the outer fold becomes evaluation-only: selection moves to a cyclic single-inner-fold (outer `k`, inner `(k+1) mod 5`, three-fold stage-1 epoch selection on inner whole-vertebra AUROC, four-fold stage-2 refit at that fixed epoch count, one outer inference), which doubles training cost, requires replaying stage 1's recorded LR trajectory because `ReduceLROnPlateau` cannot run unmonitored, and registers stage 2's ~1.33x optimizer steps. No held-out test set: a random 20% test would cut the region endpoint 268 -> 214 (R2 59 -> 47), and drawing the test only from non-annotated studies is invalid because the 160 annotated studies are measurably not a random sample of positives (31.50% versus 8.24% vertebra prevalence; 2.19 versus 1.34 fractured vertebrae per positive patient; annotated share of positive vertebrae 42.5% at C3 down to 9.4% at C7), so annotated patients have zero probability of entering the test set and positivity fails. A `Control` (no-region-mask MTL) arm is added to break the Baseline-0/Baseline-1 confound and to supply the only adequately powered region comparison; Baseline 0 emits no region logits and cannot serve that role. Still open before training: the single primary contrast, annotated-stream loss composition, SideAcc aggregation and gate definition, and the lambda/beta/mixing-ratio procedure. |
| 2026-08-17 | Second-round user decisions on the redesigned study: no entailed-zero region supervision (region loss restricted to the 268 annotated bags; remove the entailed-zero path from `common/losses.py` during implementation), method-A aggregation (max vs noisy-OR) becomes an ablation, `pos_weight=2.0` fixed for all arms, and the Proposed arm adopts PMGAN-style mask-guided attention (per-region MA modules with spatial attention maps RMSE-regressed to region masks, training-time only, `(1+m)*f` residual re-weighting; total loss `L_whole + lambda*m*L_region + beta*L_att`), which satisfies the hard-pooling ban. The frozen patient-level stratified 5-fold `folds.csv` is confirmed reusable unchanged for all arms; nested CV is unnecessary after dropping pseudo-labels. Open: lambda, two-stream mixing ratio, beta (measured loss balancing). |
| 2026-08-17 | User-directed study redesign: replaced the four-arm plan (Baseline 1/2, Proposed A/B, matched-cohort training) with full-data (13,928-bag) hard-parameter-sharing multi-task learning plus missing-label masking, after the matched fold-0 diagnostic run demonstrated overfitting on the partial cohort. New arms are Baseline 0 (CT + whole mask, the existing `baseline1/` full configuration), Baseline 1 (6ch early-fusion shared backbone with whole + 4-logit region heads), and Proposed (mask-guided four-branch model), each MTL arm compared under whole-output method A (region aggregation) and B (independent head). Region loss uses `L = L_whole + lambda*m*L_region` with masking (missing labels never treated as 0) and two-stream sampling. Discarded: Baseline 2, pseudo-labels, weak supervision, matched training, and the two-stem encoder. Open pre-implementation items: lambda, mixing ratio, entailed-zero handling, aggregation function, and the soft mask-injection mechanism (hard pooling remains banned). |
| 2026-08-14 | Redirected Baseline 1 multiprocessing temporary files from NFS-backed `.tmp` to `/tmp/vai-baseline1-{uid}` after the completed revised fold-0 run emitted repeated DataLoader finalizer cleanup tracebacks. The run artifacts were intact; this is an exit-cleanup fix for subsequent runs. |
| 2026-08-14 | Completed and then user-refined the Baseline 1 `matched` parameter audit. The diagnostic fold-0 run learned but severely overfit after epoch 21. The final revision removes the stale 10-epoch freeze, uses two-epoch all-layer warmup followed by `ReduceLROnPlateau(val_bce)`, keeps AUROC-based checkpointing/early stopping, raises global clip from 1.0 to 5.0, saves scheduler state for exact resume, adds clip-rate and validation score-gap diagnostics, and isolates the revised B0 run under `test-2/matched_b0_v2`. The old checkpoint is incompatible and must not be resumed. |
| 2026-08-12 | Deferred Baseline 1 schedule changes to the next session by user decision. The next review must jointly reconsider freeze duration, differential backbone/head LR, warmup transitions, cosine horizon, epoch budget, early-stopping eligibility/patience, and total optimizer updates. The current fold-0 partial run is a diagnostic smoke run and must not be used as an experimental result or resumed under a revised schedule. |
| 2026-08-12 | Flagged the Baseline 1 `matched` optimization schedule for revalidation: it was selected for the superseded 536-bag cohort (about 430 training bags, 27 steps/epoch), but the natural-prevalence cohort now supplies 2,122-2,125 training bags per fold (about 133 steps/epoch) while retaining the same 200-epoch schedule. No LR behavior was changed pending an explicit experiment-design decision. |
| 2026-08-12 | Added immediate Baseline 1 startup-stage logs and per-batch train/validation/final-checkpoint progress with running BCE. Epoch-start output explicitly identifies first-batch worker startup and prefetch, making NFS/data-augmentation latency observable instead of leaving the terminal silent after W&B initialization. |
| 2026-08-12 | Made the Baseline 1 training entry point directly executable as `uv run python fracture_detection/baseline1/train.py` from the repository root. The script derives and adds the project root before package imports, preserves module execution, and has a subprocess `--help` regression test. |
| 2026-08-11 | Added fixed `pos_weight=2.0` to every Baseline 1 configuration after restoring the matched cohort to the full-data prevalence. The same weight is applied to broadcast plane BCE during training and validation, saved in local/W&B effective configs and checkpoints, and config validation rejects missing or alternative values. Baseline 2 remains a separate unimplemented decision and is not changed by this entry. |
| 2026-08-11 | Replaced the unused 50:50 Baseline-1/Baseline-2 comparison cohort with a natural-prevalence cohort. It retains all 268 annotated positive bags and deterministically samples 2,387 negative bags in proportion to the full manifest's fold-by-level negative distribution, allows multiple vertebrae per patient, and matches full prevalence (10.094% versus 10.095%). The new frozen cohort has 2,655 bags/1,498 patients and SHA256 `91de42ca0475b570efb9392218c9aca0b43ce05373ecf9ec761f8527c99c6bb1`; it supersedes the never-trained 536-bag artifact. |
| 2026-08-11 | Implemented and verified the fixed matched cohort and Baseline 1. The cohort is SHA256-pinned at `b120cc7593e439ae58c44d4b8eb607505cb4b4a64120a951c32ab6feab058cb4` and enforces `train.csv` `patient_overall == 0` for every sampled negative. Baseline 1 now provides six-channel CT/whole-mask loading, non-reflective bag-coherent augmentation, timm EfficientNetV2 + BiLSTM, broadcast plain BCE with mean-sigmoid scoring, matched/full schedules, shared full-data staging, fold-safe local/W&B management, and pooled vertebra-only OOF evaluation. Related unit tests, static checks, and one real-bag forward/loss/backward smoke test pass; no training run has started |
| 2026-08-11 | Added the user-requested Baseline 1 experiment-management contract based on `Unet/line_only`: `experiment.phase/name` derive isolated local outputs, the CLI-resolved effective config is saved, each fold is one optional W&B run, epoch metrics and best/final summaries are logged, and local checkpoints/predictions remain the source of truth rather than W&B artifacts |
| 2026-08-11 | Made Baseline 1's inclusive execution range mandatory in YAML as `data.start_fold` / `data.end_fold`, kept `data.n_folds=5` as the frozen split count, validated the range explicitly, and changed the CLI fold arguments into optional recorded overrides |
| 2026-08-11 | Drafted the approval-gated Baseline 1 implementation plan: first freeze the shared matched cohort, then implement the six-channel data adapter, CNN+BiLSTM objective, staged optimizer/trainer, shared full-data staging cache, and pooled OOF evaluator. The audit also identified two contract fixes required before training: fracture-free patients must be verified against `train.csv` rather than the complete-bag manifest alone, and Baseline 1 needs a vertebra-only common evaluation entry point instead of dummy region scores |
| 2026-08-11 | Unified the fracture input under `data/rsna_data/fracture_dataset/`. `fracture_dataset_blind` was a composite view containing 234 real rebuilt bbox-study directories plus 1,778 symlinks to `fracture_dataset`; the 234 real directories were overlaid onto the full dataset with blind content taking precedence, transferring 1,712 content-different files (2.27 GB) without deleting target-only bags. All fracture runtime paths now use the unified dataset; the complete modeling cohort remains 13,928 bags/2,010 studies |
| 2026-08-11 | Fixed the Baseline-1/Baseline-2 data comparison contract. Both primary matched runs use the exact same pinned 536 bags from 428 patients: 268 annotated bags/160 patients plus 268 vertebra-negative bags from 268 distinct, non-overlapping patients, selected one vertebra per patient with fold-by-level counts matched exactly. Baseline 1 additionally supports a separately named `full` mode using all 13,928 bags/2,010 patients. In five-fold CV the matched training folds contain 340-345 patients; full training folds contain 1,608 patients |
| 2026-08-11 | Restricted `fracture_detection/common/` to invariant infrastructure only: manifest, dataset, ordinary reusable region BCE, metrics, and tests. Removed the prematurely added two-stem model and multitask-loss wrapper; every model and model-specific objective must live in its own experiment project |
| 2026-08-11 | Simplified Baseline 2 to preserve its role as a baseline: four independent region models use ordinary unweighted BCE on all outer-training annotated bags plus the matched cohort's fixed equal number of level-matched vertebra-negative bags. Removed P/H/N weighting from the primary plan and common loss; other-region fractures remain ordinary negative labels within the annotated cohort |
| 2026-08-11 | Completed the fracture-detection Phase-1 common foundation: SHA256-pinned 13,928-row manifest, deterministic 10-channel dataset contract, ordinary reusable region BCE, and study-clustered vertebra/region/side evaluation |
| 2026-08-11 | Fixed the fracture-detection augmentation contract: no horizontal/vertical flip or transpose augmentation in any arm. The common dataset preserves stored orientation and contains no R2/R3 remapping path; any later augmentation must be non-reflective and shared across arms |
| 2026-08-10 | Recorded a pending Proposed-B consultation recommendation: one full-context CNN pass with soft mask-normalized regional pooling and a shared temporal/scalar head; normalized log-mean-exp over planes and regions; asymmetric entailed-negative loss with aggregate-negative calibration for the learnable bias; no global bypass; explicit missing-region handling; and strict separation of the 268 eval-only labels from all training-time monitoring. Also recorded that I3Dr's linear sum/layer swap and patient-model rescaling do not transfer literally to nonlinear binary OR aggregation. This does not yet override the 2026-08-04 no-mask-pooling PI decision |
| 2026-08-07 | Fixed the anatomical term for R2/R3 in both annotation tools and the region-mask code: the Japanese UI text and comments said 椎間孔 (intervertebral foramen) where the study means 横突孔 (transverse foramen), the vertebral-artery canal. The annotator confirmed the 268 labels are transverse-foramen judgments, so this was a wording-only fix with no label change; English identifiers (`right_foramen` etc.) were left alone because they are dictionary keys and report names and are not wrong. Both tool legends now also state that right/left are image-side, which is the patient's opposite side |
| 2026-08-07 | Corrected the region-label aggregation rule. `run_id` was misread as a repeated annotation of the same vertebra, so a keep-latest-run deduplication was used; the annotation tool actually splits each vertebra's fracture bboxes into runs of contiguous slices, making multiple runs separate fracture sites (5-50 slice gaps) that the annotator judged separately, and the annotator confirmed every run's labels are correct as recorded. The per-vertebra label is now the OR across runs, which changes R1 77->78, R3 71->72, R4 155->158, multi-region vertebrae 65->70, and the R2-xor-R3 population 94->95; bag and study counts are unchanged. Folds were regenerated on the corrected labels. The shortcut floors, level-only macro-AP 0.451, and power MDEs still derive from the superseded counts and are flagged for recomputation before any gate is pre-registered |
| 2026-08-07 | Adopted `memo/計画書/提案手法.md` as the active study definition: four arms (Baseline 1 vertebra CNN+LSTM, Baseline 2 four independent region models, Proposed A semi-supervised pseudo-label multitask, Proposed B weakly supervised OR-constrained multitask), superseding the 2026-08-04 "no pseudo-labels in the core method" restriction. Two user decisions recorded: slice count fixed at 15 planes (existing `fracture_dataset_blind`) for all experiments with endplate-clipping limitations registered, and Baseline 2 trains on 268 annotated bags plus vertebra-negative entailed negatives |
| 2026-08-06 | Reverted a Codex-authored "Design-review recommendations" section added while the same `--full-auto`/`--sandbox read-only` override from 2026-08-05 let Codex patch this file directly again. The section was never PI-reviewed and was written against an unverified synthesis (a same-session architecture proposal) that the PI subsequently rejected in full; removed rather than partially merged |
| 2026-08-05 | Corrected two Codex-authored edits made while a sandbox override (`--full-auto` silently upgrading `--sandbox read-only` to `workspace-write`) let Codex patch this file directly: (1) slice count per vertebra was wrongly asserted as fixed at 15; the PI listed it as open, and the current 15 planes cover only 77.5% median of vertebra SI extent, so this is restored to open. (2) The design-reset decision-log row wrongly recorded a "label-blind 3D vertebra crop" as the accepted classifier input; that recommendation was proposed and withdrawn within the same 2026-08-04 session once the bbox-forced-plane leak it addressed turned out to already be fixed, and 2.5D on the existing dataset was confirmed instead |
| 2026-08-05 | Synchronized the study contract to 2.5D, 268 region-annotated vertebrae from 160 patients, and a correlation-dependent paired regional-AP MDE of 0.027-0.059 |
| 2026-08-05 | Added the confirmatory interpretation contract: regional claims are limited to multiplicity-controlled superiority over prespecified anatomical-prior floors, vertebra-level fusion superiority is tested separately, and regional arm rankings or localization-mediated explanations remain exploratory; a null regional ranking does not imply equivalence at the current MDE |
| 2026-08-04 | Superseded the old 2.5D/MIL and mask-pooling fracture models with the PI-mandated from-scratch fully supervised design boundary; recorded delayed weak-label use and per-region OOF AP against region-specific level-only floors as the recommended first validation contract |
| 2026-08-04 | Aligned `line_2p5d` evaluation config with adaptive heatmap thresholding and added configured angle/rho outlier rates; kept line extension out because this project does not render finite segments |
| 2026-08-04 | Moved the `line_2p5d` fold execution range into validated `folds.start/end` config fields while retaining optional CLI overrides |
| 2026-08-04 | Restored `line_2p5d` supervised heatmap learning to the same plain mean MSE used by `line_only`; removed weighted-MSE and Dice terms from implementation and configs |
| 2026-08-04 | Added the independent local-consistency `line_2p5d` design: explicit +/-2-slice input, slice-shared U-Net, lightweight residual z fusion, center-once supervision/evaluation, and optionally delayed local angle/position consistency without any plane or RNN assumption |
| 2026-08-03 | Specified the strict-plane experiment contract: slab-level doubled-angle normal canonicalization, regularized signed-offset fit, invariant tilt vector plus zero-initialized bottleneck head, per-surface reliability/fallback supervision, `+/-4 mm` virtual evaluation, central non-inferiority gates, and sample-clustered comparison with level/line priors |
| 2026-08-03 | Audited current 3D training scale: 40 independent samples, 284 annotated vertebrae total, 175 with at least five valid central slices, and only 24 samples / about 102-108 plane-supervised vertebrae per training fold; limited the first experiment to a compact shared 3D feasibility model |
| 2026-08-03 | Added the plane-GT fallback policy: retain reliable signed z tilt, but use zero-slope vertical extrusion for ambiguous directions while preserving provenance and confidence |
| 2026-08-03 | Corrected the immediate rollout order: defer a new 3D model, use baseline-v1 2.5D heatmaps plus differentiable strict-plane projection to validate GT fallback, tilt losses, and evaluation first, and revisit 3D only after this succeeds |
| 2026-08-03 | Clarified the plane-learning supervision priority: annotate the central transverse-foramen-visible band, judge central non-crossing anatomy first, and extend the learned plane beyond that band without requiring uncertain outer-slice annotations |
| 2026-08-03 | Validated the geometry of the plane-only line-surface definition on 700 manual surfaces: median point residual was 0.994 px, and meaningful z shifts of at least 1 px retained their sign in 93.6% of all leave-one-out fits; direct learned prediction remains unverified |
| 2026-08-03 | Clarified the full-3D line-surface problem definition only: every boundary is a plane, and its signed z-axis tilt direction must be preserved |
| 2026-07-30 | Implemented the standalone `Unet/line_surface_3d/` pipeline end to end: dense/sparse slab data, shared replay augmentation, TinyUNet heatmap baseline, differentiable doubled-angle/centroid ribbon losses, sliding inference, overlap disagreement, region-missing evaluation, and orthogonal reformat visualization |
| 2026-07-30 | Aligned `line_surface_3d` validation with `line_only`: identical GT/prediction extraction and common metrics, angle-based checkpointing, MSE-based scheduling/early stopping, plus namespaced surface diagnostics |
| 2026-07-30 | Bumped line-surface checkpoints to protocol v2, reset stale per-fold outputs on fresh runs, and stabilized eight-worker loading with prefetch one and persistent workers |
| 2026-07-30 | Fixed the 3D line-surface implementation boundary: independent `Unet/line_surface_3d/`, `Unet/line_only/` as the sole existing-code reference, and TinyUNet-based `2N -> 4N` slab heatmaps |
| 2026-07-30 | Implemented Stage4 protocol v2 recovery: four-stratum population-risk losses, exact 1:1 region-supervision exposure, exposure manifests, every-epoch validation monitoring, incompatible output/checkpoint versioning, and sampler/loss/DDP invariant tests |
| 2026-07-30 | Invalidated and stopped the initial Stage4 Weak-only run after identifying strong/weak bag-risk bias and approximately 12.5:1 effective strong/negative region-supervision exposure; added recovery requirements |
| 2026-07-29 | Fixed Stage4 implementation location at `train_models/stage4/` and fixed the confirmatory training budget at 75 epochs |
| 2026-07-29 | Documented the current Stage3 hierarchy in Architecture: six-channel 15-plane input, EfficientNetV2-S/FPN feature extraction, four-region mask pooling, shared BiLSTM contextual evidence, plane and region aggregation, and vertebra-label-only supervision |
| 2026-07-20 | Added a five-step auto-segmentation process diagram before the four-region qualitative image in the poster and Markdown draft |
| 2026-07-20 | Split the poster's proposed method into numbered anatomical four-region auto-segmentation and deep-learning region-wise fracture-detection subsections |
| 2026-07-20 | Replaced the poster's generic model flow with an explicit hierarchical weak-label architecture showing CT/mask inputs, four region evidence paths, two-stage aggregation, and vertebra-label-only supervision |
| 2026-07-20 | Simplified the company-facing poster draft to title/author, research background, proposed method, and a side-by-side results versus discussion/outlook block; removed the reference section |
| 2026-07-20 | Added the company-facing disclosure policy for the four-region hierarchical weak-label fracture summary, including genericized implementation details and evidence-score/VAI claim boundaries |
| 2026-07-20 | Designated the native-DICOM tool as the sole future manual fracture-region annotation path and restricted the bbox-centered corrected 15-plane tool to visualization/QC only |
| 2026-07-20 | Added memory-only cyan overlays of the assigned cleaned C1-C7 mask to the native-DICOM bbox annotation tool, using nearest-neighbor physical-coordinate sampling and drawing the source CSV bbox above the mask |
| 2026-07-20 | Added an independent native-DICOM bbox annotation tool that displays all 7,217 source CSV rectangles without spatial transformation, uses canonical metadata for vertebra/run assignment, and writes a separate comparison label CSV |
| 2026-07-19 | Upgraded bbox-centered QC figures to overlay four anatomical regions, mark missing region outputs, simplify display-only supersampling stair steps, and retain non-rectangular 3D-envelope intersections without changing stored occupancy |
| 2026-07-19 | Regenerated all 235 bbox-bearing studies into 536 bbox-centered runs and audited all 7,217 source rows with zero missing or duplicate assignments; 534 runs use native DICOM geometry and two runs from study `24673` retain explicit repaired-affine approximation metadata. Generated four-region masks for 527 runs and retained nine runs as existing level-QC exclusions rather than fabricating masks |
| 2026-07-19 | Implemented native-DICOM per-slice bbox-to-LPS conversion, contiguous-run 3D envelopes, corrected-plane polygon intersection with partial occupancy, bbox-centered 15-plane CT/mask regeneration, matching SDF four-region regeneration, visualization, native-geometry gating, and focused geometry tests |
| 2026-07-19 | Added a separate bbox-centered 15-plane regeneration policy using per-run occupied volume-median centers, symmetric context coverage, long-run window splitting, common-plane resampling of every modality, and study-level split preservation |
| 2026-07-19 | Separated bbox geometry correction from downstream region soft labels: defined slice-specific DICOM-to-LPS bbox envelopes, exact corrected-plane polygon intersections, multi-plane occupancy, gap preservation, and geometry-only QC before any region overlap or label weighting |
| 2026-07-15 | Restricted the fracture-region annotation tool to canonical bbox-bearing `(study, level)` pairs, removing 25 positive levels that had no bbox assigned to that vertebra |
| 2026-07-15 | Completed Phase 3 production regeneration for all 235 bbox-bearing studies and audited all 7,217 bbox rows with zero assignment/output issues; isolated four allowed empty masks to exact bbox-forced planes and recorded 83 FOV-contact and 83 orientation-boundary studies |
| 2026-07-15 | Added exact study-ID CSV filtering to `process_dataset.py` and kept non-required classifier fill planes inside the vertebra robust range after three production studies exposed gap-induced empty masks |
| 2026-07-15 | Completed Phase 4 downstream integration: regenerated `fracture_bbox_planes.csv` for 235 studies/7,217 rows with zero canonical-level mismatches, and refreshed 30 deterministic region-bbox visualizations from the CSV-only viewer |
| 2026-07-15 | Added `--clean-output` to the bbox-region viewer after `11103/C1` exposed a stale June 29 PNG beside the July 15 CSV; removed 52 old PNGs, regenerated the deterministic sample, and confirmed current `11103/C1` uses planes 0-4 with bbox overlap on C1 |
| 2026-07-15 | Completed Phase 2 validation on studies `31077`, `10678`, `28657`, and `23904`; added post-assignment bbox-anchor compression after real-data testing exposed the 15-plane limit, persisted complete per-level slice assignments, and confirmed all 341 bbox rows were assigned exactly once to positive levels with nonempty classifier masks |
| 2026-07-15 | Implemented row-level RSNA fracture-bbox assignment restricted to fracture-positive vertebrae, removed pre-assignment contiguous-run representatives and Hungarian one-to-one matching, and added focused regression coverage |
| 2026-07-10 | Updated Claude-to-Codex routing to `gpt-5.6-sol` for analysis/review and `gpt-terra` for implementation/refactoring, with reasoning effort fixed at `high` |
| 2026-07-06 | Changed the Stage2 region notebook to bbox-only, four-column visualization: one row per bbox-intersecting plane, one panel per anatomical region, numeric region score, threshold-based detection status, and yellow projected GT bbox |
| 2026-07-05 | Extended the Stage2 region viewer with source-DICOM bbox projection, yellow bbox polygons on intersecting classifier planes, bbox-bearing OOF sample filtering, and bbox-plane-only display |
| 2026-07-05 | Added the Stage2 region-head visualization notebook and helper: evidence-scaled four-region CT overlays, per-plane region/Noisy-OR curves, OOF category filtering, top-plane mode, PNG export, and explicit weak-supervision labeling |
| 2026-07-04 | Recorded Stage1 AMP stabilization direction after the fold2 epoch-11 NaN: prefer explicit BF16 over implicit FP16 when available, port Stage2-style non-finite loss/gradient diagnostics, and add validation-side finite guards plus optional parameter/buffer scans because the current failure signature points to forward-path non-finites rather than the BCE criterion itself |
| 2026-07-01 | Completed a full Stage2 consistency audit; found non-overlapping Stage1/Stage2 splits, stale differential-LR rationale, unversioned config/tests, missing validation component logs, incomplete auxiliary metrics, non-exact resume, duplicated DDP validation, and an invalid-mask `0 * inf` NaN path; recorded the remediation order |
| 2026-07-01 | Added the phased Stage2 redesign implementation plan covering Stage1 parity, shared-encoder dual paths, per-plane Noisy-OR loss, region semantics, evaluation migration, tests, rollout, and rollback |
| 2026-07-01 | Finalized region-branch supervision order: Noisy-OR over four regions separately on each plane, repeated vertebra-label BCE on every valid plane, then mean probability across planes for the auxiliary vertebra score |
| 2026-07-01 | Audited the redesigned Stage2 region contract and found the existing horizontal-flip remap follows obsolete quadrant semantics; recorded the required body/right/left/posterior IDs and right-left-only swap |
| 2026-07-01 | Confirmed that no region labels are available; finalized Stage2 region training as vertebra-label-only weak MIL with stable Noisy-OR, removed direct region BCE from the design, and restricted region outputs to evidence-score interpretation |
| 2026-07-01 | Enforced FP32 execution for the Stage2 primary BiLSTM/head and FP32 conversion of all floating prediction tensors before NumPy export, preventing BF16 validation crashes while preserving BF16 CNN/FPN autocast |
| 2026-06-30 | Replaced Stage2 FP16 autocast with capability-checked BF16 after a finite 468-batch run ended in a non-finite forward loss; added output-component diagnostics and AMP configuration tests |
| 2026-06-30 | Corrected Stage2 AMP handling so initial non-finite scaled gradients trigger GradScaler step skipping and scale reduction instead of immediate failure; added a lower initial scale, an eight-consecutive-skip failure guard, skip metrics, and regression coverage |
| 2026-06-30 | Followed up the first-batch Stage2 DDP gradient NaN by moving BiLSTM/LayerNorm/head math to FP32, fixing temperature at `1.0`, adding offending-gradient parameter diagnostics, and guaranteeing NCCL process-group cleanup |
| 2026-06-30 | Fixed Stage2 normalized-LSE `NaN` gradients for invalid regions, added gradient clipping and component diagnostics, moved multiprocessing scratch to local `/tmp`, and switched the stable baseline to two GPUs with unchanged global batch size |
| 2026-06-30 | Diagnosed and fixed Stage2 startup latency by replacing the 13,433-file region-content scan with path/exclusion filtering, retaining Dataset validation, and adding collection timing logs (`109.3 s` to `10.9 s`) |
| 2026-06-29 | Implemented `train_models/stage2/` with 56×56 FPN region pooling, shared region/global BiLSTM heads, constrained FP32 LSE aggregation, synchronized region-mask augmentation, DDP/AMP training, resumable checkpoints, OOF diagnostics, and optional manual-region evaluation |
| 2026-06-29 | Added the phased Stage2 Region MIL implementation plan and standardized its package/output directory on `train_models/stage2/` |
| 2026-06-29 | Reviewed Region-Masked MIL and chose single-pass full-plane feature extraction with mask-normalized region pooling, shared region-conditioned sequence modeling, logit-domain aggregation, and a global auxiliary path over four zero-masked CNN passes with probability noisy-or |
| 2026-06-26 | Added RSNA 4-region QC guidance: validate classifier-plane input quality before region-size judgments and reconstruct line segments exactly as `rsna_line_vis` does |
| 2026-06-29 | Added fracture-region label extraction policy: center-weighted `bbox ∩ vertebra` overlap, hybrid per-slice thresholding, and persistence-based multi-slice aggregation instead of pure OR or single-slice assignment |
| 2026-06-26 | Recorded study `24673` as requiring explicit CT/mask orientation review because its C7 max-area binary mask appears vertically flipped relative to CT |
| 2026-06-20 | Added corrected-axis 15-plane planning with robust `1%-99%` mask-volume endpoints, exact representative bbox-plane insertion, unique nearest-vertebra bbox assignment, and explicit head-to-tail sequence metadata |
| 2026-06-20 | Added physical-coordinate vertebra orientation search using a transient local `0.4 mm` isotropic mask ROI and the established max-axial-area coarse-to-fine objective; integrated corrected LPS bases and boundary QC into study metadata |
| 2026-06-20 | Added `process_study.py` as the atomic study-level integration boundary for DICOM geometry, complete C1-C7 mask processing, physical ROI/QC metadata, and in-memory handoff to orientation correction |
| 2026-06-20 | Added RSNA vertebra-mask cleaning and physical ROI extraction with explicit NIfTI RAS to DICOM LPS conversion, largest-component filtering, sampled DICOM-alignment QC, and nonzero-bbox performance optimization |
| 2026-06-20 | Revised classifier-plane selection for bbox-annotated training studies: preserve one actual annotated slice per contiguous bbox interval inside the fixed 15-plane sequence, then use remaining slots for full-range coverage and record forced positions as annotation-informed metadata |
| 2026-06-18 | Separated full-coverage classifier planes from clear line-detection anchors; added dense transient candidate sampling, no-GT quality scoring, position-binned anchor selection, and z-trajectory propagation to the final 15 planes |
| 2026-06-18 | Defined the 15-slice rule: robust `1%-99%` cumulative mask-volume endpoints on the corrected superior-inferior axis, followed by 15 uniformly spaced physical planes |
| 2026-06-18 | Reused the established max-axial-mask-area coarse-to-fine tilt search and direct oblique-plane sampling for RSNA orientation correction, adapted to physical patient coordinates for native anisotropic spacing |
| 2026-06-18 | Recorded line-detection architecture judgment: defer large 2D architecture changes on the 42-patient / 1,610-slice dataset, prioritize robust post-processing and slice-level selection/aggregation first, and reserve 2.5D inter-slice context as the highest-ROI escalation if residual edge-slice collapse persists |
| 2026-06-18 | Recorded redesigned RSNA preprocessing direction: avoid persisted full CT NIfTI, prefer DICOM-to-TotalSegmentator or temporary per-study NIfTI, keep physical-coordinate ROI management without whole-volume isotropic resampling, and emit final `224x224` slices |
| 2026-06-16 | Recorded robust line-moment estimation direction: intensity tempering plus detached soft perpendicular-band refinement, explicitly rejecting centroid-radius clipping as the primary estimator fix |
| 2026-06-16 | Added adaptive peak-relative heatmap thresholding for `Unet/line_only` evaluation and visualization extraction, with fixed-threshold backward compatibility and focused regression coverage |
| 2026-06-15 | Refactored `Unet/line_only` into dedicated evaluation, inference, example-output, and experiment-management modules while preserving the existing `trainer.py` imports; renamed three diagnostic scripts to prevent accidental pytest collection |
| 2026-06-15 | Started phased repository refactoring by extracting shared fracture-training infrastructure from the MIL and bag-only trainers into `learning/utils/training.py`, with regression tests preserving experiment-specific behavior |
| 2026-06-11 | Reverted the fixed test holdout due to the 41-patient data limit; restored all-data patient-grouped 5-fold CV with per-fold and pooled OOF reporting |
| 2026-06-11 | Replaced inward `line_2/line_4` extrapolation in the region-mask design with an explicit `J_L-J_R` central bridge and outward-only side rays to prevent false posterior intrusion into the body |
| 2026-06-11 | Restricted the central-bridge fix on z-extrapolated slices to direct propagated-junction inputs with topology guards; nearest-endpoint junction reconstruction is unsafe on clipped endpoint slices |
| 2026-06-11 | Chose finite separator-graph rasterization plus marker-based component filling as the long-term region-mask method, replacing globally acting infinite half-planes |
| 2026-06-11 | Hardened finite separator construction against unordered annotation points using TLS junction intersections and all-point projection; all 1,618 valid manual slices now generate successfully |
| 2026-06-11 | Replaced the straight central chord with the vertebra mask's anterior spinal-canal arc to remove residual posterior wedges inside the vertebral body |
| 2026-06-11 | Superseded global graph regeneration with a bounded local posterior-to-body correction; all observed changes are restricted to the intended class transition |
| 2026-06-11 | Recorded the high slice-coverage fracture finding and updated the preferred fracture-classification direction from hard top-k DMIL toward slice-supervised 2.5D mean-pooling with optional bag-consistency loss |
| 2026-06-11 | Added a ranked non-MIL follow-up plan for dense positive-slice coverage: slice-supervised mean pooling first, attention pooling second, plain mean pooling third, and max pooling only for analysis |
| 2026-06-11 | Added explicit C1-C7 one-hot conditioning to the ResNet18 fracture classifier and connected vertebra labels through both training and validation paths |
| 2026-06-11 | Removed the redundant C3-7 pooled classification-metric row; level reporting now contains only individual C1-C7 entries |
| 2026-06-11 | Switched the fracture MIL backbone from ResNet34 to ResNet18 as the next controlled tiny-data capacity ablation after run02 failed to improve fold-0 AUPRC |
| 2026-06-11 | Hardened tiny-data MIL training against rapid overfitting by freezing bag-wise BatchNorm, zeroing normalized masked backgrounds, strengthening regularization/augmentation defaults, and preventing output-run reuse by default |
| 2026-06-11 | Added hardened defaults for weakly supervised cervical fracture MIL training: logit-domain DMIL, negative-bag variance center loss, capped small-k selection, stack-consistent augmentation, and constant-LR warmup schedule |
| 2026-06-11 | Added sparse straight-line z-propagation guidance: joint junction + doubled-angle representation, interior smoothing only, and capped/damped linear tails for stable endpoint extrapolation |
| 2026-06-10 | Added tiny-data cervical fracture baseline decision: start with end-to-end 2.5D MIL with fixed top-k mean pooling instead of a two-stage CNN+RNN stack |
| 2026-01-31 | Added packaging and fallback strategy decisions; noted TODOs and open questions |
| 2026-01-31 | Added wrapper-based fallback decisions and context persistence follow-ups |
| 2026-02-18 | Recorded decision to align angle evaluation methods and added TODOs/open questions for line-angle analysis |
| 2026-03-04 | Planned refactor of training/evaluation pipeline and comment language normalization |
| 2026-03-04 | Added concrete refactor TODOs for train_heat2.py decomposition and eval utilities |
| 2026-03-10 | Added plan pattern for heatmap-moment line geometry loss (phi, rho) with sign alignment and warmup |
| 2026-03-16 | Recorded minimal line-eval metric recommendation (angle/rho core + perpendicular distance + one heatmap diagnostic); added sigma selection guidance for heatmap regression |
| 2026-03-16 | Added staged training decision for heatmap regression with angle-then-rho geometry constraints |
| 2026-03-17 | Added decision to detach prediction-derived confidence in line losses to avoid trivial collapse |
| 2026-03-17 | Added NaN-safe line-loss decision: avoid NaN sentinels inside arithmetic, use valid-only indexing, and add finite-guard training checks |
| 2026-03-17 | Recorded epoch-48 collapse diagnosis: NaN-mask arithmetic failure likely dominates, with warmup near 1.0 amplifying impact |
| 2026-03-17 | Added concrete implementation decision: use `isfinite` + valid-index slicing before angle/rho loss arithmetic to avoid NaN propagation and empty reductions |
| 2026-03-18 | Recorded implementation sequence for loss fixes: apply NaN-handling first, then warmup + smoothing adjustments (two-commit rollout) |
| 2026-03-18 | Refined loss-fix sequencing to prefer `#3 -> #2 -> #1` with per-step validation, while keeping `#3` isolation mandatory even in fast rollout |
| 2026-03-17 | Added delegation rule decision to require explicit `CRITICAL: You MUST call [Tool] CLI` prefix in subagent prompts (.claude/rules/codex-delegation.md, .claude/rules/gemini-delegation.md) |
| 2026-03-18 | Established agent workflow protocol (.claude/rules/agent-workflow.md): mandatory 6-phase process for Codex/Gemini consultation (info gathering → subagent invocation → wait for completion → read results → create implementation plan → report) |
| 2026-03-18 | Created detailed Phase 1 implementation plan (.claude/docs/loss-design-fix-plan.md): 3 critical fixes (warmup formula, double smoothing removal, NaN handling) with impact/risk assessment, test strategy, and rollback plan |
| 2026-03-18 | Verified implementation order via Codex (.claude/docs/codex/20260318-0327-implementation-order.md): confirmed #3 (NaN) → #2 (smoothing) → #1 (warmup) sequence with strong dependency rationale |
| 2026-03-18 | Added warmup strategy decision: recommend robust adaptive trigger for geometry-loss ramp (EMA + relative-improvement patience + hysteresis) instead of fixed 50-epoch warmup for 200-epoch runs |
| 2026-03-18 | Refined adaptive warmup spec with dual-gate plateau detector (EMA + rel-improvement + patience + min-epoch + latch) and post-trigger cosine ramp plus latest-start fallback for stability |
| 2026-03-18 | Added concrete adaptive warmup defaults for 200-epoch training: EMA=0.9, rel-improve threshold 0.2%, patience=5, min-epoch=20, forced latest-start=60, and 20-epoch cosine geometry ramp with `L=(1-0.5w)L_mse+wL_geo` |
| 2026-03-18 | Updated next-step priority from latest experiment outcome: defer adaptive warmup, first run geometry-loss reweighting (`lambda_rho` staged increase, then `lambda_theta`) and keep scheduler-on-warmup because monitor is `val_loss_mse` |
| 2026-03-18 | Recorded post-fix ROI decision: prefer staged `lambda_rho` increase first (not 10x jump), evaluate adaptive warmup only if geometry metrics stall after reweighting |
| 2026-03-18 | Added moment-extraction diagnostics decision: eigenvector formula is likely not the primary bug; prioritize coordinate checks, anisotropy guards, background suppression, and closed-form `atan2` phi extraction with dedicated covariance unit tests |
| 2026-03-18 | Added explicit `2x2` eigenvector-form validation: `(b, λ-a)` and `(λ-d, b)` are valid; component-swapped variants are invalid for general symmetric matrices |
| 2026-03-18 | Added convention-resolution decision for `line_losses.py`: keep prediction Y-up grid and flip GT Y around center to align `(phi, rho)` coordinate systems |
| 2026-03-18 | Clarified coordinate-convention consistency: keep pred `y_grid` negation (math coordinates) and add GT-side centered-y flip in `extract_gt_line_params` to unify `(phi, rho)` across loss/eval/visualization |
| 2026-03-18 | Added canonical boundary-adapter pattern for coordinates: keep pixel-domain processing in image frame (Y-down), keep `(phi, rho)` in math frame (Y-up), and confine conversions to GT extraction/visualization interfaces |
| 2026-03-19 | Added migration decision for line detection unification: treat `Unet/line_only/line_detection.py` as canonical, keep `Unet/line_detection_moments.py` as temporary compatibility shim, then remove |
| 2026-03-19 | Added guardrail for migration: avoid partial copy of `detect_line_moments` into legacy evaluator; use shim-based call redirection to canonical `line_only` path to prevent mixed-coordinate metrics |
| 2026-03-19 | Added evaluation-frame consistency decision: `compute_perpendicular_distance` must convert GT points to math coordinates (flip centered `y`) before distance computation |
| 2026-03-19 | Added rollout strategy detail: perform unification in two phases (shim/deprecate -> remove after usage scan + parity tests) to minimize breakage while eliminating duplicate line-detection logic |
| 2026-03-20 | Added crosstalk-mitigation decision: prioritize GT-aware anti-crosstalk suppression, treat channel-correlation loss as auxiliary, and require background channel if using spatial softmax |
| 2026-03-20 | Added concrete crosstalk objective policy: staged anti-crosstalk ramp + optional masked local (`4+bg`) competition, with decorrelation kept as low-weight auxiliary only |
| 2026-03-20 | Added architecture policy for crosstalk: defer full multi-head UNet by default, prioritize loss-level anti-crosstalk first, and use lightweight late branching/adapters before duplicating full decoders |
| 2026-03-20 | Added explicit crosstalk escalation policy with measured TinyUNet parameter cost (`483,348` -> `1,050,180`, ~`2.17x`) to gate when full multi-decoder architecture is justified |
| 2026-03-20 | Added crosstalk architecture refinement: when escalating beyond loss-level mitigation, apply head-specific skip adapters/gates with late branching before full decoder duplication, and avoid raw shared skips in full multi-head |
| 2026-03-20 | Added decision gate for 5-10% crosstalk question: full multi-head expected gain is moderate/uncertain, so keep it as last-resort unless cheaper loss-level and late-branch options fail target recovery |
| 2026-03-20 | Clarified theoretical basis of "uncertain effectiveness" for full multi-head UNet: shared-representation limits + decoder-decoupling variability + parameter-cost gate |
| 2026-03-23 | Added thresholding policy for moment extraction on sigmoid Gaussian-target heatmaps: avoid fixed absolute threshold by default, prioritize soft weighting, and if gating is needed use adaptive peak/quantile cutoffs with mass/anisotropy guards |
| 2026-03-23 | Refined moment-threshold guidance for sigma~2.5 heatmaps: avoid all-pixel moments, prefer adaptive soft gating, and if forced to fixed cutoff use ~0.1 before considering 0.2 |
| 2026-04-01 | Added vertebra region-mask generation decision: snap polyline pairs to junctions, extend them as separators, and fill the 4 anatomical regions with seed-based topology-aware assignment plus watershed fallback |
| 2026-03-23 | Added robustness decision for moment extraction: use closed-form theta + anisotropy gating, and model 5-10% off-center crosstalk as covariance amplification (`d^2`) rather than an eigenvector-formula error |
| 2026-03-23 | Added threshold API integration policy: keep extractor default unfiltered for backward compatibility, and set explicit call-site thresholds (`train=0.0`, `eval=0.2`) to prevent accidental path drift |
| 2026-03-23 | Added implementation-level threshold integration detail: single-function `torch.where` pre-processing, explicit `train=0.0`/`eval=0.2` call-site policy, and backward-compatible default `threshold=None` |
| 2026-03-23 | Added training-threshold risk policy: do not use hard `0.2` in geometry loss by default; if tested, restrict to late-phase ablation with safeguards and prefer soft gating for production |
| 2026-03-23 | Added operational guardrail for training-time hard threshold ablation: plateau-gated activation, staged threshold ramp (`0.0 -> 0.1 -> 0.2`), and rollback on spike/collapse signals |
| 2026-03-23 | **CRITICAL BUG FOUND**: meshgrid coordinate swapping in both `line_detection.py:87` and `line_losses.py:109` causes mu20/mu02 reversal, explaining vertical heatmap → horizontal line mismatch; requires coordinate system fix before any other optimizations |
| 2026-03-30 | Added vertebra-label conditioning policy for TinyUNet: start with bottleneck-only one-hot conditioning and identity-preserving `1x1` fusion init, then escalate to FiLM or multi-level conditioning only if bottleneck-only underperforms |
| 2026-03-31 | Added line-angle-target decision: anisotropic GT kernels are secondary (optimization shaping), while first-order gains should come from robust moment extraction, geometry-aware objectives, and prediction-noise reduction |
| 2026-04-01 | Added post-eval visualization decision: use line-level distribution plots plus slice-level worst-case ranking, defaulting worst-sample ordering to `max` line error with optional `mean` fallback |
| 2026-04-02 | Added concrete implementation decision for region-mask preprocessing: fixed fallback order, fixed debug schema, and explicit `swap_detected` signaling in `Unet/preprocessing/generate_region_mask.py` |
| 2026-04-02 | Added region-mask visualization decision: deterministic seeded random sampling (~20 slices from C3-C7), per-slice overlays, and 4-column combined grid output in `Unet/preprocessing/output/region_mask_viz` |
| 2026-04-06 | Replaced `Unet/preprocessing/generate_region_mask.py` with a compact half-plane partition implementation while preserving required public API and debug payload keys |
| 2026-04-06 | Added distorted-vertebra-mask handling policy: prioritize QC/triage and boundary-insensitive region generation before training-time compensation, with direct line-supervised methods as phase-2 escalation |
| 2026-04-06 | Added offline GT mask generation policy: walk all `dataset/sample*/C1-C7`, validate each `lines.json` slice, skip `hard_fail` masks, and save successful outputs as single-channel label PNGs (`0-4`) to `gt_masks/*.png` |
| 2026-04-07 | Added dataset integration policy for offline `gt_masks`: keep slices even when masks are missing, emit zero `gt_region_mask` + `has_gt_region_mask`, register `gt_mask` as augmentation `mask`, and swap left/right labels after applied horizontal flips |
| 2026-06-10 | Added annotation-tool region-mask status list and canvas overlay inspection, distinguishing ready, outdated, and missing outputs |
| 2026-06-10 | Started a new line-only retraining experiment for the expanded 1,610-slice all-vertebra annotation set |
| 2026-06-10 | Added one shared effective config file to each line-only training output directory |
| 2026-06-10 | Revised z-axis propagation to cover the full vertebra-mask z range using constrained linear-tail extrapolation, provenance/confidence metadata, and distance-aware training weights |
| 2026-06-10 | Implemented and piloted full-z line propagation on `sample1/C3`: generated 73/73 masks without exceptions; leave-one-anchor-out region Dice mean was 0.9937 and angle error mean was 0.51° |
| 2026-06-10 | Applied full-z propagation to all 284 annotated vertebrae: generated 21,507/21,507 region masks with zero generation failures; added built-in NRRD loading, preprocessing-volume fallback, resumable batch reports, and degenerate endpoint-line stabilization |
| 2026-04-07 | Updated half-plane body classification for tilted posterior walls: reverted `line_2/line_4` normal references to `centroid+[0,-1]`, anchored `line_3` body reference to `j_l`, and introduced `x_mid` side-gating (`right->s2`, `left->s4`) to stop left-body pixels from being mislabeled as posterior |
| 2026-04-08 | Recorded first-pass multitask ResUNet defaults: GN basic residual blocks with `1x1` projections, shared encoder + independent decoders, and default width `[24,48,96,192]` for partial-label training |
| 2026-04-12 | Added shared-decoder-trunk rollout guidance for multitask ResUNet: share `dec4/dec3`, keep `dec2` branch-local, ramp segmentation influence into the trunk, and treat detached boundary fusion as a later ablation |
| 2026-04-08 | Recorded initial multitask loss policy: `L_line + alpha * L_seg`, fixed `alpha` baseline (`0.02-0.05`, default `0.03`), seg mean over labeled samples only, and dynamic weighting reserved for observed stagnation or negative transfer |
| 2026-04-09 | Added multitask cervical MRI prioritization: optimize line loss / extraction robustness and C1/C7 hard-case handling before architecture expansion; keep decoder branching as an escalation path only if cheaper interventions fail |
| 2026-04-09 | Added negative-transfer recovery policy for multitask MRI: protect line quality first with staged seg activation, line-centric checkpointing, and lightweight line-specific adapters before heavier architecture changes |
| 2026-04-12 | For boundary-aware multitask ResUNet, prefer a shared decoder trunk with late branching at the last decoder block (`dec2`) over both fully duplicated decoders and pure final-layer-only dual heads; start with shared `dec4/dec3`, task-specific high-resolution refinement + separate heads, and add one-way line-to-seg boundary fusion only if plain dual-head sharing fails to improve segmentation boundaries |
| 2026-04-13 | Added vertebra-region segmentation loss decision: prioritize internal-boundary-aware CE + narrow-band Dice from GT label transitions before trying heavier surface/topology objectives or architecture changes |
| 2026-04-14 | Recorded initial `seg_sdf` baseline choices: fixed loss weighting, post-augmentation per-boundary signed-distance target generation, and `dec2` branching with shared low-resolution decoder trunk |
| 2026-04-15 | Added augmentation decision for line heatmaps: use Albumentations keypoints for polyline geometry and regenerate heatmaps after augmentation instead of interpolating existing heatmaps |
| 2026-06-18 | Implemented header-only RSNA DICOM geometry with irregular-spacing coordinate transforms and bounded-memory vectorized physical-plane sampling; retained serial header I/O after a real-study timing check showed it was small relative to segmentation |
| 2026-06-18 | Implemented a resource-bounded DICOM-first TotalSegmentator runner with seven-mask atomic completion, local-scratch temporary NIfTI fallback, one-worker-per-GPU defaults, and an eight-thread global CPU budget |
| 2026-06-18 | Switched the RSNA TotalSegmentator default to temporary dcm2niix NIfTI after two real studies exposed a missing-`Modality` failure in the library DICOM converter; added exact `--study-id` selection and forced isolated-worker exit to avoid lingering loky resource trackers |
| 2026-06-19 | Repaired bbox-positive study `1.2.826.0.1.3680043.24673` with forced dcm2niix slice merging and endpoint-derived slice-affine recovery; generated complete C1-C7 masks while preserving per-slice DICOM geometry as the bbox-mapping source of truth |
| 2026-06-20 | Implemented direct 15-plane CT/mask sampling, established bone-window uint8 output, atomic study output replacement, and quantitative soft FOV-boundary QC after a real bbox-positive pilot |
| 2026-06-20 | Corrected classifier input to native-spacing five-channel 2.5D stacks and made the orientation-search maximum-area plane an exact mandatory member of the 15 center planes; bumped output metadata to `rsna-preprocessing-v2` |
| 2026-06-20 | Added the resumable eight-worker full-dataset runner with schema validation, per-study failure isolation, JSONL progress logging, and an eight-study throughput pilot |
| 2026-06-21 | Recovered bbox-positive studies `17382` and `24673` with positive-level one-to-one bbox assignment, bbox-forced empty-mask QC, exact per-slice DICOM bbox conversion, and repaired-NIfTI geometry fallback for nonparallel gantry tilt |
| 2026-06-21 | Updated Stage1 RSNA augmentation definitions for Albumentations 2.0.8 compatibility while preserving brightness-only, affine, Gaussian-noise, and fixed-cutout behavior |
| 2026-06-21 | Migrated Stage1 mixed precision from deprecated `torch.cuda.amp` calls to device-explicit `torch.amp` APIs |
| 2026-06-21 | Added Stage1 per-batch train/validation progress and running-loss reporting so long epochs no longer appear stalled |
| 2026-06-21 | Moved Stage1 multiprocessing temporary directories from NFS-backed project `.tmp` to per-user local `/tmp` to prevent DataLoader finalizer `EBUSY` tracebacks |
| 2026-06-22 | Accelerated Stage1 DataLoader delivery with uint8 worker output, device-side normalization, vectorized validation loading, persistent workers, configurable prefetch, and worker thread caps without changing batch size or training semantics |
| 2026-06-24 | Added config-gated Stage1 patient-level auxiliary head with `patient_overall` labels and fixed slice/patient loss weights |
| 2026-06-25 | Updated RSNA line restoration policy: extract infinite lines from predicted heatmap moments, render each `line_1`-`line_4` with its own training-set average line length, and feed the same average-length segments into region partitioning |
| 2026-06-28 | Unified RSNA SDF z-propagation line-to-mask restoration with `rsna_line_vis` by preserving/interpolating heatmap centroids and using per-line training-set average segment lengths |
| 2026-06-28 | Replaced fixed four-region/minimum-area SDF QC with a five-study reference-distribution score over z-wise area trends, presence, centroid layout, and compactness |
| 2026-06-28 | Replaced whole-study exclusion for bbox-negative reference-QC outliers with 496 vertebra-level exclusions, preserving unaffected levels and bbox-positive failures |
| 2026-06-28 | Regenerated 13,433 non-excluded RSNA vertebra region masks with the centroid/average-line-length method and added 15 insufficient-anchor levels to `excluded_levels.csv` |
| 2026-07-01 | Isolated Stage2 training from VS Code instability by excluding large data/output trees from editor watching and requiring detached terminal execution for long runs without reducing training throughput |
| 2026-07-15 | Added Stage3 hierarchical weak-label implementation: no direct head, contextual evidence pooling, deterministic spatial-scramble control, and separate parity/safety gradient-clip protocols |
| 2026-07-16 | Made Stage3 training operational with config-selected GPU IDs, mp.spawn DDP, BF16 CNN/FPN execution, FP32 hierarchical heads and pooling, local dataset staging, early stopping, model/optimizer/scheduler continuation, and AUROC-best checkpoints. Resume is not bitwise-exact because persistent DataLoader-worker RNG state is not checkpointed |
| 2026-07-16 | Made Stage3 `data.start_fold`, `data.end_fold`, and `data.n_folds` required configuration values with explicit range validation; removed silent fold defaults from the runtime |
| 2026-07-16 | Added rank-0-only Stage3 W&B tracking configured from YAML, including hierarchical loss components, validation metrics, optimizer-group learning rates, patience, and fold-best summaries |
| 2026-07-16 | Kept the Stage3 W&B configuration surface identical to Stage1 (`enabled`, `project`, `run_name`) and derive the project/run names only when those values are null |
| 2026-07-16 | Restored the Stage1/Stage2 fixed 20% test holdout in Stage3 before CV splitting and added test ensemble predictions, per-fold predictions, contextual-evidence output, metrics, and holdout-isolation regression tests |
| 2026-07-16 | Audited Stage3 parity, excluded the sole unrecoverable missing-region level (`29949/C1`) after regeneration failed from insufficient line anchors, verified identical 13,432-row Stage1/2/3 manifests and all five fold assignments, blocked pre-holdout checkpoints, made fold aggregation persistent across sessions, reset fold seeds, and added automatic evidence-collapse diagnostics |
| 2026-08-04 | Added `line_only`-style post-training heatmap examples, TEST-wide line comparison images, heatmap/line panels, prediction JSON, and functional `line_extend_ratio` rendering to `line_2p5d` |
| 2026-08-04 | Switched `line_2p5d` experiment management from `output_dir + name` to required `phase + name` with automatic `Unet/outputs/{phase}/{name}` derivation |
| 2026-08-04 | Added config-driven fold-level W&B initialization, scalar epoch logging, best-checkpoint summaries, and test summaries to `line_2p5d` |
| 2026-08-04 | Recorded the completed `line_2p5d` MSE-only fold0 result: angle outliers were unchanged versus matched `line_only`, rho error/outliers regressed (especially `line_4`), and the next gate is a same-split geometry-enabled fold0 run before any five-fold expansion |
| 2026-08-05 | Refined the transverse-foramen symmetry-sharing policy: retain/remap original side, use patient-grouped validation, evaluate R2/R3 separately, and test side-specific calibration/adapters for symmetry failure |
| 2026-08-05 | Defined the primary region endpoint as four per-region pooled-OOF APs with patient-bootstrap CIs and paired excess over each region's level-only floor; retained raw macro-AP as secondary descriptive reporting only |
| 2026-08-20 | Made W&B tracking default-on for every shared-core training arm, added fold-specific resumable runs that log the complete persisted epoch row, and added history.csv backfill so tracking omissions never require retraining |
